
&НаСервере
Процедура бит_ПриСозданииНаСервереПосле(Отказ, СтандартнаяОбработка)
	
	// {{ПервыйБИТ ЛистТребований [ЛТ6.1] [ASHolyavko@1cbit.ru] [10.02.2026] +        
	
	ТекстНаряды = 
	
	"ВЫБРАТЬ
	|	бит_Наряд.Ссылка.ДокументОснование КАК ПроизводствоБезЗаказаСсылка,
	|	бит_Наряд.Ссылка КАК Наряд
	|ПОМЕСТИТЬ вт_Наряды
	|ИЗ
	|	Документ.бит_Наряд КАК бит_Наряд
	|ГДЕ
	|	НЕ бит_Наряд.Ссылка.ПометкаУдаления
	|	И бит_Наряд.Ссылка.ДокументОснование ССЫЛКА Документ.ПроизводствоБезЗаказа
	|
	|СГРУППИРОВАТЬ ПО
	|	бит_Наряд.Ссылка.ДокументОснование,
	|	бит_Наряд.Ссылка
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|"; 
	
	Список.ТекстЗапроса = ТекстНаряды + Список.ТекстЗапроса;
	
	Список.ТекстЗапроса = СтрЗаменить(Список.ТекстЗапроса, 
	"Документ.ПроизводствоБезЗаказа КАК ПроизводствоБезЗаказа
	|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.СостоянияПоОбъектамУчетаЭДО КАК СостоянияЭД
	|		ПО (СостоянияЭД.СсылкаНаОбъект = ПроизводствоБезЗаказа.Ссылка)",
	"Документ.ПроизводствоБезЗаказа КАК ПроизводствоБезЗаказа
	|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.СостоянияПоОбъектамУчетаЭДО КАК СостоянияЭД
	|		ПО (СостоянияЭД.СсылкаНаОбъект = ПроизводствоБезЗаказа.Ссылка)
	|		ЛЕВОЕ СОЕДИНЕНИЕ вт_Наряды КАК вт_Наряды
	|		ПО ПроизводствоБезЗаказа.Ссылка = вт_Наряды.ПроизводствоБезЗаказаСсылка");
	
	Список.ТекстЗапроса = СтрЗаменить(Список.ТекстЗапроса, 
	"ПроизводствоБезЗаказа.Ссылка,", 
	"ПроизводствоБезЗаказа.Ссылка, 
	|	вт_Наряды.Наряд есть null КАК НетНаряда,");
	
	
	НовыйЭлемент = Этаформа.Элементы.Вставить("бит_НетНаряда", Тип("ПолеФормы"), Элементы.Список);
	НовыйЭлемент.Вид = ВидПоляФормы.ПолеВвода;
	НовыйЭлемент.ПутьКДанным = "Список.НетНаряда";
	НовыйЭлемент.Заголовок = "(бит) Нет наряда";
	НовыйЭлемент.Видимость = Ложь;
	
	//
	Элемент = УсловноеОформление.Элементы.Добавить();

	ПолеЭлемента = Элемент.Поля.Элементы.Добавить();
	ПолеЭлемента.Поле = Новый ПолеКомпоновкиДанных(Элементы.Список.Имя);
	
	ОтборЭлемента = Элемент.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ОтборЭлемента.ЛевоеЗначение = Новый ПолеКомпоновкиДанных("Список.НетНаряда");
	ОтборЭлемента.ВидСравнения = ВидСравненияКомпоновкиДанных.Равно;
	ОтборЭлемента.ПравоеЗначение = Истина;
	ОтборЭлемента.Использование = Истина;
	
	Элемент.Оформление.УстановитьЗначениеПараметра("ЦветФона", WebЦвета.СветлоРозовый);
	Элемент.Использование = Истина;
	
	//
	Элемент = УсловноеОформление.Элементы.Добавить();

	ПолеЭлемента = Элемент.Поля.Элементы.Добавить();
	ПолеЭлемента.Поле = Новый ПолеКомпоновкиДанных(Элементы.Список.Имя);
	
	ОтборЭлемента = Элемент.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ОтборЭлемента.ЛевоеЗначение = Новый ПолеКомпоновкиДанных("Список.НетНаряда");
	ОтборЭлемента.ВидСравнения = ВидСравненияКомпоновкиДанных.Равно;
	ОтборЭлемента.ПравоеЗначение = Ложь;
	ОтборЭлемента.Использование = Истина;
	
	Элемент.Оформление.УстановитьЗначениеПараметра("ЦветФона", WebЦвета.Аквамарин);
	Элемент.Использование = Истина;
	
	// }}ПервыйБИТ ЛистТребований [ЛТ6.1] [ASHolyavko@1cbit.ru] [10.02.2026] -
	
КонецПроцедуры
