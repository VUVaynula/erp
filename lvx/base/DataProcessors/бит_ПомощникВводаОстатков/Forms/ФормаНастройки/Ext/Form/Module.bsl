
#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Объект.ВыводитьСуммуРегл 	 = Параметры.ВыводитьСуммуРегл;
	Объект.ВыводитьСуммуУпр 	 = Параметры.ВыводитьСуммуУпр;	
    Объект.ВыводитьСуммуМУ       = Параметры.ВыводитьСуммуМУ;
	Объект.ВыводитьСуммуСценарий = Параметры.ВыводитьСуммуСценарий;
	Объект.РегистрПриемник 		 = Параметры.РегистрПриемник;
	Объект.СкрыватьПустыеСтроки  = Параметры.СкрыватьПустыеСтроки;
	
	УстановитьВидимость();
	
КонецПроцедуры // ПриСозданииНаСервере()

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура ОК(Команда)
	
	СтруктураПараметров = Новый Структура;
	СтруктураПараметров.Вставить("ВыводитьСуммуРегл"	, Объект.ВыводитьСуммуРегл);
	СтруктураПараметров.Вставить("ВыводитьСуммуУпр"	    , Объект.ВыводитьСуммуУпр);
	СтруктураПараметров.Вставить("ВыводитьСуммуМУ"	    , Объект.ВыводитьСуммуМУ);
	СтруктураПараметров.Вставить("ВыводитьСуммуСценарий", Объект.ВыводитьСуммуСценарий);
	СтруктураПараметров.Вставить("СкрыватьПустыеСтроки" , Объект.СкрыватьПустыеСтроки);
	
	Закрыть(СтруктураПараметров);
	
КонецПроцедуры // ОК()

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

&НаСервере
Процедура УстановитьВидимость()

	ПриемникХозрасчетный   = Объект.РегистрПриемник.ИмяОбъекта = "Хозрасчетный";
	ПриемникМеждународный  = Объект.РегистрПриемник.ИмяОбъекта = "бит_Дополнительный_2";
	ПриемникБюджетирование = Объект.РегистрПриемник.ИмяОбъекта = "бит_Бюджетирование";
	
	Если ПриемникХозрасчетный Тогда
		Элементы.ВыводитьСуммуУпр.Видимость = Ложь;
	Иначе
		Элементы.ВыводитьСуммуУпр.Видимость = Истина;
	КонецЕсли;
	Если ПриемникМеждународный Тогда
		Элементы.ВыводитьСуммуМУ.Видимость = Истина;
	Иначе
		Элементы.ВыводитьСуммуМУ.Видимость = Ложь;
	КонецЕсли;
	Если ПриемникБюджетирование Тогда
		Элементы.ВыводитьСуммуСценарий.Видимость = Истина;
	Иначе
		Элементы.ВыводитьСуммуСценарий.Видимость = Ложь;
	КонецЕсли;

КонецПроцедуры // УстановитьПоказатели()

#КонецОбласти 



