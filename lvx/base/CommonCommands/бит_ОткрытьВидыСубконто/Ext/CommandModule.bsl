
#Область ОбработчикиСобытий

&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	
	// Сформируем список выбора планов видов характеристик.
	СписокВыбора = СформироватьСписокОбъектовДляВыбора();
	
    ВыбранныйЭлемент = СписокВыбора.ВыбратьЭлемент("Выбор объекта: План видов характеристик");
    
    Если ВыбранныйЭлемент = Неопределено Тогда
        Возврат;
    КонецЕсли;
    
    // Откроем форму списка выбранного ПВХ счетов.
	ИмяОбъекта = ВыбранныйЭлемент.Значение; 
	ОткрытьФорму("ПланВидовХарактеристик." + ИмяОбъекта + ".ФормаСписка");
	
КонецПроцедуры // ОбработкаКоманды()

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

// Формирует список имен планов видов характеристик БИТ.
// 
&НаСервере
Функция СформироватьСписокОбъектовДляВыбора()
	
	СписокВыбора = бит_УправленческийУчет.СформироватьСписокОбъектовДляВыбора(Перечисления.бит_ВидыОбъектовСистемы.ПланВидовХарактеристик, "бит_ВидыСубконтоДополнительные");
	
	Для Каждого ТекЭлемент Из СписокВыбора Цикл
		ТекЭлемент.Значение = ТекЭлемент.Значение.ИмяОбъекта;
	КонецЦикла;
	
	Возврат СписокВыбора;
	
КонецФункции // СформироватьСписокОбъектовДляВыбора()

#КонецОбласти
