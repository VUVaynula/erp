
#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	бит_РаботаСДиалогамиСервер.ФормаСпискаРегистраПриСозданииНаСервере(Отказ, СтандартнаяОбработка, ЭтотОбъект);
	
	УстановитьВидимостьДоступность();
	
КонецПроцедуры // ПриСозданииНаСервере()

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура УдалитьЗаписи(Команда)
	
	Оповещение = Новый ОписаниеОповещения("УдалитьЗаписиЗавершение", ЭтотОбъект);
	
	ТекстВопроса = НСтр("ru='Несогласованное удаление записей по версиям объектов может привести к нарушению последовательности записей и в дальнейшем к недостоверности отчета по изменениям. Продолжить?';
						|en='Uncoordinated removal of records in objects versions can lead to a violation of the sequence of records and subsequently to the inaccuracy of the report on changes. Continue?'");
	ПоказатьВопрос(Оповещение, ТекстВопроса, РежимДиалогаВопрос.ДаНет, , КодВозвратаДиалога.Нет, НСтр("ru='Предупреждение';en='Warning'"));
	
КонецПроцедуры // УдалитьЗаписи()

// Процедура - завершение обработчика команды "УдалитьЗаписи".
// 
&НаКлиенте
Процедура УдалитьЗаписиЗавершение(Ответ, ДополнительныеПараметры) Экспорт
	
	Если Ответ = КодВозвратаДиалога.Да Тогда
		УдалитьВерсииИЗРегистра(Элементы.Список.ВыделенныеСтроки);
	КонецЕсли;

КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытийЭлементовТаблицыФормыСписок

&НаКлиенте
Процедура СписокПередНачаломДобавления(Элемент, Отказ, Копирование, Родитель, Группа)
	
	Отказ = Истина;
	
КонецПроцедуры // СписокПередНачаломДобавления()

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

// Процедура управляет аидимость и доступностью элементов формы.
// 
&НаСервере
Процедура УстановитьВидимостьДоступность()
	
	ВерсионированиеВключено = Ложь;
	
	Элементы.ФормаУдалитьЗаписи.Видимость 			  = ВерсионированиеВключено;
	Элементы.ГруппаВерсионированиеОтключено.Видимость = Не ВерсионированиеВключено;
	
КонецПроцедуры // УстановитьВидимостьДоступность()

// Процедура удаляет записи версий из регистра.
// 
&НаСервере
Процедура УдалитьВерсииИЗРегистра(знач МассивКлючейЗаписей)
	
	Для Каждого КлючЗаписи Из МассивКлючейЗаписей Цикл
		
		НаборЗаписей = РегистрыСведений.бит_УдалитьВерсииОбъектов.СоздатьНаборЗаписей();
		
		НаборЗаписей.Отбор.Объект.Значение      = КлючЗаписи.Объект;
		НаборЗаписей.Отбор.Объект.ВидСравнения  = ВидСравнения.Равно;
		НаборЗаписей.Отбор.Объект.Использование = Истина;
		
		НаборЗаписей.Отбор.НомерВерсии.Значение 	 = КлючЗаписи.НомерВерсии;
		НаборЗаписей.Отбор.НомерВерсии.ВидСравнения  = ВидСравнения.Равно;
		НаборЗаписей.Отбор.НомерВерсии.Использование = Истина;
		
		НаборЗаписей.Записать(Истина);
		
	КонецЦикла;
	
	Элементы.Список.Обновить();
	
КонецПроцедуры // УдалитьВерсииИЗРегистра()

#КонецОбласти
