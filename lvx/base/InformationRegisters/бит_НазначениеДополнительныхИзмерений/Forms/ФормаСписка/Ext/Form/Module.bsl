
#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	бит_РаботаСДиалогамиСервер.ФормаСпискаРегистраПриСозданииНаСервере(Отказ, СтандартнаяОбработка, ЭтотОбъект);
    
	Если Отказ Тогда
		Возврат;
	КонецЕсли;
	
	УстановитьМонопольный();
	
КонецПроцедуры // ПриСозданииНаСервере()

&НаКлиенте
Процедура ПриЗакрытии(ЗавершениеРаботы)
	
	Если НЕ ЗавершениеРаботы Тогда
		СнятьМонопольный();
	КонецЕсли; 
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытийЭлементовТаблицыФормыСписок

&НаКлиенте
Процедура СписокПередУдалением(Элемент, Отказ)
	
	ТекущиеДанные = Элементы.Список.ТекущиеДанные;
	ПередУдалениемЭлементаСписка(ТекущиеДанные.ИмяИзмерения, Отказ);
		
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура КомандаУстановитьМонопольный(Команда)
	
	УстановитьМонопольный();
	
КонецПроцедуры

&НаКлиенте
Процедура ЗаменитьТипыАналитикВДокументах(Команда)
	
	ОткрытьФорму("Обработка.бит_ЗаменаТиповАналитикВДокументах.Форма",, ЭтотОбъект);
	
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

// Процедура устанавливает монопольный режим, если не удалось блокируем форму.
// 
&НаСервере
Процедура УстановитьМонопольный()
	
	Попытка
		
		УстановитьМонопольныйРежим(Истина);
		
		ТекстИнформация = НСтр("ru='Редактирование настроек дополнительных измерений осуществляется в МОНОПОЛЬНОМ режиме. 
								   |Пока открыта данная форма другие пользователи не могут работать с базой.';
						  	   |en='Редактирование настроек дополнительных измерений осуществляется в МОНОПОЛЬНОМ режиме. 
								   |Пока открыта данная форма другие пользователи не могут работать с базой.'");
		
		Элементы.ТекстИнформация.ЦветТекста = WebЦвета.Синий;		
		
		ТолькоПросмотр = Ложь;
		
	Исключение
		ТолькоПросмотр = Истина;
		
		ТекстИнформация = НСтр("ru='Не удалось установить МОНОПОЛЬНЫЙ режим (с базой работают другие пользователи или недостаточно прав).
								   |Редактирование настроек дополнительных измерений осуществляется в МОНОПОЛЬНОМ режиме.';
						  	   |en='Не удалось установить МОНОПОЛЬНЫЙ режим (с базой работают другие пользователи или недостаточно прав).
								   |Редактирование настроек дополнительных измерений осуществляется в МОНОПОЛЬНОМ режиме.'");
		
		Элементы.ТекстИнформация.ЦветТекста = WebЦвета.Красный;
		
	КонецПопытки;	
	
КонецПроцедуры

// Процедура снимает монопольный режим.
// 
&НаСервере
Процедура СнятьМонопольный()
	
	Попытка
		
		УстановитьМонопольныйРежим(Ложь);
		
	Исключение
		
	КонецПопытки;
	
КонецПроцедуры

&НаСервере
Процедура ПередУдалениемЭлементаСписка(ИмяИзмерения, Отказ)
	
	Если бит_МеханизмДопИзмерений.ЕстьДвиженияПоАналитике(ИмяИзмерения, Истина) Тогда		
		Отказ = Истина;		
	КонецЕсли;
	
	бит_МеханизмДопИзмерений.УдалитьОбязательностьЗаполненияВСтатьеОборотов(ИмяИзмерения);
	
КонецПроцедуры

#КонецОбласти

