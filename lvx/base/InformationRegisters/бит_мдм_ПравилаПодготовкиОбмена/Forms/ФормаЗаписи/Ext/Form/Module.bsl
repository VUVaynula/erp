
#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	бит_РаботаСДиалогамиСервер.ФормаЗаписиРегистраПриСозданииНаСервере(Отказ, СтандартнаяОбработка, ЭтотОбъект);

	// Сформируем список выбора доступных видов ИБ - базы расположенные на веб-сервере.
	Запрос = Новый Запрос;
	Запрос.УстановитьПараметр("ТекущаяБаза", Справочники.бит_мпд_ВидыИнформационныхБаз.ТекущаяИнформационнаяБаза);
	Запрос.Текст = 
	"ВЫБРАТЬ
	|	бит_мпд_ВидыИнформационныхБаз.Ссылка
	|ИЗ
	|	Справочник.бит_мпд_ВидыИнформационныхБаз КАК бит_мпд_ВидыИнформационныхБаз
	|ГДЕ
	|	бит_мпд_ВидыИнформационныхБаз.Ссылка <> &ТекущаяБаза";
				   
	Результат = Запрос.Выполнить();
	
	Выборка = Результат.Выбрать();	
	Пока Выборка.Следующий() Цикл	
		Элементы.ВидИнформационнойБазыПриемник.СписокВыбора.Добавить(Выборка.Ссылка);	
	КонецЦикла; 
	
КонецПроцедуры // ПриСозданииНаСервере()

#КонецОбласти
