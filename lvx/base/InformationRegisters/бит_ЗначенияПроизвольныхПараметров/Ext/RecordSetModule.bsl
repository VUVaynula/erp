#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ОбработчикиСобытий

// Процедура - обработчик события "ПередЗаписью" НабораЗаписей.
// 
Процедура ПередЗаписью(Отказ, Замещение)
	
	Если ОбменДанными.Загрузка Тогда
		// В случае выполнения обмена данными не производить проверку.
		Возврат;
	КонецЕсли;
	
	ПривестиПустыеЗначенияАналитикСоставногоТипа(ЭтотОбъект);
			
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

Процедура ПривестиПустыеЗначенияАналитикСоставногоТипа(НаборЗаписей)
	
	Если НаборЗаписей.Количество() = 0 Тогда
		Возврат;
	КонецЕсли;
	
	ПолноеИмяМетаданного     = НаборЗаписей.Метаданные().ПолноеИмя();
	МассивСоставныхИзмерений = бит_ОбщегоНазначенияПовтИсп.ИзмеренияСоставногоТипа(ПолноеИмяМетаданного);
	
	Если МассивСоставныхИзмерений.Количество() = 0 Тогда
		Возврат;
	КонецЕсли;
	
	Для каждого Запись Из НаборЗаписей Цикл
		Для каждого СоставноеИзмерение Из МассивСоставныхИзмерений Цикл
			ЗначениеИзмерения = Запись[СоставноеИзмерение];
			Если ЗначениеИзмерения <> Неопределено И НЕ ЗначениеЗаполнено(ЗначениеИзмерения) Тогда
				Запись[СоставноеИзмерение] = Неопределено;
			КонецЕсли;
		КонецЦикла;
	КонецЦикла;
	
КонецПроцедуры

#КонецОбласти

#Иначе
  ВызватьИсключение НСтр("ru='Недопустимый вызов объекта на клиенте.';en='Invalid object call on client'");
#КонецЕсли