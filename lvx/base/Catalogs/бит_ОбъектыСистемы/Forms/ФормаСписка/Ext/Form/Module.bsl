
#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	// Стандартные действия при создании на сервере.
  	бит_РаботаСДиалогамиСервер.СписокПриСозданииНаСервере(Отказ, СтандартнаяОбработка, ЭтотОбъект);
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура КомандаОбновитьОбъектыСистемы(Команда)
	
	ТекстСостояние = НСтр("ru='Выполняется обновление объектов системы...';en='Updating system objects ...'");
	Состояние(ТекстСостояние);
	
	ОбновитьОбъектыСистемы();
	
КонецПроцедуры // КомандаОбновитьОбъектыСистемы()

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

// Процедура выполняет обновление объектов системы.
// 
&НаСервере
Процедура ОбновитьОбъектыСистемы()

	бит_УправлениеОбъектамиСистемы.ОбновитьОбъектыСистемы();
	ТекстСообщения = НСтр("ru='Объекты системы обновлены.';en='System objects updated'");
	ОбщегоНазначения.СообщитьПользователю(ТекстСообщения);

КонецПроцедуры // ОбновитьОбъектыСистемы()

#КонецОбласти

