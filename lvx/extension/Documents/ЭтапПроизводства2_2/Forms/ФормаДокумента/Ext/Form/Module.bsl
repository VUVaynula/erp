
&После("ПриСозданииНаСервере")
Процедура бит_ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	// {{ПервыйБИТ ЛистТребований [ЛТ6.9] [Микляев К.А.] [04.06.2026] +
	СоздатьПолеПризнакаПечати();
	// }}ПервыйБИТ ЛистТребований [ЛТ6.9] [Микляев К.А.] [04.06.2026] -

	// {{ПервыйБИТ ЛистТребований [ЛТ6.10]  [VUVaynula@1cbit.ru] [08.07.2026 18:46:04] +
	НовыйЭлемент = Элементы.Добавить("бит_ПечатьПаспорта", Тип("ПолеФормы"), Элементы.Группа11);
	НовыйЭлемент.Вид = ВидПоляФормы.ПолеФлажка;
	НовыйЭлемент.ПутьКДанным = "Объект.бит_ПечатьПаспорта";
	
	НовыйЭлемент = Элементы.Добавить("бит_ПаспортРаспечатан", Тип("ПолеФормы"), Элементы.Группа11);
	НовыйЭлемент.Вид = ВидПоляФормы.ПолеФлажка;
	НовыйЭлемент.ПутьКДанным = "Объект.бит_ПаспортРаспечатан"; 
	
	Команда = Команды.Добавить("бит_ПечатьФайловГП");
	Команда.Заголовок = "(бит) Печать файлов ГП";
	Команда.Действие = "бит_ПечатьФайловГП";
	Кнопка = Элементы.Добавить("бит_ПечатьФайловГП", Тип("КнопкаФормы"), ЭтаФорма.КоманднаяПанель);
	Кнопка.ИмяКоманды = "бит_ПечатьФайловГП";
	// }}ПервыйБИТ ЛистТребований [ЛТ6.10]  [VUVaynula@1cbit.ru] [08.07.2026 18:46:04] -
КонецПроцедуры

&НаКлиенте
Процедура бит_ПечатьФайловГП(Команда)
// {{ПервыйБИТ ЛистТребований [ЛТ6.10]  [VUVaynula@1cbit.ru] [10.07.2026 1:48:38] +        
	Если не Объект.Проведен или Модифицированность Тогда
		ОбщегоНазначенияКлиентСервер.СообщитьПользователю("Сначала необходимо провести документ");
		Возврат;
	КонецЕсли;
	Если Объект.бит_ПечатьПаспорта = Ложь Тогда 
		ОбщегоНазначенияКлиентСервер.СообщитьПользователю("Из этапа " + Объект.Ссылка + " не печатается паспорт ГП");
		Возврат;
	КонецЕсли;
	
 	Массив = Новый Массив;
	Массив.Добавить(Объект.Ссылка);
	ФайлыКПечати = ПолучитьФайлыГПНаСервере(Массив);
	
	Если ФайлыКПечати.Количество() = 0 Тогда
		Сообщить("Нет прикрепленных файлов для печати.");
		Возврат;
	КонецЕсли;
	
	РаботаСФайламиКлиент.НапечататьФайлы(ФайлыКПечати, УникальныйИдентификатор);    
	Объект.бит_ПаспортРаспечатан = Истина;     
	Модифицированность = Истина; 
	ОбщегоНазначенияКлиентСервер.СообщитьПользователю("Установлен признак `Паспорт распечатан` необходимо записать документ для сохранения изменений");
// }}ПервыйБИТ ЛистТребований [ЛТ6.10]  [VUVaynula@1cbit.ru] [10.07.2026 1:48:38] -	
КонецПроцедуры 

&НаСервереБезКонтекста
Функция ПолучитьФайлыГПНаСервере(Массив) 
	// {{ПервыйБИТ ЛистТребований [ЛТ6.10]  [VUVaynula@1cbit.ru] [10.07.2026 1:55:11] +        
	Возврат Документы.ЭтапПроизводства2_2.ПолучитьФайлыГП(Массив);
	// }}ПервыйБИТ ЛистТребований [ЛТ6.10]  [VUVaynula@1cbit.ru] [10.07.2026 1:55:11] -
КонецФункции

&НаСервере
Процедура СоздатьПолеПризнакаПечати()
	// {{ПервыйБИТ ЛистТребований [ЛТ6.9] [Микляев К.А.] [04.06.2026] +
	Если Элементы.Найти("бит_Напечатано") <> Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	Родитель = Элементы.Найти("Группа4");
	Если Родитель = Неопределено Тогда
		Родитель = Элементы.Найти("ГруппаДополнительно");
	КонецЕсли;
	Если Родитель = Неопределено Тогда
		Родитель = Элементы.Найти("СтраницаДополнительно");
	КонецЕсли;
	
	Если Родитель = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	НовыйЭлемент = Элементы.Добавить("бит_Напечатано", Тип("ПолеФормы"), Родитель);
	НовыйЭлемент.Вид = ВидПоляФормы.ПолеФлажка;
	НовыйЭлемент.ПутьКДанным = "Объект.бит_Напечатано";
	НовыйЭлемент.Заголовок = "Напечатано";
	НовыйЭлемент.ТолькоПросмотр = Ложь;
	// }}ПервыйБИТ ЛистТребований [ЛТ6.9] [Микляев К.А.] [04.06.2026] -
КонецПроцедуры

