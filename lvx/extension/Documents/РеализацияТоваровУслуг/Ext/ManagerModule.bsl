
Процедура бит_СформироватьДвижения(Регистратор, ТаблицаДвиженийСебестоимостьТоваров) Экспорт
	
	// {{ПервыйБИТ ЛистТребований [ЛТ8.3] [ASHolyavko@1cbit.ru] [06.05.2026] +        
	
	Дата = Регистратор.Дата;
	Организация = Регистратор.Организация;
	//Склад = Регистратор.Склад;
	Клиент = Регистратор.Контрагент;
	Соглашение = Регистратор.Соглашение;
	Договор = Регистратор.Договор;
	Подразделение = Регистратор.Подразделение;
	//ПодразделениеЦФО = РегистрыСведений.бит_СоответствияАналитик.ПолучитьПравуюАналитику(Справочники.бит_ВидыСоответствийАналитик.СтруктураПредприятия_ЦФО, Склад.Подразделение, Дата); 
	//ПодразделениеСкидка = РегистрыСведений.бит_СоответствияАналитик.ПолучитьПравуюАналитику(Справочники.бит_ВидыСоответствийАналитик.СтруктураПредприятия_ЦФО, Склад.Подразделение, Дата);

	Менеджер = Регистратор.Менеджер;      
	
	ПредоставленныеСкидки = Справочники.бит_ЗначенияПоУмолчанию.ПолучитьМассивЗначений("ПредоставленныеСкидки", , Истина);
	
	//СкладЦФО = РегистрыСведений.бит_СоответствияАналитик.ПолучитьПравуюАналитику(Справочники.бит_ВидыСоответствийАналитик.СтруктураПредприятия_ЦФО, Склад.Подразделение, Дата);
	//СкладПолучательЦФО = РегистрыСведений.бит_СоответствияАналитик.ПолучитьПравуюАналитику(Справочники.бит_ВидыСоответствийАналитик.СтруктураПредприятия_ЦФО, СкладПолучатель.Подразделение, Дата);
	
	СоответствиеАналитикСчетаУчета = Справочники.бит_ЗначенияПоУмолчанию.ПолучитьМассивЗначений("СоответствиеАналитикСчетаУчета", Неопределено, Истина);
	
	Запрос = Новый Запрос;
	Запрос.Текст =                                                          
		"ВЫБРАТЬ
		|	бит_СоответствияАналитикСрезПоследних.ЛеваяАналитика_1 КАК ПодразделениеСклада,
		|	бит_СоответствияАналитикСрезПоследних.ПраваяАналитика_1 КАК ЦФО
		|ПОМЕСТИТЬ вт_ЦФОПодразделенияСкладов
		|ИЗ
		|	РегистрСведений.бит_СоответствияАналитик.СрезПоследних(&Дата, ВидСоответствия = ЗНАЧЕНИЕ(Справочник.бит_ВидыСоответствийАналитик.СтруктураПредприятия_ЦФО)) КАК бит_СоответствияАналитикСрезПоследних
		|;
		|
		|////////////////////////////////////////////////////////////////////////////////
		|ВЫБРАТЬ
		|	Товары.Ссылка КАК Ссылка,
		|	Товары.Ссылка.Организация КАК Организация,
		|	Товары.АналитикаУчетаНоменклатуры КАК АналитикаУчетаНоменклатуры,
		|	ВидыЗапасов.ИдентификаторСтроки КАК ИдентификаторСтроки,
		|	Товары.КлючСвязи КАК КлючСвязи,
		|	Товары.КоличествоУпаковок КАК Количество,
		|	Товары.СуммаСНДС КАК СуммаСНДС,
		|	Товары.Склад КАК Склад,
		|	вт_ЦФОПодразделенияСкладов.ЦФО КАК ЦФО
		|ПОМЕСТИТЬ вт_Товары
		|ИЗ
		|	Документ.РеализацияТоваровУслуг.Товары КАК Товары
		|		ЛЕВОЕ СОЕДИНЕНИЕ Документ.РеализацияТоваровУслуг.ВидыЗапасов КАК ВидыЗапасов
		|		ПО Товары.Ссылка = ВидыЗапасов.Ссылка
		|			И Товары.АналитикаУчетаНоменклатуры = ВидыЗапасов.АналитикаУчетаНоменклатуры
		|		ЛЕВОЕ СОЕДИНЕНИЕ вт_ЦФОПодразделенияСкладов КАК вт_ЦФОПодразделенияСкладов
		|		ПО Товары.Склад.Подразделение = вт_ЦФОПодразделенияСкладов.ПодразделениеСклада
		|ГДЕ
		|	Товары.Ссылка = &Регистратор
		|	И ВидыЗапасов.Ссылка = &Регистратор
		|;
		|
		|////////////////////////////////////////////////////////////////////////////////
		|ВЫБРАТЬ
		|	СкидкиНаценки.Ссылка КАК Ссылка,
		|	СкидкиНаценки.КлючСвязи КАК КлючСвязи,
		|	СкидкиНаценки.СкидкаНаценка КАК СкидкаНаценка,
		|	СкидкиНаценки.СкидкаНаценка.бит_СкидкаЗаСчет КАК СкидкаЗаСчет,
		|	СкидкиНаценки.Сумма КАК СуммаСкидки
		|ПОМЕСТИТЬ вт_Скидки
		|ИЗ
		|	Документ.РеализацияТоваровУслуг.СкидкиНаценки КАК СкидкиНаценки
		|ГДЕ
		|	СкидкиНаценки.Ссылка = &Регистратор
		|;
		|
		|////////////////////////////////////////////////////////////////////////////////
		|ВЫБРАТЬ
		|	вт_Товары.Ссылка КАК Ссылка,
		|	вт_Товары.Организация КАК Организация,
		|	вт_Товары.Ссылка.Подразделение КАК Подразделение,
		|	вт_Товары.Склад КАК Склад,
		|	вт_Товары.КлючСвязи КАК КлючСвязи,
		|	вт_Товары.АналитикаУчетаНоменклатуры КАК АналитикаУчетаНоменклатуры,
		|	вт_Товары.ИдентификаторСтроки КАК ИдентификаторСтроки,
		|	вт_Скидки.СкидкаНаценка КАК СкидкаНаценка,
		|	вт_Скидки.СкидкаЗаСчет КАК СкидкаЗаСчет,
		|	вт_Товары.Количество КАК Количество,
		|	вт_Скидки.СуммаСкидки КАК СуммаСкидки,
		|	вт_Скидки.СуммаСкидки / вт_Товары.Количество КАК СкидкаНаЕдиницу,
		|	вт_Товары.СуммаСНДС КАК СуммаСНДС,
		|	вт_Товары.СуммаСНДС / вт_Товары.Количество КАК Цена,
		|	вт_Товары.ЦФО КАК ЦФО
		|ПОМЕСТИТЬ вт_СкидкиВДокументе
		|ИЗ
		|	вт_Товары КАК вт_Товары
		|		ЛЕВОЕ СОЕДИНЕНИЕ вт_Скидки КАК вт_Скидки
		|		ПО вт_Товары.Ссылка = вт_Скидки.Ссылка
		|			И вт_Товары.КлючСвязи = вт_Скидки.КлючСвязи
		|;
		|
		|////////////////////////////////////////////////////////////////////////////////
		|ВЫБРАТЬ
		|	вт_СкидкиВДокументе.Ссылка КАК Ссылка,
		|	вт_СкидкиВДокументе.Организация КАК Организация,
		|	Склады.Ссылка КАК Склад,
		|	Склады.Подразделение КАК ПодразделениеСклада,
		|	вт_СкидкиВДокументе.СкидкаЗаСчет КАК СкидкаЗаСчет,
		|	ДействиеСкидокНаценокСрезПоследних.СкидкаНаценка КАК СкидкаНаценка,
		|	ЕСТЬNULL(ДействиеСкидокНаценокСрезПоследних.Статус, ЗНАЧЕНИЕ(Перечисление.СтатусыДействияСкидок.НеДействует)) КАК Статус,
		|	ЕСТЬNULL(ДействиеСкидокНаценокСрезПоследних.Статус, ЗНАЧЕНИЕ(Перечисление.СтатусыДействияСкидок.НеДействует)) = ЗНАЧЕНИЕ(Перечисление.СтатусыДействияСкидок.Действует)
		|		И вт_СкидкиВДокументе.СкидкаЗаСчет <> вт_СкидкиВДокументе.ЦФО
		|		И вт_СкидкиВДокументе.СкидкаЗаСчет <> ЗНАЧЕНИЕ(Справочник.Подразделения.ПустаяСсылка)
		|		И (&Дата МЕЖДУ ЕСТЬNULL(ДействиеСкидокНаценокСрезПоследних.Период, ДАТАВРЕМЯ(1, 1, 1)) И ВЫБОР
		|				КОГДА ДействиеСкидокНаценокСрезПоследних.Период ЕСТЬ NULL
		|					ТОГДА ЕСТЬNULL(ДОБАВИТЬКДАТЕ(ДействиеСкидокНаценокСрезПервых.Период, ДЕНЬ, -1), ДАТАВРЕМЯ(1, 1, 1))
		|				ИНАЧЕ ЕСТЬNULL(ДОБАВИТЬКДАТЕ(Таблица.Период, ДЕНЬ, -1), ДАТАВРЕМЯ(1, 1, 1))
		|			КОНЕЦ
		|			ИЛИ &Дата > ЕСТЬNULL(ДействиеСкидокНаценокСрезПоследних.Период, ДАТАВРЕМЯ(1, 1, 1))
		|				И ВЫБОР
		|					КОГДА ДействиеСкидокНаценокСрезПоследних.Период ЕСТЬ NULL
		|						ТОГДА ЕСТЬNULL(ДОБАВИТЬКДАТЕ(ДействиеСкидокНаценокСрезПервых.Период, ДЕНЬ, -1), ДАТАВРЕМЯ(1, 1, 1))
		|					ИНАЧЕ ЕСТЬNULL(ДОБАВИТЬКДАТЕ(Таблица.Период, ДЕНЬ, -1), ДАТАВРЕМЯ(1, 1, 1))
		|				КОНЕЦ = ДАТАВРЕМЯ(1, 1, 1)) КАК Использование,
		|	ЕСТЬNULL(ДействиеСкидокНаценокСрезПоследних.Период, ДАТАВРЕМЯ(1, 1, 1)) КАК ДатаНачала,
		|	ВЫБОР
		|		КОГДА ДействиеСкидокНаценокСрезПоследних.Период ЕСТЬ NULL
		|			ТОГДА ЕСТЬNULL(ДОБАВИТЬКДАТЕ(ДействиеСкидокНаценокСрезПервых.Период, ДЕНЬ, -1), ДАТАВРЕМЯ(1, 1, 1))
		|		ИНАЧЕ ЕСТЬNULL(ДОБАВИТЬКДАТЕ(Таблица.Период, ДЕНЬ, -1), ДАТАВРЕМЯ(1, 1, 1))
		|	КОНЕЦ КАК ДатаОкончания,
		|	вт_СкидкиВДокументе.КлючСвязи КАК КлючСвязи,
		|	вт_СкидкиВДокументе.АналитикаУчетаНоменклатуры КАК АналитикаУчетаНоменклатуры,
		|	вт_СкидкиВДокументе.ИдентификаторСтроки КАК ИдентификаторСтроки,
		|	вт_СкидкиВДокументе.СуммаСкидки КАК СуммаСкидки,
		|	вт_СкидкиВДокументе.Количество КАК Количество,
		|	вт_СкидкиВДокументе.СкидкаНаЕдиницу КАК СкидкаНаЕдиницу,
		|	вт_СкидкиВДокументе.Цена КАК Цена,
		|	вт_СкидкиВДокументе.ЦФО КАК ЦФО
		|ИЗ
		|	Справочник.Склады КАК Склады
		|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ДействиеСкидокНаценок.СрезПоследних(
		|				&Дата,
		|				СкидкаНаценка В
		|						(ВЫБРАТЬ
		|							вт_Скидки.СкидкаНаценка КАК СкидкаНаценка
		|						ИЗ
		|							вт_Скидки КАК вт_Скидки)
		|					И Источник ССЫЛКА Справочник.Склады) КАК ДействиеСкидокНаценокСрезПоследних
		|			ЛЕВОЕ СОЕДИНЕНИЕ вт_СкидкиВДокументе КАК вт_СкидкиВДокументе
		|			ПО ДействиеСкидокНаценокСрезПоследних.СкидкаНаценка = вт_СкидкиВДокументе.СкидкаНаценка
		|		ПО (ДействиеСкидокНаценокСрезПоследних.Источник = Склады.Ссылка)
		|		ЛЕВОЕ СОЕДИНЕНИЕ (ВЫБРАТЬ
		|			МИНИМУМ(Т.Период) КАК Период,
		|			Т.Источник КАК Источник,
		|			Срез.СкидкаНаценка КАК СкидкаНаценка
		|		ИЗ
		|			РегистрСведений.ДействиеСкидокНаценок.СрезПоследних(
		|					&Дата,
		|					СкидкаНаценка В
		|							(ВЫБРАТЬ
		|								вт_Скидки.СкидкаНаценка КАК СкидкаНаценка
		|							ИЗ
		|								вт_Скидки КАК вт_Скидки)
		|						И Источник ССЫЛКА Справочник.Склады) КАК Срез
		|				ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ДействиеСкидокНаценок КАК Т
		|				ПО Срез.Источник = Т.Источник
		|					И (Т.СкидкаНаценка В
		|						(ВЫБРАТЬ
		|							вт_Скидки.СкидкаНаценка КАК СкидкаНаценка
		|						ИЗ
		|							вт_Скидки КАК вт_Скидки))
		|					И (Т.Период > Срез.Период)
		|					И (Т.Статус <> Срез.Статус)
		|		
		|		СГРУППИРОВАТЬ ПО
		|			Т.Источник,
		|			Срез.СкидкаНаценка) КАК Таблица
		|		ПО (Таблица.Источник = Склады.Ссылка)
		|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ДействиеСкидокНаценок.СрезПервых(
		|				&Дата,
		|				СкидкаНаценка В
		|						(ВЫБРАТЬ
		|							вт_Скидки.СкидкаНаценка КАК СкидкаНаценка
		|						ИЗ
		|							вт_Скидки КАК вт_Скидки)
		|					И Источник ССЫЛКА Справочник.Склады) КАК ДействиеСкидокНаценокСрезПервых
		|		ПО (ДействиеСкидокНаценокСрезПервых.Источник = Склады.Ссылка)
		|ГДЕ
		|	НЕ Склады.ЭтоГруппа
		|	И Склады.Ссылка В
		|			(ВЫБРАТЬ
		|				вт_Товары.Склад КАК Склад
		|			ИЗ
		|				вт_Товары КАК вт_Товары)
		|	И вт_СкидкиВДокументе.СкидкаНаценка <> ЗНАЧЕНИЕ(справочник.СкидкиНаценки.пустаяссылка)
		|
		|СГРУППИРОВАТЬ ПО
		|	вт_СкидкиВДокументе.Ссылка,
		|	вт_СкидкиВДокументе.Организация,
		|	Склады.Ссылка,
		|	вт_СкидкиВДокументе.СкидкаЗаСчет,
		|	ДействиеСкидокНаценокСрезПоследних.СкидкаНаценка,
		|	ЕСТЬNULL(ДействиеСкидокНаценокСрезПоследних.Статус, ЗНАЧЕНИЕ(Перечисление.СтатусыДействияСкидок.НеДействует)),
		|	ЕСТЬNULL(ДействиеСкидокНаценокСрезПоследних.Статус, ЗНАЧЕНИЕ(Перечисление.СтатусыДействияСкидок.НеДействует)) = ЗНАЧЕНИЕ(Перечисление.СтатусыДействияСкидок.Действует)
		|		И вт_СкидкиВДокументе.СкидкаЗаСчет <> вт_СкидкиВДокументе.ЦФО
		|		И вт_СкидкиВДокументе.СкидкаЗаСчет <> ЗНАЧЕНИЕ(Справочник.Подразделения.ПустаяСсылка)
		|		И (&Дата МЕЖДУ ЕСТЬNULL(ДействиеСкидокНаценокСрезПоследних.Период, ДАТАВРЕМЯ(1, 1, 1)) И ВЫБОР
		|				КОГДА ДействиеСкидокНаценокСрезПоследних.Период ЕСТЬ NULL
		|					ТОГДА ЕСТЬNULL(ДОБАВИТЬКДАТЕ(ДействиеСкидокНаценокСрезПервых.Период, ДЕНЬ, -1), ДАТАВРЕМЯ(1, 1, 1))
		|				ИНАЧЕ ЕСТЬNULL(ДОБАВИТЬКДАТЕ(Таблица.Период, ДЕНЬ, -1), ДАТАВРЕМЯ(1, 1, 1))
		|			КОНЕЦ
		|			ИЛИ &Дата > ЕСТЬNULL(ДействиеСкидокНаценокСрезПоследних.Период, ДАТАВРЕМЯ(1, 1, 1))
		|				И ВЫБОР
		|					КОГДА ДействиеСкидокНаценокСрезПоследних.Период ЕСТЬ NULL
		|						ТОГДА ЕСТЬNULL(ДОБАВИТЬКДАТЕ(ДействиеСкидокНаценокСрезПервых.Период, ДЕНЬ, -1), ДАТАВРЕМЯ(1, 1, 1))
		|					ИНАЧЕ ЕСТЬNULL(ДОБАВИТЬКДАТЕ(Таблица.Период, ДЕНЬ, -1), ДАТАВРЕМЯ(1, 1, 1))
		|				КОНЕЦ = ДАТАВРЕМЯ(1, 1, 1)),
		|	ЕСТЬNULL(ДействиеСкидокНаценокСрезПоследних.Период, ДАТАВРЕМЯ(1, 1, 1)),
		|	ВЫБОР
		|		КОГДА ДействиеСкидокНаценокСрезПоследних.Период ЕСТЬ NULL
		|			ТОГДА ЕСТЬNULL(ДОБАВИТЬКДАТЕ(ДействиеСкидокНаценокСрезПервых.Период, ДЕНЬ, -1), ДАТАВРЕМЯ(1, 1, 1))
		|		ИНАЧЕ ЕСТЬNULL(ДОБАВИТЬКДАТЕ(Таблица.Период, ДЕНЬ, -1), ДАТАВРЕМЯ(1, 1, 1))
		|	КОНЕЦ,
		|	вт_СкидкиВДокументе.АналитикаУчетаНоменклатуры,
		|	вт_СкидкиВДокументе.ИдентификаторСтроки,
		|	вт_СкидкиВДокументе.СуммаСкидки,
		|	Склады.Подразделение,
		|	вт_СкидкиВДокументе.КлючСвязи,
		|	вт_СкидкиВДокументе.Количество,
		|	вт_СкидкиВДокументе.СкидкаНаЕдиницу,
		|	вт_СкидкиВДокументе.Цена,
		|	вт_СкидкиВДокументе.ЦФО
		|
		|ОБЪЕДИНИТЬ ВСЕ
		|
		|ВЫБРАТЬ
		|	вт_СкидкиВДокументе.Ссылка,
		|	вт_СкидкиВДокументе.Организация,
		|	вт_СкидкиВДокументе.Склад,
		|	NULL,
		|	вт_СкидкиВДокументе.СкидкаЗаСчет,
		|	NULL,
		|	NULL,
		|	ЛОЖЬ,
		|	NULL,
		|	NULL,
		|	вт_СкидкиВДокументе.КлючСвязи,
		|	вт_СкидкиВДокументе.АналитикаУчетаНоменклатуры,
		|	вт_СкидкиВДокументе.ИдентификаторСтроки,
		|	вт_СкидкиВДокументе.СуммаСкидки,
		|	вт_СкидкиВДокументе.Количество,
		|	вт_СкидкиВДокументе.СкидкаНаЕдиницу,
		|	вт_СкидкиВДокументе.Цена,
		|	вт_СкидкиВДокументе.ЦФО
		|ИЗ
		|	вт_СкидкиВДокументе КАК вт_СкидкиВДокументе
		|ГДЕ
		|	вт_СкидкиВДокументе.СкидкаНаценка ЕСТЬ NULL
		|
		|СГРУППИРОВАТЬ ПО
		|	вт_СкидкиВДокументе.Ссылка,
		|	вт_СкидкиВДокументе.Организация,
		|	вт_СкидкиВДокументе.СкидкаЗаСчет,
		|	вт_СкидкиВДокументе.АналитикаУчетаНоменклатуры,
		|	вт_СкидкиВДокументе.ИдентификаторСтроки,
		|	вт_СкидкиВДокументе.СуммаСкидки,
		|	вт_СкидкиВДокументе.КлючСвязи,
		|	вт_СкидкиВДокументе.Количество,
		|	вт_СкидкиВДокументе.СкидкаНаЕдиницу,
		|	вт_СкидкиВДокументе.Цена,
		|	вт_СкидкиВДокументе.Склад,
		|	вт_СкидкиВДокументе.ЦФО";
	
	Запрос.УстановитьПараметр("Регистратор", Регистратор);
	Запрос.УстановитьПараметр("Дата", Дата);
	//Запрос.УстановитьПараметр("Склад", Склад);
	//Запрос.УстановитьПараметр("ПодразделениеЦФО", ПодразделениеЦФО);
	
	ТаблицаСкидок = Запрос.Выполнить().Выгрузить();		
	
		
	ТаблицаДвижений_СебестоимостьТоваров = ТаблицаДвиженийСебестоимостьТоваров;
	
	ТаблицаДвижений_СтоимостьТМЦ_УУ = РегистрыНакопления.бит_СтоимостьТМЦ_УУ.ПолучитьТаблицуДляДвижений();
	ТаблицаДвижений_СтоимостьТМЦ_УУ_Расход = РегистрыНакопления.бит_СтоимостьТМЦ_УУ.ПолучитьТаблицуДляДвижений();
	
	ТаблицаДвижений_ВыручкаИСебестоимость_УУ = РегистрыНакопления.бит_ВыручкаИСебестоимость_УУ.ПолучитьТаблицуДляДвижений();
		
	//1. Регистр накопления "Стоимость ТМЦ УУ".
	//Вид движения				Расход
	//Номенклатура				Реквизит Номенклатура из ТЧ документа
	//ГФУ						Реквизит Группа финансового учета в карточке Номенклатура
	//Организация				Реквизит Организация документа
	//Партия					В соответствии с типовым механизмом ЕРП по определению партии номенклатуры
	//Склад						Реквизит Склад 
	//Подразделение				Получаем Подразделение из соответствия Склад-Подразделение в карточке склад. 
	//							Далее ищем  соответствие Подразделение -ЦФО с помощью Документ.бит_НазначениеСоответствияАналитик, 
	//							с видом соответствия Структура предприятия --> ЦФО. И полученным ЦФО заполняем реквизитПодразделение..
	//Количество				Реквизит Количество из ТЧ документа
	//Стоимость					Определяем цену на данную номенклатуру, исходя из партии номенклатуры и цены номенклатуры в этой партии (партию определяем по ФИФО) и умножаем на количество.
	//Доп.расходы				Оставляем не заполненным 
		
	РегистрыНакопления.бит_СтоимостьТМЦ_УУ.ЗаполнитьТаблицуДвиженийПоВидуДвижения(ТаблицаДвижений_СтоимостьТМЦ_УУ_Расход, ТаблицаДвижений_СебестоимостьТоваров, ВидДвиженияНакопления.Расход,,, Дата);		
	
	ТаблицаСтоимостиНоменклатурыПоПартиям = РегистрыНакопления.бит_СтоимостьТМЦ_УУ.ПолучитьСтоимостьНоменклатурыПоПартиям(Дата, ТаблицаДвижений_СтоимостьТМЦ_УУ_Расход); 
	
	РегистрыНакопления.бит_СтоимостьТМЦ_УУ.ЗаполнитьСтоимостьВТаблицеДвижений(ТаблицаДвижений_СтоимостьТМЦ_УУ_Расход, ТаблицаСтоимостиНоменклатурыПоПартиям);
	
	ОбщегоНазначенияУТКлиентСервер.ДополнитьТаблицу(ТаблицаДвижений_СтоимостьТМЦ_УУ_Расход, ТаблицаДвижений_СтоимостьТМЦ_УУ);
		
	//2. Регистр накопления "Выручка и себестоимость  УУ"
	//2.1.Если скидок в документе нет.	
	//Номенклатура				Реквизит Номенклатура из ТЧ документа
	//Заказ клиента				Если в  реквизите Назначение ТЧ документа указан Заказ Клиента, то заполняем этим значением. 
	//							Иначе оставляем пустым
	//Подразделение				Получаем Подразделение из соответствия Склад-Подразделение в карточке склад. Далее ищем  соответствие Подразделение -ЦФО с помощью Документ.бит_НазначениеСоответствияАналитик, с видом соответствия Структура предприятия --> ЦФО. И полученным ЦФО заполняем реквизит Подразделение.
	//Покупатель				Заполняем значением из реквизита Клиент 
	//Партия					В соответствии с типовым механизмом ЕРП по определению партии номенклатуры
	//Тип реализации			Значение по умолчанию Внешняя реализация
	//Менеджер					Реквизит Менеджер
	//Склад	           	 		Склад 	
	//Соглашение				Заполняем значением из реквизита Соглашение 
	//Договор					Заполняем значением из реквизита Договор
	//ХозяйственнаяОперация		Значение по умолчанию Реализация
	//Количество				Реквизит Количество из ТЧ документа
	//Сумма выручки				Заполняем значением из реквизита Сумма с НДС, установленного в ТЧ Товары.
	//Стоимость					Определяем цену на данную номенклатуру, исходя из партии номенклатуры и цены номенклатуры в этой партии 
	//							(партию определяем по ФИФО) и умножаем на количество.
	//Сумма скидки				Оставляем не заполненным
	//Вид скидки				Оставляем не заполненным
	//Доп.расходы				Оставляем не заполненным
	
	//ПодразделениеЦФОПолучатель = РегистрыСведений.бит_СоответствияАналитик.ПолучитьПравуюАналитику(Справочники.бит_ВидыСоответствийАналитик.СтруктураПредприятия_ЦФО, Склад.Подразделение, Дата);
	
	СоответствиеАналитикЦФОКонтрагент = Справочники.бит_ЗначенияПоУмолчанию.ПолучитьМассивЗначений("СоответствиеАналитикЦФОКонтрагент", , Истина);
			
	//ЦФОКонтрагент = РегистрыСведений.бит_СоответствияАналитик.ПолучитьПравуюАналитику(СоответствиеАналитикЦФОКонтрагент, ПодразделениеЦФОПолучатель, Дата);	
		
	РегистрыНакопления.бит_ВыручкаИСебестоимость_УУ.ЗаполнитьТаблицуДвижений(ТаблицаДвижений_ВыручкаИСебестоимость_УУ, ТаблицаДвижений_СебестоимостьТоваров, , , Клиент, Менеджер, Соглашение, Договор, Дата);		
	
	ТаблицаСтоимостиНоменклатурыПоПартиям = РегистрыНакопления.бит_СтоимостьТМЦ_УУ.ПолучитьСтоимостьНоменклатурыПоПартиям(Дата, ТаблицаДвижений_ВыручкаИСебестоимость_УУ); 
	
	ТаблицаСтоимостиНоменклатуры = Новый ТаблицаЗначений;
	ТаблицаСтоимостиНоменклатуры.Колонки.Добавить("Номенклатура", Новый ОписаниеТипов("СправочникСсылка.КлючиАналитикиУчетаНоменклатуры"));
	ТаблицаСтоимостиНоменклатуры.Колонки.Добавить("Организация", Новый ОписаниеТипов("СправочникСсылка.Организации"));
	ТаблицаСтоимостиНоменклатуры.Колонки.Добавить("Партия", Новый ОписаниеТипов(Метаданные.ОпределяемыеТипы.ДокументПартии.Тип));
	ТаблицаСтоимостиНоменклатуры.Колонки.Добавить("Подразделение", Новый ОписаниеТипов("СправочникСсылка.Подразделения"));
	ТаблицаСтоимостиНоменклатуры.Колонки.Добавить("Склад", Новый ОписаниеТипов("СправочникСсылка.Склады"));
	ТаблицаСтоимостиНоменклатуры.Колонки.Добавить("Цена", Новый ОписаниеТипов("Число", Новый КвалификаторыЧисла(15,2)));
		
	РегистрыНакопления.бит_ВыручкаИСебестоимость_УУ.ЗаполнитьСтоимостьВТаблицеДвижений(ТаблицаДвижений_ВыручкаИСебестоимость_УУ, ТаблицаСтоимостиНоменклатурыПоПартиям, ТаблицаСтоимостиНоменклатуры);		
	РегистрыНакопления.бит_ВыручкаИСебестоимость_УУ.ЗаполнитьСкидкиВТаблицеДвижений(ТаблицаДвижений_ВыручкаИСебестоимость_УУ, ТаблицаСкидок);
	
	ТаблицаДвижений_ВыручкаИСебестоимость_УУ_Окончательный = РегистрыНакопления.бит_ВыручкаИСебестоимость_УУ.ПолучитьТаблицуДляДвижений();
	//ТаблицаДвижений_ВыручкаИСебестоимость_УУ_Окончательный.Колонки.Добавить("Использование");
	
	//2.2. Если скидки в документе есть.
	//Необходимо проверить в каждой строке табличной части Товары, на наличие скидки, проверка осуществляется для каждой строки. 
	//При наличии скидки проверка (п. 2.2.2.1-2.2.2.4) осуществляется для каждой строки и формированием движений по регистрам/проводкам.
	//При наличии в табличной части Товары предоставленной скидки (реквизит "Сумма автоматической скидки" >0), необходимо:
	//2.2.2.1 Проверить наличие в справочнике Скидки/наценки, скидки, 
	//	которая установлена на Номенклатуру в скрытой ТЧ документа "Скидка Наценка" 
	//2.2.2.2 Если найдена скидка в п.2.2.2.1, то проверить период ее действия, 
	//	она должна входить в дату документа "Реализация товаров и услуг",
	//2.2.2.3 Если найдена скидка, в п.2.2.2.2 то проверить, что Получателем скидки является склад указанный в реквизите 
	//	Получатель (справочник скидка/наценка, вкладка основное - реквизит Получатель - На складах.
	//2.2.2.4 Если найдена скидка в п, 2.2.2.3. то проверить значение реквизита "Скидка за счет" и если оно совпадает с 
	//	Подразделением(из соответствия Склад-Подразделение в карточке склад) или не заполнено, то ничего не предпринимаем.  
	//	Если же  не совпадает, то необходимо создать еще 2 записи в регистр "Выручка и себестоимость  УУ" 
	//	На каждый вид предоставленной скидки делается отдельная запись.
	
	//////
	//Для подразделения получившего скидку:
	//	
	//Номенклатура				Реквизит Номенклатура из ТЧ документа
	//Заказ клиента				Если в  реквизите Назначение ТЧ документа указан Заказ Клиента, то заполняем этим значением. 
	//							Иначе оставляем пустым
	//Подразделение				Получаем Подразделение из соответствия Склад-Подразделение в карточке склад. 
	//							Далее ищем  соответствие Подразделение -ЦФО с помощью Документ.бит_НазначениеСоответствияАналитик, 
	//							с видом соответствия Структура предприятия --> ЦФО. И полученным ЦФО заполняем реквизит Подразделение.
	//Покупатель				Заполняем значением из реквизита Клиент 
	//Партия					В соответствии с типовым механизмом ЕРП по определению партии номенклатуры
	//Тип реализации			Значение по умолчанию Внешняя реализация
	//Менеджер					Реквизит Менеджер
	//Склад	     				Склад 	
	//Соглашение				Заполняем значением из реквизита Соглашение 
	//Договор					Заполняем значением из реквизита Договор
	//ХозяйственнаяОперация		Значение по умолчанию Реализация
	//Количество				Реквизит Количество из ТЧ документа
	//Сумма выручки				Заполняем значением из реквизита Сумма, установленного в  скрытой ТЧ Скидка наценка
	//Стоимость					Оставляем не заполненным
	//Сумма скидки				Оставляем не заполненным
	//Вид скидки				Необходимо указать Наименование предоставленной скидки 
	//Доп.расходы				Оставляем не заполненным	
	
	//////
	//Для Подразделения предоставившего скидку:
	//		
	//Номенклатура				Реквизит Номенклатура из ТЧ документа
	//Заказ клиента				Если в  реквизите Назначение ТЧ документа указан Заказ Клиента, то заполняем этим значением. 
	//							Иначе оставляем пустым
	//Подразделение				Получаем значение реквизита "Скидка за счет". 
	//							Далее ищем  соответствие Подразделение -ЦФО с помощью Документ.бит_НазначениеСоответствияАналитик, 
	//							с видом соответствия Структура предприятия --> ЦФО. И полученным ЦФО заполняем реквизит Подразделение.
	//Покупатель				Исходя из заполненного значение Подразделение (см. строка выше), 
	//							необходимо найти соответствие ЦФО-Контрагент с помощью Документ.бит_НазначениеСоответствияАналитик, 
	//							с видом соответствия ЦФО-Контрагент. И полученным Контрагентом заполняем реквизит Покупатель
	//Партия					В соответствии с типовым механизмом ЕРП по определению партии номенклатуры
	//Тип реализации			Значение по умолчанию Внутренняя реализация
	//Менеджер					Реквизит Менеджер
	//Склад						Склад 
	//Соглашение				Заполняем значением из реквизита Соглашение 
	//Договор					Заполняем значением из реквизита Договор
	//ХозяйственнаяОперация		Значение по умолчанию Реализация
	//Количество				Реквизит Количество из ТЧ документа
	//Сумма выручки				Оставляем не заполненным
	//Стоимость					Оставляем не заполненным
	//Сумма скидки				Заполняем значением из реквизита Сумма, установленного в скрытой ТЧ Скидка наценка
	//Вид скидки				Необходимо указать Наименование предоставленной скидки 
	//Доп.расходы				Оставляем не заполненным
	
	Для Каждого Строка Из ТаблицаДвижений_ВыручкаИСебестоимость_УУ Цикл
		
		Если Строка.Использование Тогда
			
			НоваяСтрока = ТаблицаДвижений_ВыручкаИСебестоимость_УУ_Окончательный.Добавить();
			ЗаполнитьЗначенияСвойств(НоваяСтрока, Строка);
			
			//
			НоваяСтрока = ТаблицаДвижений_ВыручкаИСебестоимость_УУ_Окончательный.Добавить();
			ЗаполнитьЗначенияСвойств(НоваяСтрока, Строка);
			
			НоваяСтрока.СуммаВыручки = Строка.Количество * Строка.СкидкаНаЕдиницу;
			НоваяСтрока.Стоимость = 0;
			НоваяСтрока.СуммаСкидки = 0;
			НоваяСтрока.ВидСкидки = Строка.СкидкаНаценка;
			
			СкладЦФО = РегистрыСведений.бит_СоответствияАналитик.ПолучитьПравуюАналитику(Справочники.бит_ВидыСоответствийАналитик.СтруктураПредприятия_ЦФО, Строка.Склад.Подразделение, Дата);
	
			НоваяСтрока.Подразделение = СкладЦФО;	
			НоваяСтрока.Соглашение = Неопределено;
			НоваяСтрока.ТипРеализации = Перечисления.бит_ТипыРеализаций.ВнутренняяРеализация;
			
			КонтрагентСкидкаЗаСчет = РегистрыСведений.бит_СоответствияАналитик.ПолучитьПравуюАналитику(СоответствиеАналитикЦФОКонтрагент, Строка.СкидкаЗаСчет, Дата);
			ДоговорКонтрагентСкидкаЗаСчет = Справочники.ДоговорыКонтрагентов.бит_ПолучитьДоговорКнтрагента(Организация, КонтрагентСкидкаЗаСчет, Перечисления.ТипыДоговоров.СПокупателем, Дата);
			НоваяСтрока.Покупатель = КонтрагентСкидкаЗаСчет;
			НоваяСтрока.Договор = ДоговорКонтрагентСкидкаЗаСчет;
				
			//
			НоваяСтрока = ТаблицаДвижений_ВыручкаИСебестоимость_УУ_Окончательный.Добавить();
			ЗаполнитьЗначенияСвойств(НоваяСтрока, Строка);
			
			НоваяСтрока.СуммаВыручки = 0;
			НоваяСтрока.Стоимость = 0;
			НоваяСтрока.СуммаСкидки = Строка.Количество * Строка.СкидкаНаЕдиницу;
			НоваяСтрока.ВидСкидки = Строка.СкидкаНаценка;
			НоваяСтрока.Подразделение = Строка.СкидкаЗаСчет;
			НоваяСтрока.Соглашение = Неопределено;
			НоваяСтрока.ТипРеализации = Перечисления.бит_ТипыРеализаций.ВнутренняяРеализация;
			
			ЦФОКонтрагент = РегистрыСведений.бит_СоответствияАналитик.ПолучитьПравуюАналитику(СоответствиеАналитикЦФОКонтрагент, Строка.Подразделение, Дата);
			ДоговорЦФОКонтрагент = Справочники.ДоговорыКонтрагентов.бит_ПолучитьДоговорКнтрагента(Организация, ЦФОКонтрагент, Перечисления.ТипыДоговоров.СПоставщиком, Дата);
			НоваяСтрока.Покупатель = ЦФОКонтрагент;
			НоваяСтрока.Договор = ДоговорЦФОКонтрагент;
			
		ИначеЕсли НЕ ЗначениеЗаполнено(Строка.СкидкаНаценка) Тогда	
			
			НоваяСтрока = ТаблицаДвижений_ВыручкаИСебестоимость_УУ_Окончательный.Добавить();
			ЗаполнитьЗначенияСвойств(НоваяСтрока, Строка);
			НоваяСтрока.СуммаВыручки = Строка.Количество * Строка.Цена;
			
		КонецЕсли;	
		
	КонецЦикла;
		
	НаборЗаписей = РегистрыНакопления.бит_СтоимостьТМЦ_УУ.СоздатьНаборЗаписей();
	НаборЗаписей.Отбор.Регистратор.Установить(Регистратор);
	НаборЗаписей.Загрузить(ТаблицаДвижений_СтоимостьТМЦ_УУ);
	НаборЗаписей.Записать(Истина);
	
	НаборЗаписей = РегистрыНакопления.бит_ВыручкаИСебестоимость_УУ.СоздатьНаборЗаписей();
	НаборЗаписей.Отбор.Регистратор.Установить(Регистратор);
	НаборЗаписей.Загрузить(ТаблицаДвижений_ВыручкаИСебестоимость_УУ_Окончательный);
	НаборЗаписей.Записать(Истина);
	
	//2.3. Движение по плану счетов бюджетирования
	
	НаборПроводок = РегистрыБухгалтерии.бит_Бюджетирование.СоздатьНаборЗаписей();
	НаборПроводок.Отбор.Регистратор.Установить(Регистратор);
	
	РегистрыНакопления.бит_ВыручкаИСебестоимость_УУ.ДополнитьСчетаУчатНоменклатуры(ТаблицаДвижений_ВыручкаИСебестоимость_УУ_Окончательный);
	
	Для Каждого СтрокаДвижений Из ТаблицаДвижений_ВыручкаИСебестоимость_УУ_Окончательный Цикл
		
		//2.3.1.Проводка №1.
		//
		//Период. 			Заполняется значением из документа, реквизит Дата.
		//Сценарий. 		Заполняется значением ФАКТ
		//Организация. 		Заполняется значением из документа, реквизит Организация.
		//ЦФО.				Изначально берем Подразделение из соответствия Склад-Подразделение в карточке склад, для реквизита Склад. 
		//					Далее ищем  соответствие СтруктураПредприятия_ЦФО с помощью Документ.бит_НазначениеСоответствияАналитик, 
		//					с видом соответствия Структура предприятия --> ЦФО. И заполняем полученным ЦФО 
		//Статья оборотов. 	Заполняем значением Выручка от реализации. 
		//Счет Дебет. 		Заполняется значением счет учета 62
		//Субконто Дт 1. 	Изначально берем Подразделение из соответствия Склад-Подразделение в карточке склад, для реквизита Склад. 
		//					Далее ищем  соответствие СтруктураПредприятия_ЦФО с помощью Документ.бит_НазначениеСоответствияАналитик, 
		//					с видом соответствия Структура предприятия --> ЦФО. И полученным ЦФО заполняем субконто
		//Субконто Дт 2.	Заполняем значением из реквизита Клиент
		//Субконто Дт 3. 	Заполняем значением из реквизита Договор, 
		//Счет Кредит. 		Заполняется значением счет учета 90.01
		//Субконто Кт 1. 	Изначально берем Подразделение из соответствия Склад-Подразделение в карточке склад, для реквизита Склад. 
		//					Далее ищем  соответствие СтруктураПредприятия_ЦФО с помощью Документ.бит_НазначениеСоответствияАналитик, 
		//					с видом соответствия Структура предприятия --> ЦФО. И полученным ЦФО заполняем субконто
		//Субконто Кт 2. 	Заполняем значением Выручка от реализации
		//Сумма Сценария. 	Заполняем значением из Сумма выручки в Регистре накопления "Выручка и себестоимость УУ", 
		//					сформированному к этому документу.
		//Сумма Упр. 		Заполняется аналогично Сумма сценария
		//Сумма Регл 		Заполняется аналогично Сумма сценария
		//Содержание. 		Заполняем значением Выручка от реализации
		
		Если ЗначениеЗаполнено(СтрокаДвижений.СуммаВыручки)
			И НЕ ЗначениеЗаполнено(СтрокаДвижений.ВидСкидки) Тогда
			Проводка = НаборПроводок.Добавить();
			Проводка.Регистратор = Регистратор;
			
			Проводка.Период = Дата;
			Проводка.Сценарий = Справочники.СценарииПланирования.ФАКТ;
			Проводка.Организация = Организация;
			
			СкладЦФО = РегистрыСведений.бит_СоответствияАналитик.ПолучитьПравуюАналитику(Справочники.бит_ВидыСоответствийАналитик.СтруктураПредприятия_ЦФО, СтрокаДвижений.Склад.Подразделение, Дата);
			Проводка.ЦФО = СкладЦФО;
			
			//Контрагент = РегистрыСведений.бит_СоответствияАналитик.ПолучитьПравуюАналитику(СоответствиеАналитикЦФОКонтрагент, СкладЦФО, Дата);
			
			//Договор = Справочники.ДоговорыКонтрагентов.бит_ПолучитьДоговорКнтрагента(Организация, Контрагент, Перечисления.ТипыДоговоров.СПокупателем, Дата);
			
			Проводка.СтатьяОборотов = Справочники.бит_ЗначенияПоУмолчанию.ПолучитьМассивЗначений("ВыручкаОтРеализации", , Истина);
			
			Проводка.СчетДт = РегистрыСведений.бит_СоответствияАналитик.ПолучитьПравуюАналитику(СоответствиеАналитикСчетаУчета, ПланыСчетов.Хозрасчетный.РасчетыСПокупателямиИЗаказчиками, Дата);
			бит_БухгалтерскийУчет.УстановитьСубконто(Проводка.СчетДт, Проводка.СубконтоДт, 1, СкладЦФО);
			бит_БухгалтерскийУчет.УстановитьСубконто(Проводка.СчетДт, Проводка.СубконтоДт, 2, Клиент);
			бит_БухгалтерскийУчет.УстановитьСубконто(Проводка.СчетДт, Проводка.СубконтоДт, 3, Договор);
			
			Проводка.СчетКт = РегистрыСведений.бит_СоответствияАналитик.ПолучитьПравуюАналитику(СоответствиеАналитикСчетаУчета, ПланыСчетов.Хозрасчетный.Выручка, Дата);				
			бит_БухгалтерскийУчет.УстановитьСубконто(Проводка.СчетКт, Проводка.СубконтоКт, 1, СкладЦФО);
			бит_БухгалтерскийУчет.УстановитьСубконто(Проводка.СчетКт, Проводка.СубконтоКт, 2, Справочники.бит_ЗначенияПоУмолчанию.ПолучитьМассивЗначений("ВыручкаОтРеализации", , Истина));
			
			Проводка.СуммаСценарий = СтрокаДвижений.СуммаВыручки;
			Проводка.СуммаУпр = Проводка.СуммаСценарий;
			Проводка.СуммаРегл = Проводка.СуммаСценарий;
			
			Проводка.Содержание = "Выручка от реализации";
		КонецЕсли;
			
		//2.3.2.Проводка №2.
		//			
		//Период. 			Заполняется значением из документа, реквизит Дата.
		//Сценарий. 		Заполняется значением ФАКТ
		//Организация. 		Заполняется значением из документа, реквизит Организация.
		//ЦФО. 				Изначально берем Подразделение из соответствия Склад-Подразделение в карточке склад, для реквизита Склад. 
		//					Далее ищем  соответствие СтруктураПредприятия_ЦФО с помощью Документ.бит_НазначениеСоответствияАналитик, 
		//					с видом соответствия Структура предприятия --> ЦФО. И заполняем полученным ЦФО
		//Статья оборотов. 	Заполняем значением Себестоимость продаж
		//Счет Дебет. 		Заполняется значением счет учета 90.02
		//Субконто Дт 1. 	Изначально берем Подразделение из соответствия Склад-Подразделение в карточке склад, для реквизита Склад 
		//					Далее ищем  соответствие СтруктураПредприятия_ЦФО с помощью Документ.бит_НазначениеСоответствияАналитик, 
		//					с видом соответствия Структура предприятия --> ЦФО. И полученным ЦФО заполняем субконто
		//Субконто Дт 2. 	Заполняем значением Себестоимость продаж
		//Счет Кредит. 		Заполняется значением Счет учета в Группа финансового учета из реквизита Номенклатура (ТЧ документа). 
		//					Если счет учета не заполнен, то выводить сообщение ГФУ Номенклатуры не заполнено
		//Количество. 		Заполняется значением из Реквизит Количество из ТЧ документа
		//Субконто Кт 1. 	Изначально берем Подразделение из соответствия Склад-Подразделение в карточке склад, для Склада отправителя. 
		//					Далее ищем  соответствие СтруктураПредприятия_ЦФО с помощью Документ.бит_НазначениеСоответствияАналитик, 
		//					с видом соответствия Структура предприятия --> ЦФО. И полученным ЦФО заполняем субконто
		//Субконто Кт 2. 	Заполняем значением реквизита Номенклатура из ТЧ документа. 
		//Субконто Кт 3. 	Заполняем значением реквизита Склад Отправитель 
		//Сумма Сценария. 	Заполняем значением из Стоимость в Регистре накопления "Выручка и себестоимость УУ", 
		//					сформированному к этому документу.
		//Сумма Упр. 		Заполняется аналогично Сумма сценария
		//Сумма Регл 		Заполняется аналогично Сумма сценария
		//Содержание. 		Заполняем значением Себестоимость реализованного товара.
		Если ЗначениеЗаполнено(СтрокаДвижений.Стоимость)
			И НЕ ЗначениеЗаполнено(СтрокаДвижений.ВидСкидки) Тогда	
			Проводка = НаборПроводок.Добавить();
			Проводка.Регистратор = Регистратор;
			
			Проводка.Период = Дата;
			Проводка.Сценарий = Справочники.СценарииПланирования.ФАКТ;
			Проводка.Организация = Организация;
			
			СкладЦФО = РегистрыСведений.бит_СоответствияАналитик.ПолучитьПравуюАналитику(Справочники.бит_ВидыСоответствийАналитик.СтруктураПредприятия_ЦФО, СтрокаДвижений.Склад.Подразделение, Дата);
			Проводка.ЦФО = СкладЦФО;
			
			Проводка.СтатьяОборотов =  Справочники.бит_ЗначенияПоУмолчанию.ПолучитьМассивЗначений("СебестоимостьПродаж", , Истина);
			
			Проводка.СчетДт = РегистрыСведений.бит_СоответствияАналитик.ПолучитьПравуюАналитику(СоответствиеАналитикСчетаУчета, ПланыСчетов.Хозрасчетный.СебестоимостьПродаж, Дата);
			бит_БухгалтерскийУчет.УстановитьСубконто(Проводка.СчетДт, Проводка.СубконтоДт, 1, СкладЦФО);
			бит_БухгалтерскийУчет.УстановитьСубконто(Проводка.СчетДт, Проводка.СубконтоДт, 2, Справочники.бит_ЗначенияПоУмолчанию.ПолучитьМассивЗначений("СебестоимостьПродаж", , Истина));
			
			Проводка.СчетКт = СтрокаДвижений.СчетУчетаБюджет;
			бит_БухгалтерскийУчет.УстановитьСубконто(Проводка.СчетКт, Проводка.СубконтоКт, 1, СкладЦФО);
			бит_БухгалтерскийУчет.УстановитьСубконто(Проводка.СчетКт, Проводка.СубконтоКт, 2, СтрокаДвижений.Номенклатура.Номенклатура);
			бит_БухгалтерскийУчет.УстановитьСубконто(Проводка.СчетКт, Проводка.СубконтоКт, 3, СтрокаДвижений.Склад);
			
			Проводка.КоличествоКт = СтрокаДвижений.Количество;
			
			Проводка.СуммаСценарий = СтрокаДвижений.Стоимость;
			Проводка.СуммаУпр = Проводка.СуммаСценарий;
			Проводка.СуммаРегл = Проводка.СуммаСценарий;
			
			Проводка.Содержание = "Себестоимость реализованного товар";
			
		КонецЕсли;
									
		//2.3.3.Проводка №3. 
		//если  в п. 2.2.2. найдена Скидка, удовлетворяющая условиям.			
		//Если было предоставлено несколько скидок, то можно их объединить в одну сумму и сделать общую проводку  
		//							
		//Период. 			Заполняется значением из документа, реквизит Дата.
		//Сценарий. 		Заполняется значением ФАКТ
		//Организация. 		Заполняется значением из документа, реквизит Организация.
		//ЦФО. 				Изначально берем Подразделение из соответствия Склад-Подразделение в карточке склад, для реквизита Склад. 
		//					Далее ищем  соответствие СтруктураПредприятия_ЦФО с помощью Документ.бит_НазначениеСоответствияАналитик, 
		//					с видом соответствия Структура предприятия --> ЦФО. И заполняем полученным ЦФО 
		//Статья оборотов. 	Заполняем значением Предоставленные скидки. 
		//Счет Дебет. 		Заполняется значением счет учета 62
		//Субконто Дт 1. 	Получаем значение реквизита "Скидка за счет". Далее ищем соответствие Подразделение-ЦФО 
		//					с помощью Документ.бит_НазначениеСоответствияАналитик, с видом соответствия Структура предприятия --> ЦФО. 
		//					И полученным ЦФО заполняем реквизит Подразделение.
		//Субконто Дт 2. 	Исходя из заполненного значение Подразделение (см. субконто Дт1), необходимо найти соответствие ЦФО-Контрагент 
		//					с помощью Документ.бит_НазначениеСоответствияАналитик, с видом соответствия ЦфоКонтрагент. 
		//					И полученным Контрагентом заполняем субконто.
		//Субконто Дт 3. 	Заполняем значением Договор с контрагентом, имеющим Цель Договора Реализация. 
		//Счет Кредит. 		Заполняется значением счет учета 90.01
		//Субконто Кт 1. 	Изначально берем Подразделение из соответствия Склад-Подразделение в карточке склад, для реквизита Склад. 
		//					Далее ищем  соответствие СтруктураПредприятия_ЦФО с помощью Документ.бит_НазначениеСоответствияАналитик, 
		//					с видом соответствия Структура предприятия --> ЦФО. И полученным ЦФО заполняем субконто
		//Субконто Кт 2. 	Заполняем значением Предоставленные скидки
		//Сумма Сценария. 	Заполняем значением Сумма скидки в регистре "Выручка и себестоимость  УУ" к документу. 
		//Сумма Упр. 		Заполняется аналогично Сумма сценария
		//Сумма Регл 		Заполняется аналогично Сумма сценария
		//Содержание. 		Заполняем значением Выручка от реализации
		
		Если ЗначениеЗаполнено(СтрокаДвижений.ВидСкидки)
			И ЗначениеЗаполнено(СтрокаДвижений.СуммаВыручки) Тогда 		
			Проводка = НаборПроводок.Добавить();
			Проводка.Регистратор = Регистратор;
			
			Проводка.Период = Дата;
			Проводка.Сценарий = Справочники.СценарииПланирования.ФАКТ;
			Проводка.Организация = Организация;
			
			СкладЦФО = РегистрыСведений.бит_СоответствияАналитик.ПолучитьПравуюАналитику(Справочники.бит_ВидыСоответствийАналитик.СтруктураПредприятия_ЦФО, СтрокаДвижений.Склад.Подразделение, Дата);
			Проводка.ЦФО = СкладЦФО;
			
			Проводка.СтатьяОборотов = ПредоставленныеСкидки;
			             
			//СкидкаЗаСчетЦФО = РегистрыСведений.бит_СоответствияАналитик.ПолучитьПравуюАналитику(Справочники.бит_ВидыСоответствийАналитик.СтруктураПредприятия_ЦФО, СтрокаДвижений.ВидСкидки.бит_СкидкаЗаСчет, Дата);
			
			КонтрагентСкидкаЗаСчет = РегистрыСведений.бит_СоответствияАналитик.ПолучитьПравуюАналитику(СоответствиеАналитикЦФОКонтрагент, СтрокаДвижений.ВидСкидки.бит_СкидкаЗаСчет, Дата);		
			ДоговорКонтрагентСкидкаЗаСчет = Справочники.ДоговорыКонтрагентов.бит_ПолучитьДоговорКнтрагента(Организация, КонтрагентСкидкаЗаСчет, Перечисления.ТипыДоговоров.СПокупателем, Дата);
			
			Проводка.СчетДт = РегистрыСведений.бит_СоответствияАналитик.ПолучитьПравуюАналитику(СоответствиеАналитикСчетаУчета, ПланыСчетов.Хозрасчетный.РасчетыСПокупателямиИЗаказчиками, Дата);
			бит_БухгалтерскийУчет.УстановитьСубконто(Проводка.СчетДт, Проводка.СубконтоДт, 1, СкладЦФО);
			бит_БухгалтерскийУчет.УстановитьСубконто(Проводка.СчетДт, Проводка.СубконтоДт, 2, КонтрагентСкидкаЗаСчет);
			бит_БухгалтерскийУчет.УстановитьСубконто(Проводка.СчетДт, Проводка.СубконтоДт, 3, ДоговорКонтрагентСкидкаЗаСчет);
			
			Проводка.СчетКт = РегистрыСведений.бит_СоответствияАналитик.ПолучитьПравуюАналитику(СоответствиеАналитикСчетаУчета, ПланыСчетов.Хозрасчетный.Выручка, Дата);				
			бит_БухгалтерскийУчет.УстановитьСубконто(Проводка.СчетКт, Проводка.СубконтоКт, 1, СкладЦФО);
			бит_БухгалтерскийУчет.УстановитьСубконто(Проводка.СчетКт, Проводка.СубконтоКт, 2, ПредоставленныеСкидки);
			
			Проводка.СуммаСценарий = СтрокаДвижений.СуммаВыручки;
			Проводка.СуммаУпр = Проводка.СуммаСценарий;
			Проводка.СуммаРегл = Проводка.СуммаСценарий;
			
			Проводка.Содержание = "Выручка от реализации";
			
		КонецЕсли;
					
		//2.3.4.Проводка №4. 
		//если в п. 2.2.2. найдена Скидка, удовлетворяющая условиям.									
		//Аналогично п .2.3.3. Если было предоставлено несколько скидок, то можно их объединить в одну сумму и сделать общую проводку . 
		//									
		//Период. 			Заполняется значением из документа, реквизит Дата.
		//Сценарий. 		Заполняется значением ФАКТ
		//Организация. 		Заполняется значением из документа, реквизит Организация.
		//ЦФО. 				Изначально берем Подразделение из соответствия Склад-Подразделение в карточке склад, для реквизита Склад. 
		//					Далее ищем  соответствие СтруктураПредприятия_ЦФО с помощью Документ.бит_НазначениеСоответствияАналитик, 
		//					с видом соответствия Структура предприятия --> ЦФО. И заполняем полученным ЦФО 
		//Статья оборотов. 	Заполняем значением Предоставленные скидки. 
		//Счет Дебет. 		Заполняется значением счет учета 44
		//Субконто Дт 1. 	Получаем значение реквизита "Скидка за счет". Далее ищем соответствие Подразделение-ЦФО 
		//					с помощью Документ.бит_НазначениеСоответствияАналитик, с видом соответствия Структура предприятия --> ЦФО. 
		//					И полученным ЦФО заполняем реквизит Подразделение.
		//Субконто Дт 2. 	Заполняем значением Предоставленные скидки.
		//Счет Кредит. 		Заполняется значением счет учета 60
		//Субконто Кт 1. 	Изначально берем Подразделение из соответствия Склад-Подразделение в карточке склад, для реквизита Склад. 
		//					Далее ищем  соответствие СтруктураПредприятия_ЦФО с помощью Документ.бит_НазначениеСоответствияАналитик, 
		//					с видом соответствия Структура предприятия --> ЦФО. И полученным ЦФО заполняем субконто
		//Субконто Кт 2. 	Заполняем значением Предоставленные скидки
		//Субконто Кт 3.	Исходя из заполненного значение ЦФО(п.4), необходимо найти соответствие ЦФО-Контрагент 
		//					с помощью Документ.бит_НазначениеСоответствияАналитик, с видом соответствия ЦФО-Контрагент. 
		//					И полученным Контрагентом заполняем реквизит Покупатель
		//Сумма Сценария. 	Заполняем значением Сумма скидки в регистре "Выручка и себестоимость  УУ" к документу.
		//Сумма Упр. 		Заполняется аналогично Сумма сценария
		//Сумма Регл 		Заполняется аналогично Сумма сценария
		//Содержание. 		Заполняем значением Выручка от реализации
		
		Если ЗначениеЗаполнено(СтрокаДвижений.ВидСкидки)
			И ЗначениеЗаполнено(СтрокаДвижений.СуммаСкидки) Тогда	
			Проводка = НаборПроводок.Добавить();
			Проводка.Регистратор = Регистратор;
			
			Проводка.Период = Дата;
			Проводка.Сценарий = Справочники.СценарииПланирования.ФАКТ;
			Проводка.Организация = Организация;
			
			СкладЦФО = РегистрыСведений.бит_СоответствияАналитик.ПолучитьПравуюАналитику(Справочники.бит_ВидыСоответствийАналитик.СтруктураПредприятия_ЦФО, СтрокаДвижений.Склад.Подразделение, Дата);
			Проводка.ЦФО = СкладЦФО;
			
			Проводка.СтатьяОборотов = ПредоставленныеСкидки;            
			
			//Контрагент = РегистрыСведений.бит_СоответствияАналитик.ПолучитьПравуюАналитику(СоответствиеАналитикЦФОКонтрагент, СкладЦФО, Дата);
			//
			//Договор = Справочники.ДоговорыКонтрагентов.бит_ПолучитьДоговорКнтрагента(Организация, Контрагент, Перечисления.ТипыДоговоров.СПокупателем, Дата);
			
			//СкидкаЗаСчетЦФО = РегистрыСведений.бит_СоответствияАналитик.ПолучитьПравуюАналитику(Справочники.бит_ВидыСоответствийАналитик.СтруктураПредприятия_ЦФО, СтрокаДвижений.ВидСкидки.бит_СкидкаЗаСчет, Дата);
			
			Проводка.СчетДт = РегистрыСведений.бит_СоответствияАналитик.ПолучитьПравуюАналитику(СоответствиеАналитикСчетаУчета, ПланыСчетов.Хозрасчетный.РасходыНаПродажу, Дата);
			бит_БухгалтерскийУчет.УстановитьСубконто(Проводка.СчетДт, Проводка.СубконтоДт, 1, СтрокаДвижений.ВидСкидки.бит_СкидкаЗаСчет);
			бит_БухгалтерскийУчет.УстановитьСубконто(Проводка.СчетДт, Проводка.СубконтоДт, 2, ПредоставленныеСкидки);
			
			СоответствиеАналитикЦФОКонтрагент = Справочники.бит_ЗначенияПоУмолчанию.ПолучитьМассивЗначений("СоответствиеАналитикЦФОКонтрагент", , Истина);
			ЦФОКонтрагент = РегистрыСведений.бит_СоответствияАналитик.ПолучитьПравуюАналитику(СоответствиеАналитикЦФОКонтрагент, СтрокаДвижений.Подразделение, Дата);
			ДоговорЦФОКонтрагент = Справочники.ДоговорыКонтрагентов.бит_ПолучитьДоговорКнтрагента(Организация, ЦФОКонтрагент, Перечисления.ТипыДоговоров.СПоставщиком, Дата);
			                  
			Проводка.СчетКт = РегистрыСведений.бит_СоответствияАналитик.ПолучитьПравуюАналитику(СоответствиеАналитикСчетаУчета, ПланыСчетов.Хозрасчетный.РасчетыСПоставщикамиИПодрядчиками, Дата);				
			бит_БухгалтерскийУчет.УстановитьСубконто(Проводка.СчетКт, Проводка.СубконтоКт, 1, СтрокаДвижений.ВидСкидки.бит_СкидкаЗаСчет);
			бит_БухгалтерскийУчет.УстановитьСубконто(Проводка.СчетКт, Проводка.СубконтоКт, 2, ЦФОКонтрагент);
			бит_БухгалтерскийУчет.УстановитьСубконто(Проводка.СчетКт, Проводка.СубконтоКт, 3, ДоговорЦФОКонтрагент);
		
			Проводка.СуммаСценарий = СтрокаДвижений.СуммаСкидки;
			Проводка.СуммаУпр = Проводка.СуммаСценарий;
			Проводка.СуммаРегл = Проводка.СуммаСценарий;
			
			Проводка.Содержание = "Выручка от реализации";
		
			//КонецЕсли;
			
		КонецЕсли;
		
	КонецЦикла;
	
	НаборПроводок.Записать(Истина);
	
	// }}ПервыйБИТ ЛистТребований [ЛТ8.3] [ASHolyavko@1cbit.ru] [06.05.2026] -
	
КонецПроцедуры
