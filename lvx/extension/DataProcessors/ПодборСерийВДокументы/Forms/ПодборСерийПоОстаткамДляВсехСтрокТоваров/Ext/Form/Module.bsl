&После("ПриСозданииНаСервере")
Процедура бит_ПриСозданииНаСервереПосле(Отказ, СтандартнаяОбработка)
	// {{ПервыйБИТ ЛистТребований [ЛТ6.7] [Микляев К.А.] [03.06.2026] +
	Если Элементы.Найти("Сериибит_ЗаказНаПроизводство") <> Неопределено
		Или Элементы.Найти("Серии") = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	НоваяКолонка = Элементы.Добавить("Сериибит_ЗаказНаПроизводство", Тип("ПолеФормы"), Элементы.Серии);
	НоваяКолонка.Вид = ВидПоляФормы.ПолеВвода;
	НоваяКолонка.ПутьКДанным = "Объект.Серии.Серия.бит_ЗаказНаПроизводство";
	НоваяКолонка.Заголовок = "(бит) Заказ на производство";
	НоваяКолонка.ТолькоПросмотр = Истина;
	// }}ПервыйБИТ ЛистТребований [ЛТ6.7] [Микляев К.А.] [03.06.2026] -
КонецПроцедуры
