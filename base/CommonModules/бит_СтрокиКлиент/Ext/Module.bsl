////////////////////////////////////////////////////////////////////////////////
// Модуль содержит методы работы со строками.
//  
////////////////////////////////////////////////////////////////////////////////

#Область СлужебныйПрограммныйИнтерфейс

// Функция возвращает текст с добавлением/удаление разделителя "|".
// 
// Параметры:
//  Текст    - Строка.
//  Добавить - Булево  (По умолчанию = Ложь).
// 
// Возвращаемое значение:
//  Строка.
// 
Функция УбратьДобавитьПалки(Текст, Добавить = Ложь) Экспорт
	
	ТекстДок = Новый ТекстовыйДокумент;
	ТекстДок.УстановитьТекст(Текст);
	КолСтрок = ТекстДок.КоличествоСтрок();
	
	Если Добавить Тогда
		
		Для н = 1 По КолСтрок Цикл
		
			ТекСтр = ТекстДок.ПолучитьСтроку(н);
			
			Если н = 1 Тогда
				
				ТекСтр = """"+ТекСтр;
				
			Иначе	
				
				// Для строковых литералов делаем двойные кавычки.
				ТекСтр = СтрЗаменить(ТекСтр,"""","""""");
				
				// Добавляем переносы строк
				ТекСтр = "|"+ТекСтр;
				
				Если н = КолСтрок Тогда				
					ТекСтр = ТекСтр + """";				
				КонецЕсли; 
				
			КонецЕсли; 
			
			ТекстДок.ЗаменитьСтроку(н, ТекСтр);			
			
		КонецЦикла; 
		
	Иначе	
		
		Для н = 1 По КолСтрок Цикл
		
			ТекСтр = ТекстДок.ПолучитьСтроку(н);
			
			Если н = 1 Тогда				
				// Убираем первую кавычку
				ТекСтр = Сред(ТекСтр,2);				
			ИначеЕсли н = КолСтрок Тогда				
				// Убираем последнюю кавычку
				ТекСтр = Сред(ТекСтр,1, СтрДлина(ТекСтр)-1);				
			КонецЕсли; 
			
			// Для строковых литералов заменям двойную кавычку на одинарную.
			ТекСтр = СтрЗаменить(ТекСтр,"""""","""");
			
			ТекСтр = СтрЗаменить(ТекСтр,"|","");
			
			ТекстДок.ЗаменитьСтроку(н, ТекСтр);
			
		КонецЦикла; 
		
	КонецЕсли;
	
	ТекстНов = ТекстДок.ПолучитьТекст();
	// Убираем перенос последней строки
	ТекстНов = Сред(ТекстНов,1,СтрДлина(ТекстНов)-1);
	
	Возврат ТекстНов;
	
КонецФункции	

// Функция проверяет кооректность формул. Выполняется проверка на завершенность, 
// допустимые символы и количество открывающихся и закрывающихся скобок.
// 
// Параметры:
//  Формула 		  - Строка.
//  ВыводитьСообщения - Булево (По умолчанию = Ложь).
// 
// Возвращаемое значение:
//  Булево.
// 
Функция ФормулаКорректна(Формула, ВыводитьСообщения = Ложь) Экспорт
	
	ФлКорректна = Истина;
	ТекстСообщения = НСтр("ru='Формула некорректна:';en='The formula is not correct:'");
	
	// Совпадение количества открытых и закрытых квадратных и круглых скобок.
	КолОткр = СтрЧислоВхождений(Формула, "[");
	КолЗакр = СтрЧислоВхождений(Формула, "]");
	Если КолОткр <> КолЗакр Тогда
		СтрокаДополнения = НСтр("ru='Обнаружены незакрытые квадратные скобки.';en='Found unclosed brackets.'");
		ТекстСообщения = бит_СтрокиКлиентСервер.ДобавитьСтрокуСРазделителем(ТекстСообщения, СтрокаДополнения, Символы.ПС);
		флКорректна = Ложь;	
	КонецЕсли; 
	
	КолОткр = СтрЧислоВхождений(Формула, "(");
	КолЗакр = СтрЧислоВхождений(Формула, ")");
	Если КолОткр <> КолЗакр Тогда	
		СтрокаДополнения = НСтр("ru='Обнаружены незакрытые круглые скобки.';en='Found unclosed parentheses.'");
		ТекстСообщения = бит_СтрокиКлиентСервер.ДобавитьСтрокуСРазделителем(ТекстСообщения, СтрокаДополнения, Символы.ПС);
		флКорректна = Ложь;	
	КонецЕсли; 
	
	// Операнды в конце строки
	СтрВрем = СокрЛП(Формула);
	ДопустимыеПоследниеСимволы = "1234567890)]";
	ПоследнийСимвол = Прав(СтрВрем,1);
	Если Найти(ДопустимыеПоследниеСимволы, ПоследнийСимвол) = 0 Тогда	
		СтрокаДополнения = НСтр("ru='Формула не завершена.';en='The formula has not been completed.'");
		ТекстСообщения = бит_СтрокиКлиентСервер.ДобавитьСтрокуСРазделителем(ТекстСообщения, СтрокаДополнения, Символы.ПС);
		флКорректна = Ложь;	
	КонецЕсли; 
	
	// Недопустимые символы 
	// необходимо убрать аргументы, т.к. в GUID могут встречаться буквенные символы.
	МассивАргументов = бит_СтрокиКлиентСервер.РазобратьФормулу(Формула);
	СтрВрем = Формула;
	Для каждого Аргумент Из МассивАргументов Цикл
	
		СтрВрем = СтрЗаменить(СтрВрем, "["+Аргумент+"]"," ");
	
	КонецЦикла; 
	
	// Убираем функции встроенного языка, которые могут использоваться в формулах.
	ПереченьФункций = Новый Массив;
	ПереченьФункций.Добавить("Мин");
	ПереченьФункций.Добавить("Макс");
	ПереченьФункций.Добавить("Sqrt");
	ПереченьФункций.Добавить("Окр");
	ПереченьФункций.Добавить("Цел");
	
	Для каждого ТекФункция Из ПереченьФункций Цикл
	
		СтрВрем = СтрЗаменить(СтрВрем, ТекФункция+"(","("); 
	
	КонецЦикла; 
	
	// Теперь во временной строке должны остатья только операнды и константы, 
	// проверим оставщуюся строку на допустимые символы.
	ДопустимыеСимволы = " 1234567890+-*/%()?.,=<>";
	НедопустимыеСимволы = "";
	Для ном = 1 По СтрДлина(СтрВрем) Цикл
		ТекСимв = Сред(СтрВрем, Ном, 1);
		Если Найти(ДопустимыеСимволы, ТекСимв) = 0 Тогда
			
			НедопустимыеСимволы = НедопустимыеСимволы + ТекСимв;
			
		КонецЕсли;	
	КонецЦикла;
	
	Если СтрДлина(НедопустимыеСимволы) > 0 Тогда
		
		ТекстДополнение = НСтр("ru='Формула содержит недопустимые символы ""%1"".';
							   |en='Formula contains invalid characters ""%1"".'");
		ТекстСообщения = бит_СтрокиКлиентСервер.ДобавитьСтрокуСРазделителем(ТекстСообщения, ТекстДополнение, Символы.ПС);
		ТекстСообщения = СтрШаблон(ТекстСообщения, НедопустимыеСимволы);
		флКорректна = Ложь;			
		
	КонецЕсли; 
	
	Если НЕ ФлКорректна И ВыводитьСообщения Тогда
	
		бит_ОбщегоНазначенияКлиентСервер.ВывестиСообщение(ТекстСообщения);
	
	КонецЕсли; 
	
	Возврат ФлКорректна;
	
КонецФункции // ФормулаКорректна()

// Выполняет проверку на наличие спецсимволов.
//
// Параметры
//  ИсходнаяСтрока - Строка.
//	СпецСимволы	   - Строка.
// 
// Возвращаемое значение:
//  Строка.
// 
Функция ПроверитьСпецСимволы(ИсходнаяСтрока,СпецСимволы = " .,;:[]{}'""/\?!@#$%^&*+=<>~`|()№",ИмяКолонки = "") Экспорт
	
	ПрошлаПроверка = Истина;
	СтрокаНаПроверку = ИсходнаяСтрока;
	
	ТекстСообщения = "";
	
	ЗапрещенныеСимволы = "";
	Для ном = 1 По СтрДлина(СпецСимволы) Цикл
		ТекСимв = Сред(СпецСимволы, Ном, 1);
		ПозСимв = Найти(СтрокаНаПроверку, ТекСимв);		
		Если ПозСимв > 0 Тогда
			СтрокаНаПроверку = СтрЗаменить(СтрокаНаПроверку,ТекСимв,"");
			ЗапрещенныеСимволы = ЗапрещенныеСимволы+ТекСимв;
			ПрошлаПроверка = Ложь;
		КонецЕсли;	
	КонецЦикла;
	
	Если НЕ ПустаяСтрока(ЗапрещенныеСимволы) Тогда
		ТекстПромежуточный = НСтр("ru='Символы ""%1"" не могут быть использованы при заполнении поля ""%2"".';
								  |en='The symbols ""%1"" can not be used in the field entry ""%2"".'");
		ТекстПромежуточный = СтрШаблон(ТекстПромежуточный, ЗапрещенныеСимволы, ИмяКолонки);
		Если ПустаяСтрока(ТекстСообщения) Тогда
			ТекстСообщения = ТекстПромежуточный;
		Иначе
			ТекстСообщения = ТекстСообщения + Символы.ПС + ТекстПромежуточный;
		КонецЕсли;
		ОбщегоНазначенияКлиент.СообщитьПользователю(ТекстСообщения);	
	КонецЕсли; 
	
	Возврат СтрокаНаПроверку;
	
КонецФункции

#КонецОбласти
