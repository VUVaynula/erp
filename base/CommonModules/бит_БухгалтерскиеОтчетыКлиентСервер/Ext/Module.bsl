
#Область ПрограммныйИнтерфейс

#Область ФункцииКонструкторы

// Модифицирует универсальные настройки.
// 
// Параметры:
//  Настройки - Структура - См. БухгалтерскиеОтчетыКлиентСервер.НовыйУниверсальныеНастройки().
//  ИмяИсходногоОтчета - Строка - Имя проверяемого отчета.
//
Процедура НовыйУниверсальныеНастройки(Настройки, ИмяИсходногоОтчета) Экспорт
	
	Если СтрНачинаетсяС(ИмяИсходногоОтчета, "бит_") Тогда
		
		Настройки.Вставить("ПоказательРегл" , Ложь);
		Настройки.Вставить("ПоказательУпр"  , Ложь);
		Настройки.Вставить("ПоказательСумма", Истина);	// По умолчанию Сумма включена.
		
		Если СтрЗаканчиваетсяНа(ИмяИсходногоОтчета, "МУ") Тогда
			Настройки.Вставить("Счет"		 , ПредопределенноеЗначение("ПланСчетов.бит_Дополнительный_2.ПустаяСсылка"));
			Настройки.Вставить("ПоказательМУ", Истина);	// По умолчанию МУ включен.
		ИначеЕсли СтрЗаканчиваетсяНа(ИмяИсходногоОтчета, "Бюджетирование") Тогда	
			Настройки.Вставить("Счет"			   , ПредопределенноеЗначение("ПланСчетов.бит_Бюджетирование.ПустаяСсылка"));
			Настройки.Вставить("Сценарий" 		   , ПредопределенноеЗначение("Справочник.СценарииПланирования.ПустаяСсылка"));
			Настройки.Вставить("ПоказательСценарий", Истина);	// По умолчанию Сценарий включен.
		Иначе
			Настройки.Вставить("ПоказательРегл" , Истина);	
		КонецЕсли;			
		
	КонецЕсли; 
	
КонецПроцедуры

#КонецОбласти

#Область Интерфейсные

// Выполняет настройку формы отчета при изменении сценария.
//
// Параметры:
//	Форма - ФормаКлиентскогоПриложения - Форма отчета.
//  Организация - СправочникСсылка.Организации - организация, заданная в форме.
//
Процедура СценарийПриИзменении(Форма, Сценарий) Экспорт 
	
	Отчет = Форма.Отчет;
		
	Для Каждого ЭлементОтбора Из Отчет.КомпоновщикНастроек.Настройки.Отбор.Элементы Цикл
		Если ТипЗнч(ЭлементОтбора) = Тип("ЭлементОтбораКомпоновкиДанных") И ЭлементОтбора.Использование Тогда
			
			Если ЭлементОтбора.ЛевоеЗначение = Новый ПолеКомпоновкиДанных("Сценарий") Тогда
				
				Если ЭлементОтбора.ВидСравнения = ВидСравненияКомпоновкиДанных.Равно Тогда
					ЭлементОтбора.ПравоеЗначение = Сценарий;
				Иначе
					ЭлементОтбора.ПравоеЗначение = Неопределено;
				КонецЕсли; 
								
			ИначеЕсли ЗначениеЗаполнено(ЭлементОтбора.ПравоеЗначение) Тогда
				
				ЭлементОтбора.ПравоеЗначение = Неопределено;
				ЭлементОтбора.Использование = Ложь;
				
			КонецЕсли;
			
		КонецЕсли;
	КонецЦикла;
		
КонецПроцедуры

#КонецОбласти

#КонецОбласти

#Область СлужебныйПрограммныйИнтерфейс

// Модифицирует универсальные настройки.
// См. БухгалтерскиеОтчетыКлиентСервер.ОтчетПоддерживаетУниверсальныеНастройки(ИдентификаторОбъекта)
//
Процедура ОтчетПоддерживаетУниверсальныеНастройки(СписокОтчетов) Экспорт
	
	СписокОтчетов.Добавить("бит_АнализСубконтоБюджетирование");
	СписокОтчетов.Добавить("бит_АнализСчетаБюджетирование");
	СписокОтчетов.Добавить("бит_КарточкаСубконтоБюджетирование");
	СписокОтчетов.Добавить("бит_КарточкаСчетаБюджетирование");
	СписокОтчетов.Добавить("бит_ОборотноСальдоваяВедомостьБюджетирование");
	СписокОтчетов.Добавить("бит_ОборотноСальдоваяВедомостьПоСчетуБюджетирование");
	СписокОтчетов.Добавить("бит_ОборотыМеждуСубконтоБюджетирование");
	СписокОтчетов.Добавить("бит_ОборотыСчетаБюджетирование");
	СписокОтчетов.Добавить("бит_ОтчетПоПроводкамБюджетирование");
	СписокОтчетов.Добавить("бит_СводныеПроводкиБюджетирование");
	СписокОтчетов.Добавить("бит_ШахматнаяВедомостьБюджетирование");
	
	СписокОтчетов.Добавить("бит_АнализСубконтоМУ");
	СписокОтчетов.Добавить("бит_АнализСчетаМУ");
	СписокОтчетов.Добавить("бит_КарточкаСубконтоМУ");
	СписокОтчетов.Добавить("бит_КарточкаСчетаМУ");
	СписокОтчетов.Добавить("бит_ОборотноСальдоваяВедомостьМУ");
	СписокОтчетов.Добавить("бит_ОборотноСальдоваяВедомостьПоСчетуМУ");
	СписокОтчетов.Добавить("бит_ОборотыМеждуСубконтоМУ");
	СписокОтчетов.Добавить("бит_ОборотыСчетаМУ");
	СписокОтчетов.Добавить("бит_ОтчетПоПроводкамМУ");
	СписокОтчетов.Добавить("бит_СводныеПроводкиМУ");
	СписокОтчетов.Добавить("бит_ШахматнаяВедомостьМУ");
		
КонецПроцедуры

#КонецОбласти
