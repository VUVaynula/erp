
#Область СлужебныйПрограммныйИнтерфейс

#Область ПроцедурыИФункцииПолученияСпискаОбъектовДляВыбораВИнтерфейсе

// Функция формирует список дополнительных объектов управленческого учета:
// ПланыВидовХарактеристик, ПланыСчетов, РегистрыБухгалтерии - от 1 до 5.
// 
// Параметры:
//  ВидОбъекта - "ПеречислениеСсылка.бит_ВидыОбъектовСистемы".
//  ИмяОбъекта - Строка, имя объекта коллекции метаданных.
// 
// Возвращаемое значение:
//   СписокОбъектов - СписокЗначений.
// 
Функция СформироватьСписокОбъектовДляВыбора(ВидОбъекта, ИмяОбъекта) Экспорт

	МассивИменОбъектов = Новый Массив;
	
	// Заполняем массив имен объектов метаданных.
	Для Ном = 1 По 5 Цикл
		МассивИменОбъектов.Добавить(ИмяОбъекта + "_" + Ном);
	КонецЦикла;
	
	// Получаем массив объектов системы.
	МассивОбъектов = бит_ПраваДоступа.ОбъектыСистемыПоВидуИИменам(ВидОбъекта, МассивИменОбъектов);
	
	// Получаем список значений объектов для выбора.
	СписокВыбора = бит_ПраваДоступа.ОбъектыСистемыДляВыбора(МассивОбъектов);
	
	Возврат СписокВыбора;
	
КонецФункции // СформироватьСписокОбъектовДляВыбора()

#КонецОбласти

#Область КлассификацияПроводокПоТипамПроводок

// Процедура заполняет запись регистра бит_ОборотыПоТипамПроводок по проводке регистра бухгалтерии.
// 
// Параметры:
//  ЗаписьИсточник - РегистрБухгалтерииЗапись.
//  ЗаписьПриемник - РегистрНакопления.бит_ОборотыПоТипамПроводок.
//  ОбъектСистемыИсточник - СправочникСсылка.бит_ОбъектыСистемы.
//  СтрПар - Структура.
// 
Процедура ЗаполнитьЗаписьРегистраТипыПроводок(ЗаписьИсточник, ЗаписьПриемник, ОбъектСистемыИсточник, СтрПар) Экспорт
	
	ЗаписьПриемник.Активность  = ЗаписьИсточник.Активность;
	ЗаписьПриемник.Период      = ЗаписьИсточник.Период;
	ЗаписьПриемник.РегистрБухгалтерии = ОбъектСистемыИсточник;
	ЗаписьПриемник.Организация = ЗаписьИсточник.Организация;
	ЗаписьПриемник.ТипПроводки = ЗаписьИсточник.ТипПроводки;
    
    // Счета
	ЗаписьПриемник.СчетДт = ЗаписьИсточник.СчетДт;
    ЗаписьПриемник.СчетКт = ЗаписьИсточник.СчетКт;
    
    СвСчИсДт = бит_БухгалтерскийУчетВызовСервераПовтИсп.ПолучитьСвойстваСчета(ЗаписьИсточник.СчетДт);
    СвСчИсКт = бит_БухгалтерскийУчетВызовСервераПовтИсп.ПолучитьСвойстваСчета(ЗаписьИсточник.СчетКт);
    
    // Субконто Дт
	Для Сч = 1 По СвСчИсДт.КоличествоСубконто Цикл
        Если Сч > СтрПар.КолвоСубконтоПриемник Тогда
			Прервать;
		КонецЕсли;
    	ИмяИзмерения = "СубконтоДт"+Сч;
        ЗаписьПриемник[ИмяИзмерения] = ЗаписьИсточник.СубконтоДт[СвСчИсДт["ВидСубконто" + Сч]];
    КонецЦикла; 
	
	// Субконто Кт
    Для Сч = 1 По СвСчИсКт.КоличествоСубконто Цикл
        Если Сч > СтрПар.КолвоСубконтоПриемник Тогда
			Прервать;
		КонецЕсли;
    	ИмяИзмерения = "СубконтоКт"+Сч;
        ЗаписьПриемник[ИмяИзмерения] = ЗаписьИсточник.СубконтоКт[СвСчИсКт["ВидСубконто" + Сч]];
    КонецЦикла;    
    
	// Суммы
	ЗаписьПриемник.СуммаРегл = ЗаписьИсточник.СуммаРегл;
	ЗаписьПриемник.СуммаУпр  = ЗаписьИсточник.СуммаУпр;
	
	Если СтрПар.ЕстьСуммаМУ Тогда
		ЗаписьПриемник.СуммаМУ = ЗаписьИсточник.СуммаМУ;
	КонецЕсли;
	
КонецПроцедуры // ЗаполнитьЗаписьРегистраТипыПроводок()

// Процедура заполняет заполняет тип проводки в записях набора записей регистра бухгалтерии
// 	и переносит проводки в регистр накопления, чтобы с ними можно было работать при формировании отчетности
// 	по типам проводок.
// 
// Параметры:
// 	НаборЗаписей.
// 	Регистратор.
// 
Процедура ОбработатьПроводкиПоТипамПроводок(НаборЗаписей, Регистратор, ОчиститьРегистрПриемник = Истина) Экспорт
    
    Если Метаданные.РегистрыНакопления.Найти("бит_ОборотыПоТипамПроводок") = Неопределено Тогда
        Возврат;    
    КонецЕсли;
	
	ВестиУчетПоТипамПроводок = бит_ОбщиеПеременныеСервер.ЗначениеПеременной("бит_ВестиУчетПоТипамПроводок");
	Если НЕ ВестиУчетПоТипамПроводок Тогда
		Возврат;
	КонецЕсли;

	МетаРегистрПриемник = Метаданные.РегистрыНакопления.бит_ОборотыПоТипамПроводок;
	МетаРегистрИсточник = НаборЗаписей.Метаданные();
	МетаРегистратор		= Регистратор.Метаданные();
	
	Если бит_РаботаСМетаданными.ЕстьРеквизит("Организация", МетаРегистратор) Тогда
		Организация = Регистратор.Организация;
	Иначе
		Организация = Справочники.Организации.ПустаяСсылка();
	КонецЕсли;	
		
	// Сначала в исходном наборе записей определим тип проводки если потребуется.
	
	ТипыПроводокПоТипамДокументов = Новый Соответствие;
	
	ТипыПроводокПоТипамДокументов.Вставить(Тип("ДокументСсылка.бит_му_Элиминация")						  , Справочники.бит_ТипыПроводок.КонсолидационныеКорректировки);
	ТипыПроводокПоТипамДокументов.Вставить(Тип("ДокументСсылка.бит_му_КонсолидационныеКорректировки")	  , Справочники.бит_ТипыПроводок.КонсолидационныеКорректировки);
	ТипыПроводокПоТипамДокументов.Вставить(Тип("ДокументСсылка.бит_му_КонсолидационныеКорректировкиНовый"), Справочники.бит_ТипыПроводок.КонсолидационныеКорректировки);
	ТипыПроводокПоТипамДокументов.Вставить(Тип("ДокументСсылка.бит_му_СторноЭлиминационныхНачислений")    , Справочники.бит_ТипыПроводок.КонсолидационныеКорректировки);
	
	Если ТипыПроводокПоТипамДокументов[ТипЗнч(Регистратор)] = Неопределено Тогда
		ТипПроводкиПоУмолчанию = Справочники.бит_ТипыПроводок.ПараллельныйУчет;
	Иначе
		ТипПроводкиПоУмолчанию = ТипыПроводокПоТипамДокументов[ТипЗнч(Регистратор)];
	КонецЕсли;
	
	Для Каждого Проводка Из НаборЗаписей Цикл
		
		Если НЕ ЗначениеЗаполнено(Проводка.ТипПроводки) Тогда
			Проводка.ТипПроводки = ТипПроводкиПоУмолчанию;
		КонецЕсли;
		
	КонецЦикла;
		
	// Перенесем проводки в регистр накопления бит_ОборотыПоТипамПроводок.
	Если НЕ РегистраторУстановленПравильно(МетаРегистрИсточник, МетаРегистрПриемник, МетаРегистратор) Тогда
		Возврат;
	КонецЕсли;
	
	// Определим количество субконто доступных в регистре приемнике.
	МаксКолСубконто = МетаРегистрИсточник.ПланСчетов.МаксКоличествоСубконто;
	КолвоСубконтоПриемник = 0;
	Для Ном=1 По МаксКолСубконто Цикл
		ИмяИзмерения = "СубконтоДт"+Ном;
		НайденноеИзмерение = МетаРегистрПриемник.Измерения.Найти(ИмяИзмерения);
		Если НайденноеИзмерение = Неопределено Тогда
			Прервать;
		КонецЕсли;
		КолвоСубконтоПриемник = КолвоСубконтоПриемник + 1;
	КонецЦикла;
	
	ОбъектСистемыИсточник = бит_ПраваДоступа.ПолучитьОбъектДоступаПоМетаданным(МетаРегистрИсточник);
	
	ЕстьСуммаМУ = НЕ МетаРегистрИсточник.Ресурсы.Найти("СуммаМУ") = Неопределено;
	
	// Параметры, используемые для формирования записи.
	СтрПарЗапись = Новый Структура;
	СтрПарЗапись.Вставить("ЕстьСуммаМУ", ЕстьСуммаМУ);
	СтрПарЗапись.Вставить("КолвоСубконтоПриемник", КолвоСубконтоПриемник); 
	
	НаборПриемник = РегистрыНакопления.бит_ОборотыПоТипамПроводок.СоздатьНаборЗаписей();
	НаборПриемник.Отбор.Регистратор.Установить(Регистратор);
	НаборПриемник.Прочитать();
	Если ОчиститьРегистрПриемник Тогда
	
		 НаборПриемник.Очистить();
	
	КонецЕсли; 
	
	// Подбор типов проводок трансляцией, если правила установлены.
	УстановитьПривилегированныйРежим(Истина);
	ПараметрыТрансляции = ПодготовитьСтруктуруПараметровДляПодбораТиповПроводок(Организация, 
							Регистратор, МетаРегистрИсточник, МетаРегистрПриемник);
	УстановитьПривилегированныйРежим(Ложь);
	
	Если ПараметрыТрансляции.ПравилаТрансляции.Количество() > 0 Тогда
		// Вызов трансляции
		Протокол = "";
		ПараметрыТрансляции.Вставить("СтрПарЗапись", СтрПарЗапись);
	    бит_МеханизмТрансляции.ВыполнитьТрансляциюДвижений(Регистратор, НаборЗаписей, НаборПриемник, ПараметрыТрансляции, Протокол);
	Иначе
		Для Каждого Проводка Из НаборЗаписей Цикл
			НоваяЗапись = НаборПриемник.Добавить();
			ЗаполнитьЗаписьРегистраТипыПроводок(Проводка, НоваяЗапись, ОбъектСистемыИсточник, СтрПарЗапись);
		КонецЦикла;
	КонецЕсли; 																					  
	
	бит_ОбщегоНазначения.ЗаписатьНаборЗаписейРегистра(НаборПриемник, "Ошибки");
	
КонецПроцедуры
 
#КонецОбласти

#Область БухгалтерскиеОтчеты 

// Вызывается из стандартных отчетов Упр. учета.
//
// Параметры:
// 	СхемаКомпоновкиДанных - СхемаКомпоновкиДанных - 
// 	КомпоновщикНастроек - КомпоновщикНастроекКомпоновкиДанных - 
// 	ДокументРезультат - ТабличныйДокумент - 
// 	ДанныеРасшифровки - ДанныеРасшифровкиКомпоновкиДанных - 
// 	СтандартнаяОбработка - Булево - 
//
Процедура ПриКомпоновкеРезультата(СхемаКомпоновкиДанных, КомпоновщикНастроек, ДокументРезультат, ДанныеРасшифровки, СтандартнаяОбработка) Экспорт
	
	СтандартнаяОбработка = Ложь; 
	ДокументРезультат.Очистить();
	
	КлючВарианта = "";
	Если КомпоновщикНастроек.ПользовательскиеНастройки.ДополнительныеСвойства.Свойство("КлючВарианта") Тогда
		КлючВарианта = КомпоновщикНастроек.ПользовательскиеНастройки.ДополнительныеСвойства.КлючВарианта;
	ИначеЕсли КомпоновщикНастроек.ФиксированныеНастройки.ДополнительныеСвойства.Свойство("ВариантНаименование") Тогда
		КлючВарианта = СхемаКомпоновкиДанных.ВариантыНастроек[КомпоновщикНастроек.ФиксированныеНастройки.ДополнительныеСвойства.ВариантНаименование];
	КонецЕсли;
	
	ИндексПланаСчетов 	   = бит_ОтчетыБФКлиентСервер.ПолучитьПараметр(КомпоновщикНастроек, "ПланСчетов").Значение;
	ИмяПланаСчетовСтр 	   = СтрШаблон("бит_Дополнительный_%1", ИндексПланаСчетов);
	МаксКоличествоСубконто = Метаданные.ПланыСчетов[ИмяПланаСчетовСтр].МаксКоличествоСубконто;
	КоличествоСубконто	   = 0;

	ПараметрВидСубконто = бит_ОтчетыБФКлиентСервер.ПолучитьПараметр(КомпоновщикНастроек, "ВидСубконто");
	Если ПараметрВидСубконто <> Неопределено И ПараметрВидСубконто.Использование Тогда
		
		ТипВидаСубконтоСтр = СтрШаблон("ПланВидовХарактеристикСсылка.бит_ВидыСубконтоДополнительные_%1", ИндексПланаСчетов);
		Если Не ЗначениеЗаполнено(ПараметрВидСубконто.Значение) Тогда
			КоличествоСубконто = 0
		ИначеЕсли ТипЗнч(ПараметрВидСубконто.Значение) = Тип(ТипВидаСубконтоСтр) Тогда
			КоличествоСубконто = 1;
		ИначеЕсли ТипЗнч(ПараметрВидСубконто.Значение) = Тип("СписокЗначений") Тогда
			КоличествоСубконто = ПараметрВидСубконто.Значение.Количество();
		КонецЕсли;
		
		Если КоличествоСубконто < МаксКоличествоСубконто Тогда 
			ГруппировкаСубконто = Неопределено;
			НайтиГруппировкуПоПолю(КомпоновщикНастроек.Настройки.Структура, "Субконто" + (КоличествоСубконто + 1), ГруппировкаСубконто); 
			
			Если ГруппировкаСубконто <> Неопределено Тогда
				ГруппировкаСубконто.Использование = Ложь;
				Ид = КомпоновщикНастроек.ПользовательскиеНастройки.ПолучитьИдентификаторПоОбъекту(ГруппировкаСубконто);
				ПользовательскаяГруппировка = КомпоновщикНастроек.ПользовательскиеНастройки.ПолучитьОбъектПоИдентификатору(Ид);
				Если ПользовательскаяГруппировка <> Неопределено Тогда
					ПользовательскаяГруппировка.Использование = Ложь;
					КомпоновщикНастроек.ПользовательскиеНастройки.ДополнительныеСвойства.Вставить("ПользовательскиеНастройкиМодифицированы", Истина);
				КонецЕсли;
				
				ОсновнойНаборДанных = СхемаКомпоновкиДанных.НаборыДанных.ОсновнойНаборДанных;
				Если ТипЗнч(ОсновнойНаборДанных) = Тип("НаборДанныхОбъединениеСхемыКомпоновкиДанных") Тогда
					ТекстЗапросаОстатки = ОсновнойНаборДанных.Элементы.Найти("Остатки").Запрос;
					ТекстЗапросаОбороты = ОсновнойНаборДанных.Элементы.Найти("Обороты").Запрос;
					Для НомерСубконто = КоличествоСубконто + 1 По МаксКоличествоСубконто Цикл
						ТекстСубконто 		= ".Субконто" + НомерСубконто + " КАК Субконто" + НомерСубконто + "," + Символы.ПС;
						ТекстЗапросаОстатки = СтрЗаменить(ТекстЗапросаОстатки, "ОстаткиИОбороты" + ТекстСубконто, "");
						ИмяТаблицы 			= СтрШаблон(СтрШаблон("бит_Дополнительный_%1Обороты", ИндексПланаСчетов));
						ТекстЗапросаОбороты = СтрЗаменить(ТекстЗапросаОбороты, ИмяТаблицы + ТекстСубконто, "");
					КонецЦикла;
					ОсновнойНаборДанных.Элементы.Найти("Остатки").Запрос = ТекстЗапросаОстатки;
					ОсновнойНаборДанных.Элементы.Найти("Обороты").Запрос = ТекстЗапросаОбороты;
				КонецЕсли;
			КонецЕсли;
		КонецЕсли;
		
	КонецЕсли;

	ВалютыУпр = ВалютыУпр();
	
	УстановитьОбщиеПараметрыСтандартныхОтчетов(КомпоновщикНастроек, ВалютыУпр);
	
	НастройкиКомпоновкиДанных = КомпоновщикНастроек.ПолучитьНастройки();

	ПоказателиОтчета = Новый Структура;
	ПоказателиОтчета.Вставить("ВалютнаяСумма", 	бит_ОтчетыБФКлиентСервер.ПолучитьПараметр(КомпоновщикНастроек, "ПоказательВалютнаяСумма"));
	ПоказателиОтчета.Вставить("СуммаУпр", 		бит_ОтчетыБФКлиентСервер.ПолучитьПараметр(КомпоновщикНастроек, "ПоказательСуммаУпр"));
	ПоказателиОтчета.Вставить("СуммаРегл", 		бит_ОтчетыБФКлиентСервер.ПолучитьПараметр(КомпоновщикНастроек, "ПоказательСуммаРегл"));
	ПоказателиОтчета.Вставить("Количество", 	бит_ОтчетыБФКлиентСервер.ПолучитьПараметр(КомпоновщикНастроек, "ПоказательКоличество"));

	УстановитьГруппировкуВалюты(КомпоновщикНастроек, ПоказателиОтчета.ВалютнаяСумма.Значение);
	НастройкиКомпоновкиДанных = КомпоновщикНастроек.ПолучитьНастройки();
		
	Если КомпоновщикНастроек.ФиксированныеНастройки.ДополнительныеСвойства.Свойство("ТипИтога") Тогда
		
		ТипИтога = КомпоновщикНастроек.ФиксированныеНастройки.ДополнительныеСвойства.ТипИтога;
		НачальноеСальдо = КомпоновщикНастроек.ФиксированныеНастройки.ДополнительныеСвойства.НачальноеСальдо;
		ПоказателиОтчетаСУчетомТипаИтога(ТипИтога, НачальноеСальдо, ПоказателиОтчета);
		
	КонецЕсли;
	
	НастроитьПоляВыбора(ПоказателиОтчета, НастройкиКомпоновкиДанных);
	Если НастройкиКомпоновкиДанных.Структура.Количество() И ТипЗнч(НастройкиКомпоновкиДанных.Структура[0]) = Тип("ТаблицаКомпоновкиДанных") Тогда
		НастроитьПоляВыбора(ПоказателиОтчета, НастройкиКомпоновкиДанных.Структура[0]);
	КонецЕсли;
		
	ГруппаОтбора = КомпоновщикНастроек.ФиксированныеНастройки.Отбор.Элементы.Добавить(Тип("ГруппаЭлементовОтбораКомпоновкиДанных"));
	ГруппаОтбора.ТипГруппы = ТипГруппыЭлементовОтбораКомпоновкиДанных.ГруппаИли;
	ГруппаОтбора.Представление = НСтр("ru = 'Отбор по выбранным полям';
									  |en = 'Filter upon selected fields'");
	ГруппаОтбора.Использование = Истина;
	ГруппаОтбора.РежимОтображения = РежимОтображенияЭлементаНастройкиКомпоновкиДанных.Недоступный;	
	НастроитьОтборПоСуммам(НастройкиКомпоновкиДанных.Выбор.Элементы, ГруппаОтбора);
	
	КомпоновщикМакета = Новый КомпоновщикМакетаКомпоновкиДанных;
	МакетКомпоновки = КомпоновщикМакета.Выполнить(СхемаКомпоновкиДанных, НастройкиКомпоновкиДанных, ДанныеРасшифровки);
	
	ПроцессорКомпоновкиДанных = Новый ПроцессорКомпоновкиДанных;
	ПроцессорКомпоновкиДанных.Инициализировать(МакетКомпоновки,, ДанныеРасшифровки);
	
	ПроцессорВывода = Новый ПроцессорВыводаРезультатаКомпоновкиДанныхВТабличныйДокумент;
	ПроцессорВывода.УстановитьДокумент(ДокументРезультат);
	ПроцессорВывода.Вывести(ПроцессорКомпоновкиДанных, Истина);
	
	Параметр = КомпоновщикНастроек.Настройки.ПараметрыДанных.ДоступныеПараметры.НайтиПараметр(ПоказателиОтчета.СуммаРегл.Параметр);
	УстановитьВалюту(ДокументРезультат, ВалютыУпр.ВалютаРегл, Параметр.Заголовок);
	Параметр = КомпоновщикНастроек.Настройки.ПараметрыДанных.ДоступныеПараметры.НайтиПараметр(ПоказателиОтчета.СуммаУпр.Параметр);
	УстановитьВалюту(ДокументРезультат, ВалютыУпр.ВалютаУпр, Параметр.Заголовок);

	УдалитьЗаголовкиПолей(ДокументРезультат);

	СФормироватьЗаголовкиПолей(КлючВарианта, КомпоновщикНастроек, СхемаКомпоновкиДанных, "");
	
	ПослеКомпоновкиРезультата(ДокументРезультат, ПоказателиОтчета, КлючВарианта);

КонецПроцедуры

// АПК:142-выкл Количество необязательных параметров более 3 для совместимости с предыдущими версиями библиотеки.

// Копирует элементы отбора из одной коллекции отбора в другую
//
// Параметры:
//  ОтборИсточник - ОтборКомпоновкиДанных - откуда копировать.
//  ОтборПриемник - ОтборКомпоновкиДанных - куда копировать.
//  ТолькоИспользуемые - Булево - копировать только используемые.
//  ПоляИсключения - Структура - в ключах структуры перечислены поля которые не должны попасть в отбор приемник.
//  Постфикс - Строка - произвольная строка которая будет добавлена к имени левого поля.
//  ПроверятьДоступностьПолей - Булево - Проверять доступность полей отбора при копировании. По умолчанию Ложь.
//
Процедура СкопироватьОтбор(ОтборИсточник, ОтборПриемник, ТолькоИспользуемые = Ложь, ПоляИсключения = Неопределено, Постфикс = "", ПроверятьДоступностьПолей = Ложь) Экспорт
	
	ПараметрыПроверкиДоступностиПолей = Новый Структура;
	ПараметрыПроверкиДоступностиПолей.Вставить("ПроверятьДоступностьПолей", ПроверятьДоступностьПолей);
	
	ДоступныеПоляОтбора = Новый ТаблицаЗначений;
	ДоступныеПоляОтбора.Колонки.Добавить("ПолеКомпоновки");
	ДоступныеПоляОтбора.Колонки.Добавить("ИмяПоля");
	Если ПроверятьДоступностьПолей Тогда
		ЗаполнитьДоступныеПоляРекурсивно(ДоступныеПоляОтбора, ОтборПриемник.ДоступныеПоляОтбора.Элементы);
	КонецЕсли;
	ДоступныеПоляОтбора.Индексы.Добавить("ПолеКомпоновки");
	ДоступныеПоляОтбора.Индексы.Добавить("ИмяПоля");
	
	ПараметрыПроверкиДоступностиПолей.Вставить("ДоступныеПоляОтбора", ДоступныеПоляОтбора);
	
	СкопироватьОтборРекурсивно(ОтборИсточник, ОтборПриемник, ТолькоИспользуемые, ПоляИсключения, Постфикс, ПараметрыПроверкиДоступностиПолей);
	
КонецПроцедуры // АПК:142-вкл

#КонецОбласти 

// Функция получает таблицу переоценки денежных статей.
// 
// Параметры:
//  СтруктураПараметров - Структура.
// 						Обязательные ключи:
//                         ДатаПереоценки 	   - Дата.
//                         ИмяРегБухгалтерии   - Строка.
//                         МассивСчетов        - Массив.
//                         СтруктураКурсыВалют - Структура. Возможные имена ключей: / "Упр" / "МУ" / "Сценарий" /.
//                      Дополнительные ключи:
//                         Организация      	   - СправочникСсылка.Организации. По умолчанию Неопределено.
//                         Сценарий      		   - СправочникСсылка.СценарииПланирования. По умолчанию Неопределено.
//                         ПереоценкаСуммаУпр      - Булево. По умолчанию Ложь.
//                         ПереоценкаСуммаСценарий - Булево. По умолчанию Ложь.
//                         ПереоценкаСуммаМУ       - Булево. По умолчанию Ложь.
// 
// Возвращаемое значение:
//  ТаблицаПереоценки - ТаблицаЗначений.
// 
Функция ДанныеПереоценкиВалютныхДенежныхСтатей(СтруктураПараметров) Экспорт
	
	ТаблицаПереоценки = Новый ТаблицаЗначений;
	
	// Получим значения для выполнения переоценки.
	ДатаПереоценки	  = СтруктураПараметров.ДатаПереоценки;
	ИмяРегБухгалтерии = СтруктураПараметров.ИмяРегБухгалтерии;
	МассивСчетов	  = СтруктураПараметров.МассивСчетов;
	
	Если СтруктураПараметров.Свойство("Организация") Тогда
		Организация = СтруктураПараметров.Организация;
	Иначе
		Организация = Неопределено;
	КонецЕсли;
	
	Если СтруктураПараметров.Свойство("Сценарий") Тогда
		Сценарий = СтруктураПараметров.Сценарий;
	Иначе
		Сценарий = Неопределено;
	КонецЕсли;
	
	МатаданныеПланаСчетов	   = Метаданные.РегистрыБухгалтерии[ИмяРегБухгалтерии].ПланСчетов;
	ИмяПланаСчетов			   = МатаданныеПланаСчетов.Имя;
	ИмяПланаВидовХарактеристик = МатаданныеПланаСчетов.ВидыСубконто.Имя;
	КоличествоСубконто	   	   = МатаданныеПланаСчетов.МаксКоличествоСубконто;
	
	// Получим структура курсов валют.
	СтруктураКурсыВалют = СтруктураПараметров.СтруктураКурсыВалют;
	
	РесурсыПереоценки = Новый Структура;
	
	Если СтруктураПараметров.Свойство("ПереоценкаСуммаУпр") Тогда
		
		ПереоценкаСуммаУпр = СтруктураПараметров.ПереоценкаСуммаУпр;
		
		Если ПереоценкаСуммаУпр Тогда
			РесурсыПереоценки.Вставить("СуммаУпр", СтруктураКурсыВалют.Упр);
		КонецЕсли;
		
	Иначе
		ПереоценкаСуммаУпр = Ложь;
	КонецЕсли;
	
	Если СтруктураПараметров.Свойство("ПереоценкаСуммаРегл") Тогда
		
		ПереоценкаСуммаРегл = СтруктураПараметров.ПереоценкаСуммаРегл;
		
		Если ПереоценкаСуммаРегл Тогда
			РесурсыПереоценки.Вставить("СуммаРегл", СтруктураКурсыВалют.Регл);
		КонецЕсли;
		
	Иначе
		ПереоценкаСуммаРегл = Ложь;
	КонецЕсли;
	
	Если СтруктураПараметров.Свойство("ПереоценкаСуммаСценарий") Тогда
		
		ПереоценкаСуммаСценарий = СтруктураПараметров.ПереоценкаСуммаСценарий;
		
		Если ПереоценкаСуммаСценарий Тогда
			РесурсыПереоценки.Вставить("СуммаСценарий", СтруктураКурсыВалют.Сценарий);
		КонецЕсли;
		
	Иначе
		ПереоценкаСуммаСценарий = Ложь;
	КонецЕсли;
	
	Если СтруктураПараметров.Свойство("ПереоценкаСуммаМУ") Тогда
		
		ПереоценкаСуммаМУ = СтруктураПараметров.ПереоценкаСуммаМУ;
		
		Если ПереоценкаСуммаМУ Тогда
			РесурсыПереоценки.Вставить("СуммаМУ", СтруктураКурсыВалют.МУ);
		КонецЕсли;
		
	Иначе
		ПереоценкаСуммаМУ = Ложь;
	КонецЕсли;
	
	Если РесурсыПереоценки.Количество() = 0 Тогда
		// Не указаны ресурсы для переоценки.
		Возврат ТаблицаПереоценки;
	КонецЕсли;
	
	// Процедура удаляет счета с невалютной аналитикой.
	УдалитьСчетаСНевалютнойАналитикой(ИмяПланаСчетов, МассивСчетов);
	
	// Получим таблицу счетов с ресурсами для переоценки.
	// С учетом настроек регистра сведений "Валютные неденежные статьи".
	СуммыПереоценки = Новый Структура;
	СуммыПереоценки.Вставить("ПереоценкаСуммаУпр"	  , ПереоценкаСуммаУпр);
	СуммыПереоценки.Вставить("ПереоценкаСуммаСценарий", ПереоценкаСуммаСценарий);
	СуммыПереоценки.Вставить("ПереоценкаСуммаМУ"	  , ПереоценкаСуммаМУ);
	СуммыПереоценки.Вставить("ПереоценкаСуммаРегл"	  , ПереоценкаСуммаРегл);
	ТаблицаСчетаРесурсы = СчетаСРесурсамиДляПереоценки(ДатаПереоценки, Организация, ИмяПланаСчетов, МассивСчетов, СуммыПереоценки);
	Если ТаблицаСчетаРесурсы.Количество() = 0 Тогда
		// Нет счетов для переоценки.
		Возврат ТаблицаПереоценки;
	КонецЕсли;
	
    ЕстьОрганизацияБюдж = Ложь;
	Если ИмяРегБухгалтерии = "бит_Бюджетирование" Тогда
		Организация_Сценарий = Сценарий;
		ЕстьОрганизацияБюдж = ?(СтруктураПараметров.Свойство("Организация"), Истина, Ложь);
	Иначе
		Организация_Сценарий = Организация;
	КонецЕсли;
	
	ПараметрыПолученияОстатков = Новый Структура;
	ПараметрыПолученияОстатков.Вставить("ДатаПереоценки"      , ДатаПереоценки);
	ПараметрыПолученияОстатков.Вставить("Организация"         , Организация);
	ПараметрыПолученияОстатков.Вставить("ЦФО"                 , СтруктураПараметров.ЦФО);
	ПараметрыПолученияОстатков.Вставить("Сценарий"            , Сценарий);
	ПараметрыПолученияОстатков.Вставить("ИмяРегБухгалтерии"   , ИмяРегБухгалтерии);
	ПараметрыПолученияОстатков.Вставить("ТаблицаСчетаРесурсы" , ТаблицаСчетаРесурсы);
	ПараметрыПолученияОстатков.Вставить("КоличествоСубконто"  , КоличествоСубконто);
	ПараметрыПолученияОстатков.Вставить("ЕстьОрганизацияБюдж" , ЕстьОрганизацияБюдж);
	ПараметрыПолученияОстатков.Вставить("ПоВсемОрганизациям"  , СтруктураПараметров.ПоВсемОрганизациям);
	ПараметрыПолученияОстатков.Вставить("ПоВсемЦФО"           , СтруктураПараметров.ПоВсемЦФО);
	
	// Получим результат запроса остатков по счетам переоценки.
	РезультатОстатков = ПолучитьРезультатЗапросаОстатковПоСчетамДляПереоценки(ПараметрыПолученияОстатков);
	Если РезультатОстатков.Пустой() Тогда
		// Нет остатков для переоценки.
		Возврат ТаблицаПереоценки;
	КонецЕсли;
	
	// Сформируем заполняемые свойства для новых строк переоценки.
	ЗаполняемыеСвойства = "Счет, Организация";
	
	Если ИмяРегБухгалтерии = "бит_Бюджетирование" Тогда
		ЗаполняемыеСвойства = ЗаполняемыеСвойства + ", ЦФО";
	КонецЕсли;
	
	Для Ном = 1 По КоличествоСубконто Цикл
		ЗаполняемыеСвойства = ЗаполняемыеСвойства + ", Субконто" + Ном;
	КонецЦикла;
	
	ЗаполняемыеСвойства = ЗаполняемыеСвойства + ", Валюта, ВалютнаяСумма";
	
	// Добавим колонки результата в таблицу переоценки.
	ДобавитьКолонкиРезультатаВТаблицуПереоценки(ТаблицаПереоценки
											   ,ИмяРегБухгалтерии
											   ,ИмяПланаСчетов
											   ,ИмяПланаВидовХарактеристик
											   ,КоличествоСубконто);
	
	// Получим выборку из запроса по остаткам для выполнения переоценки.
	ВыборкаИзЗапроса = РезультатОстатков.Выбрать();
	
	Пока ВыборкаИзЗапроса.Следующий() Цикл
		
		СтруктураДельтыСумм = Новый Структура;
		
		ВалютаСтроки   = ВыборкаИзЗапроса.Валюта;
		ВалСуммаСтроки = ВыборкаИзЗапроса.ВалютнаяСумма;
		
		КурсКратностьВалютыСтроки = бит_КурсыВалют.ПолучитьКурсВалюты(ВалютаСтроки, ДатаПереоценки);
		КурсВалютыСтроки 		  = КурсКратностьВалютыСтроки.Курс;
		КратностьВалютыСтроки	  = КурсКратностьВалютыСтроки.Кратность;
		
		Для Каждого КлючЗначение Из РесурсыПереоценки Цикл
			
			ТекИмяРесурса  = КлючЗначение.Ключ;
			
			Если Не ВыборкаИзЗапроса["Переоценка" + ТекИмяРесурса] Тогда
				// Для текущего ресурса по текущему счету выполнение переоценки отключено.
				Продолжить;
			КонецЕсли;
			
			ТекКурсыВалюты = КлючЗначение.Значение;
			
			// Расчитаем значение суммы на дату переоценки.
			НоваеЗначениеСуммы = бит_КурсыВалютКлиентСервер.ПересчитатьИзВалютыВВалюту(ВалСуммаСтроки
																		    	,ВалютаСтроки		  , ТекКурсыВалюты.Валюта
																				,КурсВалютыСтроки	  , ТекКурсыВалюты.Курс
																				,КратностьВалютыСтроки, ТекКурсыВалюты.Кратность);
			// Расчитаем дельту сумм.
			ДельтаСумм = (НоваеЗначениеСуммы - ВыборкаИзЗапроса[ТекИмяРесурса]);
			
			Если Не ДельтаСумм = 0 Тогда
				СтруктураДельтыСумм.Вставить(ТекИмяРесурса, ДельтаСумм);
			КонецЕсли;
			
		КонецЦикла; // Для Каждого ТекИмяРесурса Из РесурсыПереоценки Цикл.
		
		// Если есть дельты сумм, тогда
		Если Не СтруктураДельтыСумм.Количество() = 0 Тогда
			
			НоваяСтрока = ТаблицаПереоценки.Добавить();
			ЗаполнитьЗначенияСвойств(НоваяСтрока, ВыборкаИзЗапроса, ЗаполняемыеСвойства);
			
			// Заполним суммы переоценки.
			Для Каждого КлючЗначение Из СтруктураДельтыСумм Цикл
				НоваяСтрока[КлючЗначение.Ключ] = КлючЗначение.Значение;
			КонецЦикла;
			
		КонецЕсли;
		
	КонецЦикла; // Пока ВыборкаИзЗапроса.Следующий() Цикл
	
	Возврат ТаблицаПереоценки;
	
КонецФункции // ДанныеПереоценкиВалютныхДенежныхСтатей()

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

#Область ПроцедурыИФункцииПоПолучениюТаблицыПереоценки

// Процедура удаляет из массива счета с невалютной аналитикой.
// 
// Параметры:
//  ИмяПланаСчетов - Строка.
//  МассивСчетов   - Массив. Возвращаемый.
// 
Процедура УдалитьСчетаСНевалютнойАналитикой(ИмяПланаСчетов, МассивСчетов)
	
	Если Метаданные.ПланыСчетов[ИмяПланаСчетов].ПризнакиУчетаСубконто.Найти("Валютный") = Неопределено Тогда
		// Валютный признак учета по субконто на плане счетов отсутствует.
		Возврат;
	КонецЕсли;
	
	бит_РаботаСКоллекциями.УдалитьПовторяющиесяЭлементыМассива(МассивСчетов);
	
	// Получим валютные счета с невалютной аналитикой.
	Запрос = Новый Запрос;
	Запрос.УстановитьПараметр("МассивСчетов", МассивСчетов);
	
	Запрос.Текст =
	"ВЫБРАТЬ
	|	ПланСчетовВидыСубконто.Ссылка КАК Счет,
	|	ПланСчетовВидыСубконто.ВидСубконто
	|ИЗ
	|	&ИмяПланаСчетовВидыСубконто КАК ПланСчетовВидыСубконто
	|ГДЕ
	|	ПланСчетовВидыСубконто.Ссылка В(&МассивСчетов)
	|	И ПланСчетовВидыСубконто.Ссылка.Валютный
	|	И (НЕ ПланСчетовВидыСубконто.Валютный)
	|ИТОГИ ПО
	|	Счет";
	Запрос.Текст = СтрЗаменить(Запрос.Текст, "&ИмяПланаСчетовВидыСубконто", СтрШаблон("ПланСчетов.%1.ВидыСубконто", ИмяПланаСчетов));
	
	РезультатЗапроса = Запрос.Выполнить();	
	Если РезультатЗапроса.Пустой() Тогда
		// Счета с невалютной аналитикой отсутствуют.
		Возврат;
	КонецЕсли;
	
	// Сообщим пользователю.
	ТекстСообщения = НСтр("ru='Невозможно выполнить переоценку для счетов:'; 
						  |en='It is not possible to perform a revaluation for accounts:'"); 
	ОбщегоНазначения.СообщитьПользователю(ТекстСообщения); 
	
	ВыборкаПоСчетам = РезультатЗапроса.Выбрать(ОбходРезультатаЗапроса.ПоГруппировкам);
	
	Пока ВыборкаПоСчетам.Следующий() Цикл
		
		ТекущийСчет 		   = ВыборкаПоСчетам.Счет;
		ВыборкаПоВидамСубконто = ВыборкаПоСчетам.Выбрать();
		
		Пока ВыборкаПоВидамСубконто.Следующий() Цикл
			ТекстСообщения = НСтр("ru = '- На валютном счете ""%1"" указано невалютное субконто ""%2"".'; 
								  |en = '- The currency account ""%1"" does not contain the currency sub-accounto ""%2"".'");
			ТекстСообщения = СтрШаблон(ТекстСообщения, Строка(ТекущийСчет), Строка(ВыборкаПоВидамСубконто.ВидСубконто));
			ОбщегоНазначения.СообщитьПользователю(ТекстСообщения); 
		КонецЦикла;
		
		ИндексЭлемента = МассивСчетов.Найти(ТекущийСчет);
		
		Если ИндексЭлемента <> Неопределено Тогда
			// Удалим счет из массива.
			МассивСчетов.Удалить(ИндексЭлемента);
		КонецЕсли;
		
	КонецЦикла; // Пока ВыборкаПоСчетам.Следующий() Цикл
	
КонецПроцедуры // УдалитьСчетаСНевалютнойАналитикой()

// АПК:134-выкл Количество параметров более 7 для совместимости с предыдущими версиями библиотеки.

// Функция получает таблицу счето с ресурсами для переоценки.
// 
// Параметры:
//  ДатаПереоценки 	- Дата.
//  Организация 	- СправочникСсылка.Организации.
//  ИмяПланаСчетов 	- Строка.
//  МассивСчетов 	- Массив.
//  СуммыПереоценки - Структура.
// 
// Возвращаемое значение:
//  ТаблицаЗначений таблица счетов с ресурсами для переоценки.
// 
Функция СчетаСРесурсамиДляПереоценки(ДатаПереоценки, Организация, ИмяПланаСчетов, МассивСчетов, СуммыПереоценки)
	
	ПереоценкаСуммаУпр 		= СуммыПереоценки.ПереоценкаСуммаУпр;
	ПереоценкаСуммаСценарий = СуммыПереоценки.ПереоценкаСуммаСценарий;
	ПереоценкаСуммаМУ 		= СуммыПереоценки.ПереоценкаСуммаМУ;
	ПереоценкаСуммаРегл 	= СуммыПереоценки.ПереоценкаСуммаРегл;
		
	лЕстьОбъектыМСФО = бит_ОбщегоНазначения.ЕстьОбъектыМСФО();												
													
	Запрос = Новый Запрос;
	Запрос.УстановитьПараметр("ДатаПереоценки"		   , ДатаПереоценки);
	Запрос.УстановитьПараметр("Организация"			   , Организация);
	Запрос.УстановитьПараметр("ПустаяОрганизация"	   , Справочники.Организации.ПустаяСсылка());
	Запрос.УстановитьПараметр("МассивСчетов"		   , МассивСчетов);
	Запрос.УстановитьПараметр("ПереоценкаСуммаУпр"     , ПереоценкаСуммаУпр);
	Запрос.УстановитьПараметр("ПереоценкаСуммаСценарий", ПереоценкаСуммаСценарий);
	Запрос.УстановитьПараметр("ПереоценкаСуммаМУ"	   , ПереоценкаСуммаМУ);
	Запрос.УстановитьПараметр("ПереоценкаСуммаРегл"	   , ПереоценкаСуммаРегл);
	
	Запрос.Текст = "
	|ВЫБРАТЬ
	|	0 КАК Приоритет,
	|	ВалютныеНеденежныеСтатьи_ДляОрганизации.Счет,		
	|	ВалютныеНеденежныеСтатьи_ДляОрганизации.СуммаСценарий,";
	Если лЕстьОбъектыМСФО Тогда
		Запрос.Текст = Запрос.Текст + "
		|	ВалютныеНеденежныеСтатьи_ДляОрганизации.СуммаМУ,";
	КонецЕсли;
	Запрос.Текст = Запрос.Текст + "
	|	ВалютныеНеденежныеСтатьи_ДляОрганизации.СуммаУпр,
	|	ВалютныеНеденежныеСтатьи_ДляОрганизации.СуммаРегл
	|ПОМЕСТИТЬ ВалютныеНеденежныеСтатьи_ПоПриоритетам
	|ИЗ
	|	РегистрСведений.бит_ВалютныеНеденежныеСтатьи.СрезПоследних(
	|			&ДатаПереоценки,
	|			Организация = &Организация
	|				И Счет В (&МассивСчетов)) КАК ВалютныеНеденежныеСтатьи_ДляОрганизации
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|ВЫБРАТЬ
	|	1,
	|	ВалютныеНеденежныеСтатьи_ПустаяОрганизация.Счет,		
	|	ВалютныеНеденежныеСтатьи_ПустаяОрганизация.СуммаСценарий,";
	Если лЕстьОбъектыМСФО Тогда
		Запрос.Текст = Запрос.Текст + "
		|	ВалютныеНеденежныеСтатьи_ПустаяОрганизация.СуммаМУ,";
	КонецЕсли;
	Запрос.Текст = Запрос.Текст + "
	|	ВалютныеНеденежныеСтатьи_ПустаяОрганизация.СуммаУпр,
	|	ВалютныеНеденежныеСтатьи_ПустаяОрганизация.СуммаРегл
	|ИЗ
	|	РегистрСведений.бит_ВалютныеНеденежныеСтатьи.СрезПоследних(
	|			&ДатаПереоценки,
	|			Организация = &ПустаяОрганизация
	|				И Счет В (&МассивСчетов)) КАК ВалютныеНеденежныеСтатьи_ПустаяОрганизация
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	МИНИМУМ(ВалютныеНеденежныеСтатьи_ПоПриоритетам.Приоритет) КАК Приоритет,
	|	ВалютныеНеденежныеСтатьи_ПоПриоритетам.Счет
	|ПОМЕСТИТЬ ВалютныеНеденежныеСтатьи_Группированные
	|ИЗ
	|	ВалютныеНеденежныеСтатьи_ПоПриоритетам КАК ВалютныеНеденежныеСтатьи_ПоПриоритетам
	|
	|СГРУППИРОВАТЬ ПО
	|	ВалютныеНеденежныеСтатьи_ПоПриоритетам.Счет
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	ПланСчетов.Ссылка КАК Счет,
	|	ВЫБОР 
	|		КОГДА ПланСчетов.Ссылка = ВалютныеНеденежныеСтатьи_ПоПриоритетам.Счет
	|			ТОГДА 
	|				ВЫБОР 
	|					КОГДА &ПереоценкаСуммаСценарий И (НЕ ВалютныеНеденежныеСтатьи_ПоПриоритетам.СуммаСценарий)
	|						ТОГДА ИСТИНА
	|					ИНАЧЕ ЛОЖЬ
	|				КОНЕЦ
	|		ИНАЧЕ &ПереоценкаСуммаСценарий
	|	КОНЕЦ КАК ПереоценкаСуммаСценарий,";
	Если лЕстьОбъектыМСФО Тогда
		Запрос.Текст = Запрос.Текст + "
		|	ВЫБОР
		|		КОГДА ПланСчетов.Ссылка = ВалютныеНеденежныеСтатьи_ПоПриоритетам.Счет
		|			ТОГДА 
		|				ВЫБОР
		|					КОГДА &ПереоценкаСуммаМУ И (НЕ ВалютныеНеденежныеСтатьи_ПоПриоритетам.СуммаМУ)
		|						ТОГДА ИСТИНА
		|					ИНАЧЕ ЛОЖЬ
		|				КОНЕЦ
		|		ИНАЧЕ &ПереоценкаСуммаМУ
		|	КОНЕЦ КАК ПереоценкаСуммаМУ,";
	КонецЕсли;
	Запрос.Текст = Запрос.Текст + "
	|	ВЫБОР 
	|		КОГДА ПланСчетов.Ссылка = ВалютныеНеденежныеСтатьи_ПоПриоритетам.Счет
	|			ТОГДА 
	|				ВЫБОР 
	|					КОГДА &ПереоценкаСуммаУпр И (НЕ ВалютныеНеденежныеСтатьи_ПоПриоритетам.СуммаУпр)
	|						ТОГДА ИСТИНА
	|					ИНАЧЕ ЛОЖЬ
	|				КОНЕЦ
	|		ИНАЧЕ &ПереоценкаСуммаУпр
	|	КОНЕЦ КАК ПереоценкаСуммаУпр,
	|	ВЫБОР 
	|		КОГДА ПланСчетов.Ссылка = ВалютныеНеденежныеСтатьи_ПоПриоритетам.Счет
	|			ТОГДА 
	|				ВЫБОР 
	|					КОГДА &ПереоценкаСуммаРегл И (НЕ ВалютныеНеденежныеСтатьи_ПоПриоритетам.СуммаРегл)
	|						ТОГДА ИСТИНА
	|					ИНАЧЕ ЛОЖЬ
	|				КОНЕЦ
	|		ИНАЧЕ &ПереоценкаСуммаРегл
	|	КОНЕЦ КАК ПереоценкаСуммаРегл
	|ПОМЕСТИТЬ ТаблицаСчетаРесурсы
	|ИЗ
	|	&ИмяПланаСчетов КАК ПланСчетов
	|		ЛЕВОЕ СОЕДИНЕНИЕ ВалютныеНеденежныеСтатьи_Группированные КАК ВалютныеНеденежныеСтатьи_Группированные
	|			ЛЕВОЕ СОЕДИНЕНИЕ ВалютныеНеденежныеСтатьи_ПоПриоритетам КАК ВалютныеНеденежныеСтатьи_ПоПриоритетам
	|			ПО ВалютныеНеденежныеСтатьи_Группированные.Счет = ВалютныеНеденежныеСтатьи_ПоПриоритетам.Счет
	|				И ВалютныеНеденежныеСтатьи_Группированные.Приоритет = ВалютныеНеденежныеСтатьи_ПоПриоритетам.Приоритет
	|		ПО ПланСчетов.Ссылка = ВалютныеНеденежныеСтатьи_Группированные.Счет
	|ГДЕ
	|	ПланСчетов.Ссылка В (&МассивСчетов)
	|			И ПланСчетов.Валютный
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	ТаблицаСчетаРесурсы.Счет,		
	|	ТаблицаСчетаРесурсы.ПереоценкаСуммаСценарий,";
	Запрос.Текст = СтрЗаменить(Запрос.Текст, "&ИмяПланаСчетов", СтрШаблон("ПланСчетов.%1", ИмяПланаСчетов));
	Если лЕстьОбъектыМСФО Тогда
		Запрос.Текст = Запрос.Текст + "
		|	ТаблицаСчетаРесурсы.ПереоценкаСуммаМУ,";
	КонецЕсли;
	Запрос.Текст = Запрос.Текст + "
	|	ТаблицаСчетаРесурсы.ПереоценкаСуммаУпр,
	|	ТаблицаСчетаРесурсы.ПереоценкаСуммаРегл
	|ИЗ
	|	ТаблицаСчетаРесурсы КАК ТаблицаСчетаРесурсы
	|ГДЕ
	|	ВЫБОР
	|			КОГДА ТаблицаСчетаРесурсы.ПереоценкаСуммаУпр
	|					ИЛИ ТаблицаСчетаРесурсы.ПереоценкаСуммаСценарий
	|					ИЛИ ТаблицаСчетаРесурсы.ПереоценкаСуммаРегл";
	Если лЕстьОбъектыМСФО Тогда
		Запрос.Текст = Запрос.Текст + "
		|					ИЛИ ТаблицаСчетаРесурсы.ПереоценкаСуммаМУ";
	КонецЕсли;
	Запрос.Текст = Запрос.Текст + "
	|				ТОГДА ИСТИНА
	|			ИНАЧЕ ЛОЖЬ
	|		КОНЕЦ
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|УНИЧТОЖИТЬ ВалютныеНеденежныеСтатьи_ПоПриоритетам
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|УНИЧТОЖИТЬ ВалютныеНеденежныеСтатьи_Группированные
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|УНИЧТОЖИТЬ ТаблицаСчетаРесурсы
	|";
	
	РезультатЗапроса = Запрос.Выполнить();	
	Если РезультатЗапроса.Пустой() Тогда
		// Нет счетов для переоценки.
		ТаблицаСчетаРесурсы = Новый ТаблицаЗначений;
	Иначе
		ТаблицаСчетаРесурсы = РезультатЗапроса.Выгрузить();
	КонецЕсли;
	
	Возврат ТаблицаСчетаРесурсы;
	
КонецФункции

// Функция получает результат запроса остатков по счетам для переоценки.
// 
// Параметры:
//	ПараметрыПолученияОстатков - Структура.
//		Обязательные ключи:
//  		ДатаПереоценки 		 - Дата.
//  		Организация - СправочникСсылка.Организации
//  		Сценарий - СправочникСсылка.СценарииПланирования.
//  		ИмяРегБухгалтерии	 - Строка.
//  		ТаблицаСчетаРесурсы	 - ТаблицаЗначений.
//  		КоличествоСубконто	 - Число.
//  		ЕстьОрганизацияБюдж - Булево.
// 
// Возвращаемое значение:
//  РезультатЗапроса - РезультатЗапроса.
// 
Функция ПолучитьРезультатЗапросаОстатковПоСчетамДляПереоценки(ПараметрыПолученияОстатков)
	
	ТекстЗапроса =
	"ВЫБРАТЬ
	|	ТаблицаСчетаРесурсы.Счет,
	|	ТаблицаСчетаРесурсы.ПереоценкаСуммаСценарий,
	|	&ТаблицаСчетаРесурсыПереоценкаСуммаМУ КАК ПереоценкаСуммаМУ,
	|	ТаблицаСчетаРесурсы.ПереоценкаСуммаУпр,
	|	ТаблицаСчетаРесурсы.ПереоценкаСуммаРегл
	|ПОМЕСТИТЬ ТаблицаСчетаРесурсы
	|ИЗ
	|	&ТаблицаСчетаРесурсы КАК ТаблицаСчетаРесурсы
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	&ТекстДопПоля,
	|	&ТекстСубконто,
	|	&ТекстДопРесурсы,
	|	ТаблицаОстатков.Организация,
	|	ТаблицаОстатков.Счет,
	|	ТаблицаОстатков.Валюта,
	|	СУММА(ЕСТЬNULL(ТаблицаОстатков.ВалютнаяСуммаОстаток, 0)) КАК ВалютнаяСумма,
	|	СУММА(ЕСТЬNULL(ТаблицаОстатков.СуммаРеглОстаток, 0)) КАК СуммаРегл,
	|	СУММА(ЕСТЬNULL(ТаблицаОстатков.СуммаУпрОстаток, 0)) КАК СуммаУпр,
	|	ТаблицаСчетаРесурсы.ПереоценкаСуммаСценарий,
	|	ТаблицаСчетаРесурсы.ПереоценкаСуммаМУ,
	|	ТаблицаСчетаРесурсы.ПереоценкаСуммаУпр,
	|	ТаблицаСчетаРесурсы.ПереоценкаСуммаРегл
	|ИЗ
	|	// Регистр бухгалтреии заменяется ниже в зависимости от обрабатываемого
	|	РегистрБухгалтерии.бит_Бюджетирование.Остатки(
	|			&ГраницаПереоценки,
	|			Счет В
	|				(ВЫБРАТЬ
	|					ТаблицаСчетаРесурсы.Счет
	|				ИЗ
	|					ТаблицаСчетаРесурсы КАК ТаблицаСчетаРесурсы),
	|			,
	|			&ТекстУсловие) КАК ТаблицаОстатков
	|		ЛЕВОЕ СОЕДИНЕНИЕ ТаблицаСчетаРесурсы КАК ТаблицаСчетаРесурсы
	|		ПО ТаблицаОстатков.Счет = ТаблицаСчетаРесурсы.Счет
	|
	|СГРУППИРОВАТЬ ПО
	|	&ТекстДопПоля,
	|	&ТекстСубконто,
	|	ТаблицаОстатков.Организация,
	|	ТаблицаОстатков.Счет,
	|	ТаблицаОстатков.Валюта,	
	|	ТаблицаСчетаРесурсы.ПереоценкаСуммаСценарий,
	|	ТаблицаСчетаРесурсы.ПереоценкаСуммаМУ,
	|	ТаблицаСчетаРесурсы.ПереоценкаСуммаУпр,
	|	ТаблицаСчетаРесурсы.ПереоценкаСуммаРегл";
	
	ИмяРегБухгалтерии = ПараметрыПолученияОстатков.ИмяРегБухгалтерии;
	ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "бит_Бюджетирование", ПараметрыПолученияОстатков.ИмяРегБухгалтерии);
	
	Если бит_ОбщегоНазначения.ЕстьОбъектыМСФО() Тогда
		ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "&ТаблицаСчетаРесурсыПереоценкаСуммаМУ",
						"ТаблицаСчетаРесурсы.ПереоценкаСуммаМУ");
	Иначе
		ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "&ТаблицаСчетаРесурсыПереоценкаСуммаМУ",
						"ЛОЖЬ");
	КонецЕсли;
	
	ТекстДопПоля = "";
	Если ИмяРегБухгалтерии = "бит_Бюджетирование" Тогда
		ТекстДопПоля 	= "ТаблицаОстатков.ЦФО,";
	КонецЕсли;
	ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "&ТекстДопПоля,", ТекстДопПоля);
	
	ТекстСубконто = "";
	Для НомерСубконтно = 1 По ПараметрыПолученияОстатков.КоличествоСубконто Цикл
		Если НомерСубконтно > 1 Тогда
			ТекстСубконто = ТекстСубконто + "
			|	";
		КонецЕсли;
		
		ТекстСубконто = ТекстСубконто + "ТаблицаОстатков.Субконто" + НомерСубконтно + ",";
	КонецЦикла;
	ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "&ТекстСубконто,", ТекстСубконто);
	
	ТекстДопРесурсы = "";
	Если ИмяРегБухгалтерии = "бит_Бюджетирование" Тогда
		ТекстДопРесурсы = "СУММА(ЕСТЬNULL(ТаблицаОстатков.СуммаСценарийОстаток, 0)) КАК СуммаСценарий,";
	ИначеЕсли бит_му_ОбщегоНазначения.ЭтоМеждународныйПланСчетовИлиРегистр(ИмяРегБухгалтерии) Тогда
		ТекстДопРесурсы = "СУММА(ЕСТЬNULL(ТаблицаОстатков.СуммаМУОстаток, 0)) КАК СуммаМУ,";
	КонецЕсли;
	ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "&ТекстДопРесурсы,", ТекстДопРесурсы);
	
	ТекстУсловие = "";
	Если НЕ ПараметрыПолученияОстатков.ПоВсемОрганизациям
		И (ИмяРегБухгалтерии <> "бит_Бюджетирование"
			ИЛИ ПараметрыПолученияОстатков.ЕстьОрганизацияБюдж) Тогда
		ДобавитьУсловиеЗапроса(ТекстУсловие, "Организация");
	КонецЕсли;
	Если ИмяРегБухгалтерии = "бит_Бюджетирование" Тогда
		ДобавитьУсловиеЗапроса(ТекстУсловие, "Сценарий");
		Если НЕ ПараметрыПолученияОстатков.ПоВсемЦФО Тогда
			ДобавитьУсловиеЗапроса(ТекстУсловие, "ЦФО");
		КонецЕсли;
	КонецЕсли;
	ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "&ТекстУсловие", ТекстУсловие);
	
	
	Запрос = Новый Запрос;
	ДатаПереоценки = ПараметрыПолученияОстатков.ДатаПереоценки;
	Запрос.УстановитьПараметр("ГраницаПереоценки"   , Новый Граница(ДатаПереоценки, ВидГраницы.Включая));
	Запрос.УстановитьПараметр("Организация", ПараметрыПолученияОстатков.Организация);
	Запрос.УстановитьПараметр("ЦФО", ПараметрыПолученияОстатков.ЦФО);
	Запрос.УстановитьПараметр("Сценарий", ПараметрыПолученияОстатков.Сценарий);	
	Запрос.УстановитьПараметр("ТаблицаСчетаРесурсы" , ПараметрыПолученияОстатков.ТаблицаСчетаРесурсы);
	
	Запрос.Текст = ТекстЗапроса;
	
	РезультатЗапроса = Запрос.Выполнить();
	
	Возврат РезультатЗапроса;
	
КонецФункции

Процедура ДобавитьУсловиеЗапроса(ТекстУсловие, ПолеОтбора, ПараметрОтбора = Неопределено)
	
	Если ПараметрОтбора = Неопределено Тогда
		ПараметрОтбора = ПолеОтбора;
	КонецЕсли;
	
	Если НЕ ПустаяСтрока(ТекстУсловие) Тогда
		ТекстУсловие = ТекстУсловие + "
						|	И ";
	КонецЕсли;
	
	ТекстУсловие = ТекстУсловие + ПолеОтбора + " = &" + ПараметрОтбора;
	
КонецПроцедуры

// Процедура добавляет колонки результата в таблицу переоценки.
// 
// Параметры:
//  ТаблицаПереоценки 		   - ТаблицаЗначений. Возвращаемый.
//  ИмяРегБухгалтерии 		   - Строка.
//  ИмяПланаСчетов 			   - Строка.
//  ИмяПланаВидовХарактеристик - Строка.
//  КоличествоСубконто 		   - Число.
// 
Процедура ДобавитьКолонкиРезультатаВТаблицуПереоценки(ТаблицаПереоценки
													 ,ИмяРегБухгалтерии
													 ,ИмяПланаСчетов
													 ,ИмяПланаВидовХарактеристик
													 ,КоличествоСубконто)
	
	// Получим массив типов субконто.
	МассивТипов 	= Новый Массив;
	ВыборкаСубконто = ПланыВидовХарактеристик[ИмяПланаВидовХарактеристик].Выбрать();
	
	Пока ВыборкаСубконто.Следующий() Цикл
		
		ТекТипыСубконто = ВыборкаСубконто.ТипЗначения.Типы();
		
		Для Каждого ТекТип Из ТекТипыСубконто Цикл
			МассивТипов.Добавить(ТекТип);
		КонецЦикла; 
		
	КонецЦикла;
	
	ОписаниеСубконто = Новый ОписаниеТипов(МассивТипов);
	ОписаниеЧисло	 = Новый ОписаниеТипов("Число", Новый КвалификаторыЧисла(15, 2));
	
	// Добавим колонки результата в таблицу переоценки.
	Если ИмяРегБухгалтерии = "бит_Бюджетирование" Тогда
		
		ИмяСправочникаЦФО = бит_ОбщегоНазначения.ПолучитьИмяСправочникаЦФО();
		ТаблицаПереоценки.Колонки.Добавить("ЦФО", Новый ОписаниеТипов("СправочникСсылка." + ИмяСправочникаЦФО));
		
	КонецЕсли;
	
	ТаблицаПереоценки.Колонки.Добавить("Счет", Новый ОписаниеТипов("ПланСчетовСсылка." + ИмяПланаСчетов));
	
	// Добавляем колонки для субконто.
	Для Ном = 1 По КоличествоСубконто Цикл
		ТаблицаПереоценки.Колонки.Добавить("Субконто" + Ном, ОписаниеСубконто);
	КонецЦикла;
	
	ТаблицаПереоценки.Колонки.Добавить("Организация"  , Новый ОписаниеТипов("СправочникСсылка.Организации"));
	ТаблицаПереоценки.Колонки.Добавить("Валюта"		  , Новый ОписаниеТипов("СправочникСсылка.Валюты"));
	ТаблицаПереоценки.Колонки.Добавить("ВалютнаяСумма", ОписаниеЧисло);
	ТаблицаПереоценки.Колонки.Добавить("СуммаРегл"	  , ОписаниеЧисло);
	ТаблицаПереоценки.Колонки.Добавить("СуммаУпр"	  , ОписаниеЧисло);
	
	Если ИмяРегБухгалтерии = "бит_Бюджетирование" Тогда
		ТаблицаПереоценки.Колонки.Добавить("СуммаСценарий", ОписаниеЧисло);
	ИначеЕсли бит_му_ОбщегоНазначения.ЭтоМеждународныйПланСчетовИлиРегистр(ИмяРегБухгалтерии) Тогда
	
		ТаблицаПереоценки.Колонки.Добавить("СуммаМУ", ОписаниеЧисло);
		
	КонецЕсли;
	
КонецПроцедуры // ДобавитьКолонкиРезультатаВТаблицуПереоценки()

#КонецОбласти

#Область БухгалтерскиеОтчеты 

Процедура НайтиГруппировкуПоПолю(Группировки, ИмяПоля, НайденнаяГруппировка)
	
	Для каждого Группировка Из Группировки Цикл
		Если ТипЗнч(Группировка) = Тип("ТаблицаКомпоновкиДанных") Тогда
			
			НайтиГруппировкуПоПолю(Группировка.Строки, ИмяПоля, НайденнаяГруппировка);
			Если НайденнаяГруппировка <> Неопределено Тогда
				Возврат;
			КонецЕсли;
			НайтиГруппировкуПоПолю(Группировка.Колонки, ИмяПоля, НайденнаяГруппировка);
			Если НайденнаяГруппировка <> Неопределено Тогда
				Возврат;
			КонецЕсли; 
			
		ИначеЕсли ТипЗнч(Группировка) = Тип("ГруппировкаКомпоновкиДанных") 
			ИЛИ ТипЗнч(Группировка) = Тип("ГруппировкаТаблицыКомпоновкиДанных") Тогда
			
			Если Группировка.ПоляГруппировки.Элементы.Количество() 
				И Группировка.ПоляГруппировки.Элементы[0].Поле = Новый ПолеКомпоновкиДанных(ИмяПоля) Тогда
				НайденнаяГруппировка = Группировка;
				Возврат;
			КонецЕсли;
			
			НайтиГруппировкуПоПолю(Группировка.Структура, ИмяПоля, НайденнаяГруппировка);
			
			Если НайденнаяГруппировка <> Неопределено Тогда
				Возврат;
			КонецЕсли;
		КонецЕсли;
	КонецЦикла; 
	
КонецПроцедуры

Функция НайтиГруппировку(КоллекцияЭлементов, ИмяЭлемента) Экспорт
	
	Результат = Неопределено;
	Для Каждого Элемент Из КоллекцияЭлементов Цикл
		Если Элемент.Имя = ИмяЭлемента Тогда
			Результат = Элемент;
			Прервать;
		ИначеЕсли ТипЗнч(Элемент) = Тип("ТаблицаКомпоновкиДанных") Тогда
			Результат = НайтиГруппировку(Элемент.Строки, ИмяЭлемента);
			Если Результат = Неопределено Тогда
				Результат = НайтиГруппировку(Элемент.Колонки, ИмяЭлемента);	
			КонецЕсли;
			Прервать;
		Иначе
			Результат = НайтиГруппировку(Элемент.Структура, ИмяЭлемента);
		КонецЕсли;
	КонецЦикла;
	Возврат Результат;
	
КонецФункции

Процедура ПереместитьГруппировку(ГруппировкаИсточник, ГруппировкаПриемник, ВключаяПодчиненные = Истина)
	
	ГруппировкаПриемник.Использование 	= ГруппировкаИсточник.Использование; 
	ГруппировкаПриемник.Имя 			= ГруппировкаИсточник.Имя; 
	
	Для каждого Элемент Из ГруппировкаИсточник.Выбор.Элементы Цикл 
		ДобавитьПоляВСтруктуруОтчета(ГруппировкаПриемник.Выбор.Элементы,Элемент) 
	КонецЦикла; 
		
	Для каждого Элемент Из ГруппировкаИсточник.Отбор.Элементы Цикл 
		ДобавитьОтборВСтруктуруОтчета(ГруппировкаПриемник.Отбор.Элементы,Элемент); 
	КонецЦикла; 
	
	Для каждого Элемент Из ГруппировкаИсточник.ПараметрыВывода.Элементы Цикл 
		ДобавитьПараметрыВыводаВСтруктуруОтчета(ГруппировкаПриемник.ПараметрыВывода.Элементы,Элемент); 
	КонецЦикла; 
	
	Для каждого Элемент Из ГруппировкаИсточник.ПоляГруппировки.Элементы Цикл 
		
		Если ТипЗнч(Элемент) = Тип("ПолеГруппировкиКомпоновкиДанных") Тогда 
			
			НовЭлемент = ГруппировкаПриемник.ПоляГруппировки.Элементы.Добавить(ТипЗнч(Элемент)); 
			ЗаполнитьЗначенияСвойств(НовЭлемент, Элемент, 
					"Использование,Поле,ТипГруппировки,ТипДополнения,НачалоПериода,КонецПериода");
			
		ИначеЕсли ТипЗнч(Элемент) = Тип("АвтоВыбранноеПолеКомпоновкиДанных") Тогда 
			
			НовЭлемент = ГруппировкаПриемник.ПоляГруппировки.Элементы.Добавить(ТипЗнч(Элемент)); 
			НовЭлемент.Использование = Элемент.Использование; 
			
		КонецЕсли; 
		
	КонецЦикла; 
	
	Для каждого Элемент Из ГруппировкаИсточник.Порядок.Элементы Цикл 
		ДобавитьПорядокВСтруктуруОтчета(ГруппировкаПриемник.Порядок.Элементы,Элемент); 
	КонецЦикла; 
	
	Для каждого Элемент Из ГруппировкаИсточник.УсловноеОформление.Элементы Цикл 
		ДобавитьУсловноеОформлениеВСтруктуруОтчета(ГруппировкаПриемник.УсловноеОформление.Элементы,Элемент); 
	КонецЦикла; 
	
	Если ВключаяПодчиненные Тогда
		Для каждого СтруктураНижнегоУровня Из ГруппировкаИсточник.Структура Цикл 
			
			Если ТипЗнч(ГруппировкаПриемник) = Тип("ГруппировкаТаблицыКомпоновкиДанных") Тогда
				НоваяГруппировка = ГруппировкаПриемник.Структура.Добавить();
			Иначе
				НоваяГруппировка = ГруппировкаПриемник.Структура.Добавить(Тип("ГруппировкаКомпоновкиДанных"));
			КонецЕсли;
			
			ПереместитьГруппировку(СтруктураНижнегоУровня, НоваяГруппировка) 
		КонецЦикла; 
	КонецЕсли;
	
КонецПроцедуры

Процедура УстановитьОбщиеПараметрыСтандартныхОтчетов(КомпоновщикНастроек, ВалютыУпр)
	
	бит_ОтчетыБФКлиентСервер.УстановитьПараметр(КомпоновщикНастроек, "ВалютаУпр", ВалютыУпр.ВалютаУпр);
	бит_ОтчетыБФКлиентСервер.УстановитьПараметр(КомпоновщикНастроек, "ВалютаРегл", ВалютыУпр.ВалютаРегл);
	бит_ОтчетыБФКлиентСервер.УстановитьПараметр(КомпоновщикНастроек, "СтрокаОбщиеИтоги", НСтр("ru = 'Общие итоги';
																									|en = 'Grand totals'"));

	ПараметрКонецПериодаГраница = бит_ОтчетыБФКлиентСервер.ПолучитьПараметр(КомпоновщикНастроек, "КонецПериодаГраница");
	Если ПараметрКонецПериодаГраница <> Неопределено Тогда
		ПараметрКонецПериодаГраница.Использование = Истина;
		ПараметрПериод = бит_ОтчетыБФКлиентСервер.ПолучитьПараметр(КомпоновщикНастроек, "ПериодОтчета");
		Если ПараметрПериод <> Неопределено Тогда
			ПериодЗначение = ПараметрПериод.Значение;// СтандартныйПериод - 
			Если ПараметрПериод.Использование И ЗначениеЗаполнено(ПериодЗначение.ДатаОкончания) Тогда  
				ПараметрКонецПериодаГраница.Значение = Новый Граница(КонецДня(ПериодЗначение.ДатаОкончания), ВидГраницы.Включая); 
			Иначе
				ПараметрКонецПериодаГраница.Значение = Дата(1,1,1)
			КонецЕсли;	
		КонецЕсли;
	КонецЕсли;	
	
	МассивВидовСубконто = Новый Массив;
	ПараметрВидыСубконто = бит_ОтчетыБФКлиентСервер.ПолучитьПараметр(КомпоновщикНастроек, "СписокВидовСубконто");
	Если ПараметрВидыСубконто <> Неопределено И ЗначениеЗаполнено(ПараметрВидыСубконто.Значение) Тогда
		Если ТипЗнч(ПараметрВидыСубконто.Значение) = Тип("СписокЗначений") Тогда
			СписокВидовСубконто = ПараметрВидыСубконто.Значение;
			Для Каждого ЭлементСписка Из СписокВидовСубконто Цикл
				Если ЗначениеЗаполнено(ЭлементСписка.Значение) Тогда 
					МассивВидовСубконто.Добавить(ЭлементСписка.Значение);
				КонецЕсли;
			КонецЦикла;
		Иначе
			МассивВидовСубконто.Добавить(ПараметрВидыСубконто.Значение);
		КонецЕсли;
		бит_ОтчетыБФКлиентСервер.УстановитьПараметр(КомпоновщикНастроек,"СписокВидовСубконто", МассивВидовСубконто);
	КонецЕсли;
	
	// Разделитель строк текста:
	бит_ОтчетыБФКлиентСервер.УстановитьПараметр(КомпоновщикНастроек, "ПС", Символы.ПС);

КонецПроцедуры

Процедура УстановитьГруппировкуВалюты(КомпоновщикНастроек, Включить)

	ГруппировкаВалюты = НайтиГруппировку(КомпоновщикНастроек.Настройки.Структура, "Валюта");
	
	Если ГруппировкаВалюты = Неопределено Тогда
		ГруппировкаВалюты = НайтиГруппировку(КомпоновщикНастроек.Настройки.Структура, "ВалютаБезЗаголовка");
		Если ГруппировкаВалюты <> Неопределено Тогда
			ГруппировкаВалюты.Имя = "Валюта";
		КонецЕсли;
	КонецЕсли;
	
	Если ГруппировкаВалюты <> Неопределено Тогда
		ГруппировкаРодитель = ГруппировкаВалюты.Родитель;
		
		Если ТипЗнч(ГруппировкаРодитель) = Тип("ГруппировкаКомпоновкиДанных")
		И ГруппировкаРодитель.Имя = "НеВыводитьЗаголовок" Тогда
			ГруппировкаВалюты.Имя = "ВалютаБезЗаголовка";
		КонецЕсли;
		
		Если ГруппировкаВалюты.Использование <> Включить Тогда
			ГруппировкаВалюты.Использование = Включить;
			Если НЕ Включить Тогда
				Для каждого ПодчиненнаяГруппировка Из ГруппировкаВалюты.Структура Цикл
					Если ТипЗнч(ГруппировкаРодитель) = Тип("ГруппировкаТаблицыКомпоновкиДанных") Тогда
						НоваяГруппировка = ГруппировкаРодитель.Структура.Добавить();
					Иначе
						НоваяГруппировка = ГруппировкаРодитель.Структура.Добавить(Тип("ГруппировкаКомпоновкиДанных"));
					КонецЕсли;
					ПереместитьГруппировку(ПодчиненнаяГруппировка, НоваяГруппировка);
				КонецЦикла;	
			КонецЕсли;
		КонецЕсли;
		
		Ид = КомпоновщикНастроек.ПользовательскиеНастройки.ПолучитьИдентификаторПоОбъекту(ГруппировкаВалюты);
		ПользовательскаяГруппировка = КомпоновщикНастроек.ПользовательскиеНастройки.ПолучитьОбъектПоИдентификатору(Ид);
		
		Если ПользовательскаяГруппировка <> Неопределено
		   И ПользовательскаяГруппировка.Использование <> Включить Тогда
			ПользовательскаяГруппировка.Использование = Включить;
			КомпоновщикНастроек.ПользовательскиеНастройки.ДополнительныеСвойства.Вставить("ПользовательскиеНастройкиМодифицированы", Истина);
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры

Процедура УстановитьВалюту(ДокументРезультат, Валюта, ПредставлениеВалюты)
	
	Область = ДокументРезультат.НайтиТекст(ПредставлениеВалюты + ":");
	СтрокаПоиска = НСтр("ru=': Да';en=': Yes'");
	Если СтрНайти(Область.Текст, СтрокаПоиска) > 0 Тогда
		Область.Текст = ПредставлениеВалюты + ": " + Строка(Валюта);
	Иначе
		УдаляемаяСтрока = Лев(Область.Имя,Найти(Область.Имя,"C")-1);
		УдаляемаяОбласть = ДокументРезультат.Область(УдаляемаяСтрока);
		ДокументРезультат.УдалитьОбласть(УдаляемаяОбласть, ТипСмещенияТабличногоДокумента.ПоВертикали);
		ДокументРезультат.ФиксацияСверху = ДокументРезультат.ФиксацияСверху - 1;
	КонецЕсли;
	
КонецПроцедуры

Процедура УдалитьЗаголовкиПолей(Документ)
		
	Область = Документ.НайтиТекст("Функц.");
	Если Область <> Неопределено Тогда
		Область.Текст = "";
	КонецЕсли;
	
	ОбъединитьЗаголовкиПолей(Документ, НСтр("ru = 'Показатели';
											|en = 'Indicators'"));
		
КонецПроцедуры

Процедура ОбъединитьЗаголовкиПолей(Документ, ТекстОбласти) Экспорт
	
	Область = Документ.Область("R1C1");
	
	Пока Область <> Неопределено Цикл
		Область = Документ.НайтиТекст(ТекстОбласти,Область,,,Истина);
		Если Область <> Неопределено И Документ.ФиксацияСверху <> 0 Тогда
			Колонка = "C"+Строка(Область.Лево);
			ИмяОбласти = "R"+Строка(Область.Верх)+Колонка+":R"+Строка(Документ.ФиксацияСверху)+Колонка;
			ШапкаВалюты = Документ.Область(ИмяОбласти);
			ШапкаВалюты.Объединить();
		КонецЕсли;
	КонецЦикла;
	
КонецПроцедуры

Процедура ПоказателиОтчетаСУчетомТипаИтога(ТипИтога, НачальноеСальдо, ПоказателиОтчета)
	
	ИменаПолей = Новый Массив;
	Если ТипИтога = Перечисления.ТипыИтогов.СальдоДт 
		Или ТипИтога = Перечисления.ТипыИтогов.СальдоКт 
		Или ТипИтога = Перечисления.ТипыИтогов.Сальдо Тогда
		Если НачальноеСальдо Тогда
			ИменаПолей.Добавить("Оборот");
			ИменаПолей.Добавить("КонечныйОстаток");
		Иначе
			ИменаПолей.Добавить("НачальныйОстаток");
			ИменаПолей.Добавить("Оборот");
		КонецЕсли;
	КонецЕсли;
	
	Если ТипИтога = Перечисления.ТипыИтогов.ОборотДт Тогда
		ИменаПолей.Добавить("НачальныйОстаток");
		ИменаПолей.Добавить("ОборотКт");
		ИменаПолей.Добавить("КонечныйОстаток");
	КонецЕсли;	
	Если ТипИтога = Перечисления.ТипыИтогов.ОборотКт Тогда
		ИменаПолей.Добавить("НачальныйОстаток");
		ИменаПолей.Добавить("ОборотДт");
		ИменаПолей.Добавить("КонечныйОстаток");
	КонецЕсли;
	Если ТипИтога = Перечисления.ТипыИтогов.Оборот Тогда
		ИменаПолей.Добавить("НачальныйОстаток");
		ИменаПолей.Добавить("КонечныйОстаток");
	КонецЕсли;
	
	НовыеПоказателиОтчета = Новый Структура;
	
	Для каждого ПоказательОтчета Из ПоказателиОтчета Цикл
		Если ПоказательОтчета.Значение.Значение Тогда
			Для каждого ИмяПоля Из ИменаПолей Цикл
				НовыеПоказателиОтчета.Вставить(ПоказательОтчета.Ключ + ИмяПоля, Новый Структура("Значение", Ложь));
			КонецЦикла;
		КонецЕсли; 
		НовыеПоказателиОтчета.Вставить(ПоказательОтчета.Ключ, ПоказательОтчета.Значение);
	КонецЦикла;
	
	ПоказателиОтчета = НовыеПоказателиОтчета;
		
КонецПроцедуры

Процедура НастроитьПоляВыбора(ПоказателиОтчета, ЭлементОтчета)
	
	Если ТипЗнч(ЭлементОтчета) = Тип("ТаблицаКомпоновкиДанных") Тогда
		
		Для каждого КолонкаТаблицыГруппировки Из ЭлементОтчета.Колонки Цикл
			НастроитьПоляВыбора(ПоказателиОтчета, КолонкаТаблицыГруппировки);
		КонецЦикла;
		Возврат;
		
	ИначеЕсли ТипЗнч(ЭлементОтчета) = Тип("ГруппировкаТаблицыКомпоновкиДанных") Тогда
		
		Для каждого ПодчиненнаяГруппировка Из ЭлементОтчета.Структура Цикл
			НастроитьПоляВыбора(ПоказателиОтчета, ПодчиненнаяГруппировка);
		КонецЦикла;
		НастроитьПоляВыбора(ПоказателиОтчета,ЭлементОтчета.Выбор.Элементы);
		
	ИначеЕсли ТипЗнч(ЭлементОтчета) = Тип("НастройкиКомпоновкиДанных") Тогда
		
		НастроитьПоляВыбора(ПоказателиОтчета,ЭлементОтчета.Выбор.Элементы);
		
	Иначе
		
		Для Каждого ПолеВыбора Из ЭлементОтчета Цикл	
			
			Если ТипЗнч(ПолеВыбора) = Тип("АвтоВыбранноеПолеКомпоновкиДанных") Тогда
				Продолжить;
			КонецЕсли;
			
			ИмяПоля = Строка(ПолеВыбора.Поле);
			Если ТипЗнч(ПолеВыбора) = Тип("ГруппаВыбранныхПолейКомпоновкиДанных") Тогда
				НастроитьПоляВыбора(ПоказателиОтчета, ПолеВыбора.Элементы);
			Иначе
				Для Каждого Показатель Из ПоказателиОтчета Цикл
					Если СтрНайти(ИмяПоля, Показатель.Ключ) > 0 Тогда
						ПолеВыбора.Использование = Показатель.Значение.Значение;
						Прервать;
					КонецЕсли;
				КонецЦикла;
			КонецЕсли; 
		КонецЦикла;
	КонецЕсли;
	
КонецПроцедуры

Процедура НастроитьОтборПоСуммам(ЭлементыОтчета, ГруппаОтбора)
	
	Для Каждого ЭлементОтчета Из ЭлементыОтчета Цикл
		ИмяПоля = Строка(ЭлементОтчета.Поле);
		Если ТипЗнч(ЭлементОтчета) = Тип("ГруппаВыбранныхПолейКомпоновкиДанных") Тогда
			НастроитьОтборПоСуммам(ЭлементОтчета.Элементы, ГруппаОтбора);
		Иначе
			Если (СтрНайти(ИмяПоля, "Оборот") > 0 ИЛИ СтрНайти(ИмяПоля, "Сальдо") > 0) И ЭлементОтчета.Использование Тогда
				НовыйОтбор = ГруппаОтбора.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
				НовыйОтбор.ЛевоеЗначение = ЭлементОтчета.Поле;
				НовыйОтбор.ВидСравнения = ВидСравненияКомпоновкиДанных.НеРавно;
				НовыйОтбор.ПравоеЗначение = 0;
			КонецЕсли;
		КонецЕсли;		
	КонецЦикла;
	
КонецПроцедуры

Процедура СформироватьЗаголовкиПолей(КлючВарианта, КомпоновщикНастроек, СхемаКомпоновкиДанных, Заголовок)
	
	НаборДанных = СхемаКомпоновкиДанных.НаборыДанных.Найти("ОсновнойНаборДанных");
	Если НаборДанных = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	Для Каждого ПолеНабораДанных Из НаборДанных.Поля Цикл
		Если ТипЗнч(ПолеНабораДанных) = Тип("ПолеНабораДанныхСхемыКомпоновкиДанных") Тогда		
			Если СтрНайти(ПолеНабораДанных.Поле,"ПоказателиОтчета") = 0 
				И (СтрНайти(ПолеНабораДанных.Поле,"ВалютнаяСумма") > 0 
				ИЛИ СтрНайти(ПолеНабораДанных.Поле,"СуммаРегл") > 0 
				ИЛИ СтрНайти(ПолеНабораДанных.Поле,"СуммаУпр") > 0
				ИЛИ СтрНайти(ПолеНабораДанных.Поле,"Количество") > 0) Тогда 
				
				ПолеНабораДанных.Заголовок = Заголовок;
				
			КонецЕсли;
		КонецЕсли;
	КонецЦикла;
	
КонецПроцедуры

Процедура ПослеКомпоновкиРезультата(ДокументРезультат, ПоказателиОтчета, КлючВарианта)
	
	Если ПоказателиОтчета.ВалютнаяСумма.Значение И КлючВарианта = "АнализСчетаУправленческий" Тогда
		Область = ДокументРезультат.НайтиТекст("Кор. счет");
		Если Область <> Неопределено Тогда
			Область.Текст = Область.Текст + "," + Символы.ПС + "Валюта";
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры

Процедура ДобавитьОтборВСтруктуруОтчета(СтруктураПриемник, Элемент)
	
	Если ТипЗнч(Элемент) = Тип("ЭлементОтбораКомпоновкиДанных") Тогда 
		
		НовЭлемент = СтруктураПриемник.Добавить(ТипЗнч(Элемент));
		ЗаполнитьЗначенияСвойств(НовЭлемент, Элемент, 
			"ВидСравнения,Использование,ЛевоеЗначение,ПравоеЗначение,Представление,Применение,РежимОтображения");
		
	ИначеЕсли ТипЗнч(Элемент) = Тип("ГруппаЭлементовОтбораКомпоновкиДанных") Тогда 
		
		НовЭлемент = СтруктураПриемник.Добавить(ТипЗнч(Элемент)); 
		ЗаполнитьЗначенияСвойств(НовЭлемент, Элемент, 
			"Использование,Представление,Применение,РежимОтображения,ТипГруппы");

		Для каждого ЭлементОтбора Из Элемент.Элементы Цикл 
			ДобавитьОтборВСтруктуруОтчета(НовЭлемент.Элементы, ЭлементОтбора); 
		КонецЦикла; 
		
	КонецЕсли; 
	
КонецПроцедуры 

Процедура ДобавитьУсловноеОформлениеВСтруктуруОтчета(СтруктураПриемник,Элемент) 
	
	НовЭлемент = СтруктураПриемник.Добавить(); 
	НовЭлемент.Представление = Элемент.Представление; 
	НовЭлемент.Использование = Элемент.Использование; 
	 
	Для каждого ЭлементОтбор Из Элемент.Отбор.Элементы Цикл 
		ДобавитьОтборВСтруктуруОтчета(НовЭлемент.Отбор.Элементы,ЭлементОтбор); 
	КонецЦикла; 
	 
	Для каждого ЭлементОформление Из Элемент.Оформление.Элементы Цикл 
		ДобавитьПараметрыВыводаВСтруктуруОтчета(НовЭлемент.Оформление.Элементы,ЭлементОформление); 
	КонецЦикла; 
	 
	Для каждого ЭлементПоле Из Элемент.Поля.Элементы Цикл 
		НовЭлементПоле = НовЭлемент.Поля.Элементы.Добавить(); 
		НовЭлементПоле.Использование 	= ЭлементПоле.Использование; 
		НовЭлементПоле.Поле 			= ЭлементПоле.Поле; 
	КонецЦикла; 
	
КонецПроцедуры 

Процедура ДобавитьПараметрыВыводаВСтруктуруОтчета(СтруктураПриемник,Элемент) 
	
	НовЭлемент = СтруктураПриемник.Найти(Строка(Элемент.Параметр)); 
	НовЭлемент.Значение = Элемент.Значение; 
	НовЭлемент.Использование = Элемент.Использование; 
	Для каждого Элемент Из Элемент.ЗначенияВложенныхПараметров Цикл 
		ДобавитьПараметрыВыводаВСтруктуруОтчета(НовЭлемент.ЗначенияВложенныхПараметров,Элемент); 
	КонецЦикла; 
	
КонецПроцедуры 

Процедура ДобавитьПоляВСтруктуруОтчета(СтруктураПриемник, Элемент)
	
	Если ТипЗнч(Элемент) = Тип("ВыбранноеПолеКомпоновкиДанных") Тогда
		
		НовЭлемент = СтруктураПриемник.Добавить(ТипЗнч(Элемент));
		ЗаполнитьЗначенияСвойств(НовЭлемент, Элемент, "Заголовок,Использование,Поле");
		
	ИначеЕсли ТипЗнч(Элемент) = Тип("АвтоВыбранноеПолеКомпоновкиДанных") Тогда
		
		НовЭлемент = СтруктураПриемник.Добавить(ТипЗнч(Элемент));
		НовЭлемент.Использование = Элемент.Использование;
		
	ИначеЕсли ТипЗнч(Элемент) = Тип("ГруппаВыбранныхПолейКомпоновкиДанных") Тогда
		
		НовЭлемент = СтруктураПриемник.Добавить(ТипЗнч(Элемент));
		ЗаполнитьЗначенияСвойств(НовЭлемент, Элемент, "Заголовок,Использование,Расположение,Поле");
		Для каждого ЭлементСтруктуры Из Элемент.Элементы Цикл
			ДобавитьПоляВСтруктуруОтчета(НовЭлемент.Элементы, ЭлементСтруктуры);
		КонецЦикла;
		
	КонецЕсли;
	
КонецПроцедуры 

Процедура ДобавитьПорядокВСтруктуруОтчета(СтруктураПриемник,Элемент) 
	
	Если ТипЗнч(Элемент) = Тип("ЭлементПорядкаКомпоновкиДанных") Тогда 
		
		НовЭлемент = СтруктураПриемник.Добавить(ТипЗнч(Элемент)); 
		ЗаполнитьЗначенияСвойств(НовЭлемент, Элемент, "Использование,Поле,ТипУпорядочивания");
		 
	ИначеЕсли ТипЗнч(Элемент) = Тип("АвтоЭлементПорядкаКомпоновкиДанных") Тогда 
		
		НовЭлемент = СтруктураПриемник.Добавить(ТипЗнч(Элемент)); 
		НовЭлемент.Использование = Элемент.Использование; 
		
	КонецЕсли; 
	
КонецПроцедуры

Функция ВалютыУпр()

	ВалютыУпр = Новый Структура; 
	ВалютыУпр.Вставить("ВалютаУпр", Константы.бит_ВалютаУправленческогоУчета.Получить());
	ВалютыУпр.Вставить("ВалютаРегл",Константы.ВалютаРегламентированногоУчета.Получить());
	
	Возврат ВалютыУпр;

КонецФункции

// АПК:142-выкл Количество необязательных параметров более 3 для совместимости с предыдущими версиями библиотеки.

// Копирует элементы отбора из одной коллекции отбора в другую.
//
// Параметры:
//  Источник - ОтборКомпоновкиДанных - откуда копировать.
//  Приемник - ОтборКомпоновкиДанных - куда копировать.
//  ТолькоИспользуемые - Булево - копировать только используемые.
//  ПоляИсключения - Структура - в ключах структуры перечислены поля которые не должны попасть в отбор приемник.
//  Постфикс - Строка - произвольная строка которая будет добавлена к имени левого поля.
//  ПараметрыПроверкиДоступностиПолей - Структура - Параметры проверки доступности полей. 
//													Если необходимо проверять доступность полей.
//   *ПроверятьДоступностьПолей - Булево. Проверять доступность полей отбора при копировании. По умолчанию Ложь.
//   *ДоступныеПоляОтбора - ТаблицаЗначений - Кэш доступных полей для проверки при иерархическом копировании отбора.
//     **ПолеКомпоновки - ПолеКомпоновки  - Поле компоновки данных.
//     **ИмяПоля        - Строка          - Имя поля для проверок через точку.
//
Процедура СкопироватьОтборРекурсивно(ОтборИсточник, ОтборПриемник, ТолькоИспользуемые = Ложь, ПоляИсключения = Неопределено, Постфикс = "", ПараметрыПроверкиДоступностиПолей = Неопределено)
	
	ПроверятьДоступностьПолей = ПараметрыПроверкиДоступностиПолей.ПроверятьДоступностьПолей;
	ДоступныеПоляОтбора = ПараметрыПроверкиДоступностиПолей.ДоступныеПоляОтбора;
	
	Для Каждого Элемент Из ОтборИсточник.Элементы Цикл
		
		Если НЕ (ТолькоИспользуемые И Элемент.Использование ИЛИ НЕ ТолькоИспользуемые) Тогда
			Продолжить;
		КонецЕсли;
		
		Если ТипЗнч(Элемент) = Тип("ГруппаЭлементовОтбораКомпоновкиДанных") Тогда
			НовыйЭлемент = НовыйОтбор(ОтборПриемник, Неопределено,,Тип("ГруппаЭлементовОтбораКомпоновкиДанных"));
			ЗаполнитьЗначенияСвойств(НовыйЭлемент, Элемент);
			СкопироватьОтборРекурсивно(Элемент, НовыйЭлемент, ТолькоИспользуемые, ПоляИсключения, Постфикс, ПараметрыПроверкиДоступностиПолей);
		Иначе
			
			Если Не ЗначениеЗаполнено(Элемент.ЛевоеЗначение) Тогда
				Продолжить;
			КонецЕсли;
			
			Если ЗначениеЗаполнено(ПоляИсключения) Тогда
				Поле = бит_ОтчетыБФКлиентСервер.РазделитьИмяПоляИРеквизита(Строка(Элемент.ЛевоеЗначение));
				Если ПоляИсключения.Свойство(Поле.Имя) Тогда
					Продолжить;
				КонецЕсли;
			КонецЕсли;
			
			Если ПроверятьДоступностьПолей Тогда
				ПолеДоступно = Ложь;
				Если НЕ ДоступныеПоляОтбора.Найти(Элемент.ЛевоеЗначение, "ПолеКомпоновки") = Неопределено Тогда
					ПолеДоступно = Истина;
				КонецЕсли;
				Если НЕ ПолеДоступно Тогда
					ИмяПоляИРеквизит = бит_ОтчетыБФКлиентСервер.РазделитьИмяПоляИРеквизита(Строка(Элемент.ЛевоеЗначение));
					Если НЕ ДоступныеПоляОтбора.Найти(ИмяПоляИРеквизит.Имя, "ИмяПоля") = Неопределено Тогда
						ПолеДоступно = Истина;
					КонецЕсли;
				КонецЕсли;
				Если НЕ ПолеДоступно Тогда
					Продолжить;
				КонецЕсли;
			КонецЕсли;
			
			НовыйЭлемент = НовыйОтбор(ОтборПриемник, Неопределено,,Тип("ЭлементОтбораКомпоновкиДанных"));
			ЗаполнитьЗначенияСвойств(НовыйЭлемент, Элемент);
			
			Если Постфикс <> "" Тогда
				Поле = бит_ОтчетыБФКлиентСервер.РазделитьИмяПоляИРеквизита(Строка(НовыйЭлемент.ЛевоеЗначение));
				НовоеИмяПоля = Поле.Имя + Постфикс + "." + Поле.Реквизит;
				Если СтрНайти(Поле.Имя, "Субконто") И СтрНайти("ДТКТ", ВРег(Постфикс))Тогда
					Номер = Прав(Поле.Имя, 1);
					Если СтрНайти("12345", Номер) Тогда
						Шаблон = СтрЗаменить(Поле.Имя, Номер, "%1");
						НовоеИмяПоля = СтрШаблон(Шаблон, Постфикс + Номер) + "." + Поле.Реквизит;
					КонецЕсли;
				КонецЕсли;
				НовыйЭлемент.ЛевоеЗначение = Новый ПолеКомпоновкиДанных(НовоеИмяПоля);
			КонецЕсли;
			
		КонецЕсли;
		
	КонецЦикла;
	
КонецПроцедуры // АПК:142-вкл

Процедура ЗаполнитьДоступныеПоляРекурсивно(ДоступныеПоля, ЭлементыДоступныхПолей)
	Для каждого ЭлементДоступныхПолей Из ЭлементыДоступныхПолей Цикл
		Если ЭлементДоступныхПолей.Папка Тогда
			ЗаполнитьДоступныеПоляРекурсивно(ДоступныеПоля, ЭлементДоступныхПолей.Элементы);
		Иначе
			НоваяСтрока = ДоступныеПоля.Добавить();
			НоваяСтрока.ПолеКомпоновки = ЭлементДоступныхПолей.Поле;
			НоваяСтрока.ИмяПоля = Строка(ЭлементДоступныхПолей.Поле);
		КонецЕсли;
	КонецЦикла;
КонецПроцедуры

// АПК:142-выкл Количество необязательных параметров более 3 для совместимости с предыдущими версиями библиотеки.

// Возвращает ссылку на новый элемент отбора в переданном отборе схемы компоновки данных.
//
// Параметры:
//  Отбор - ОтборКомпоновкиДанных - отбор схемы компоновки, в который необходимо добавить новый элемент
//  ЛевоеЗначение - Строка, ПолеКомпоновкиДанных - поле компоновки, по которому необходимо установить отбор (необязательное)
//  ПравоеЗначение  - Произвольный, Строка, ПолеКомпоновкиДанных - значение отбора (необязательное) или поле компоновки
//  ТипОтбора  - ЭлементОтбораКомпоновкиДанных, ГруппаЭлементовОтбораКомпоновкиДанных - тип нового элемента отбора (необязательное)
//  ВидСравнения  - ВидСравненияКомпоновкиДанных - вид сравнения левого и правого значения
//  ПравоеЗначениеПоле  - Булево - указывает использовать ли в качестве правого значения поле компоновки данных.
//
// Возвращаемое значение:
//  ЭлементОтбораКомпоновкиДанных - ссылка на новый элемент отбора схемы компоновки данных.
//
Функция НовыйОтбор(Отбор, ЛевоеЗначение, ПравоеЗначение = Неопределено, ТипОтбора = Неопределено, ВидСравнения = Неопределено, ПравоеЗначениеПоле = Ложь) 
	
	Если ТипОтбора = Неопределено Тогда
		ТипОтбора = Тип("ЭлементОтбораКомпоновкиДанных");
	КонецЕсли;
	
	НовыйОтбор = Отбор.Элементы.Добавить(ТипОтбора);
	Если ЛевоеЗначение <> Неопределено Тогда
		Если ТипЗнч(ЛевоеЗначение) = Тип("ПолеКомпоновкиДанных") Тогда
			НовыйОтбор.ЛевоеЗначение = ЛевоеЗначение;
		Иначе
			НовыйОтбор.ЛевоеЗначение = Новый ПолеКомпоновкиДанных(ЛевоеЗначение);
		КонецЕсли;
	КонецЕсли;
	
	Если ПравоеЗначение <> Неопределено Тогда
		Если ПравоеЗначениеПоле Тогда
			Если ТипЗнч(ПравоеЗначение) = Тип("ПолеКомпоновкиДанных") Тогда
				НовыйОтбор.ПравоеЗначение = ПравоеЗначение;
			Иначе
				НовыйОтбор.ПравоеЗначение = Новый ПолеКомпоновкиДанных(ПравоеЗначение);
			КонецЕсли;
		Иначе
			НовыйОтбор.ПравоеЗначение = ПравоеЗначение;
		КонецЕсли;
	КонецЕсли;
	
	Если ТипОтбора = Тип("ЭлементОтбораКомпоновкиДанных") Тогда
		Если ВидСравнения = Неопределено Тогда
			НовыйОтбор.ВидСравнения = ВидСравненияКомпоновкиДанных.Равно;
			Если ТипЗнч(ПравоеЗначение) = Тип("Массив") ИЛИ ТипЗнч(ПравоеЗначение) = Тип("СписокЗначений") Тогда
				НовыйОтбор.ВидСравнения = ВидСравненияКомпоновкиДанных.ВСписке;
			КонецЕсли;
		Иначе
			НовыйОтбор.ВидСравнения = ВидСравнения;
		КонецЕсли;
	КонецЕсли;
	
	Возврат НовыйОтбор;
	
КонецФункции // АПК:142-вкл

#КонецОбласти 

Функция РегистраторУстановленПравильно(Источник, Приемник, Регистратор)

	Отказ = Ложь;
	Если НЕ Регистратор.Движения.Содержит(Приемник) Тогда
	
		ИсточникПредставление 	 = Источник.Синоним;
		ПриемникПредставление 	 = Приемник.Синоним;
		РегистраторПредставление = Регистратор.Синоним;
		
		ТекстСообщения = НСтр("ru='Ошибка переноса проводок из регистра ""%1"" в регистр ""%2"". Документ ""%3"" не является регистратором регистра накопления ""%2""';
							  |en='Posting transfer error from the register ""%1"" to the register ""%2"". Document ""%3"" is not registrar accumulation register ""%2""'"); 
		ТекстСообщения = СтрШаблон(ТекстСообщения, ИсточникПредставление, ПриемникПредставление, РегистраторПредставление); 
		ОбщегоНазначения.СообщитьПользователю(ТекстСообщения,,,,Отказ);

	КонецЕсли; 

	Возврат НЕ Отказ;
	
КонецФункции

// Функция готовит структуру параметров трансляции для выполнения подбора типов проводок по 
// правилам трансляции.
// 
// Параметры:
//  Организация  - СправочникСсылка.Организация.
//  ДатаПравил   - Дата.
// 
// Возвращаемое значение:
//   СтруктураПараметров   - Структура.
// 
Функция ПодготовитьСтруктуруПараметровДляПодбораТиповПроводок(Организация,Регистратор,МетаРегистрИсточник,МетаРегистрПриемник)

	РегистрИсточник = бит_ПраваДоступа.ПолучитьОбъектДоступаПоМетаданным(МетаРегистрИсточник);	
	РегистрПриемник = бит_ПраваДоступа.ПолучитьОбъектДоступаПоМетаданным(МетаРегистрПриемник);
	ДатаПравил = Регистратор.Дата;
	Назначения = Новый Структура;
	Назначения.Вставить("ПодборТиповПроводок", Перечисления.бит_НазначенияПравилТрансляции.ПодборТиповПроводок);
	
	МассивПравил    = бит_МеханизмТрансляцииПовтИсп.ПолучитьПравилаТрансляцииПоНазначению(Организация
		                                                                                   ,ДатаПравил
		                                                                                   ,Назначения
																						   ,РегистрИсточник
																						   ,РегистрПриемник);
	
	СтруктураПараметров = бит_МеханизмТрансляции.КонструкторСтруктурыПараметровТрансляции();														
														
	СтруктураПараметров.ОбязательныеПоля  = Новый Структура;
	СтруктураПараметров.ПравилоЗаполнения = Справочники.бит_ПравилаЗаполненияПолейПриТрансляции.СоздатьЭлемент();
	СтруктураПараметров.ПравилоЗаполнения.КодПередФормированиемЗаписи = "
	        |бит_УправленческийУчет.ЗаполнитьЗаписьРегистраТипыПроводок(ЗаписьИсточник, ЗаписьПриемник, СтруктураПараметров.ОбъектСистемыИсточник, СтруктураПараметров.СтрПарЗапись);
			|";
			
	// Если ТипПроводки был изменен трансляцие, необходимо установить такое же значение и в РБ.Источнике.
	СтруктураПараметров.ПравилоЗаполнения.КодПослеФормированияЗаписи = "
	        |Если ЗаписьПриемник.ТипПроводки <> ЗаписьИсточник.ТипПроводки Тогда
			|  ЗаписьИсточник.ТипПроводки = ЗаписьПриемник.ТипПроводки;
			|КонецЕсли;
			|";
			
	СтруктураПараметров.ПравилаТрансляции = МассивПравил;
	СтруктураПараметров.СтруктураШапкиДокумента = Новый Структура;
	СтруктураПараметров.ПользовательскиеФункции = Новый Соответствие;
	СтруктураПараметров.ДополнительныеАналитики = Новый Соответствие;
	СтруктураПараметров.ВидыРегистров           = бит_МеханизмТрансляции.КонструкторСтруктурыВидыРегистров(Истина,Ложь,Ложь,Истина);
	СтруктураПараметров.ОбъектСистемыИсточник   = РегистрИсточник;
	СтруктураПараметров.ОбъектСистемыПриемник   = РегистрПриемник;
	СтруктураПараметров.МетаданныеРегистратора  = Регистратор.Метаданные();
	СтруктураПараметров.МетаданныеИсточника     = МетаРегистрИсточник;
	СтруктураПараметров.МетаданныеПриемника     = МетаРегистрПриемник;
	СтруктураПараметров.ЗаписыватьДвижения      = Ложь;
	СтруктураПараметров.ВестиПротокол           = Истина;
	СтруктураПараметров.ОчиститьПриемник        = Ложь;	
	
	Возврат СтруктураПараметров;

КонецФункции // ПодготовитьСтруктуруПараметровДляПодбораТиповПроводок()

#КонецОбласти