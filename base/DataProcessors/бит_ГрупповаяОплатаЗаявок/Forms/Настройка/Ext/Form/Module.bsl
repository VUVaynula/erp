
#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	СписокСвойств = "ОбъединятьНазначениеПлатежа, РежимЗаписиРасчетыЧерезБанк, РежимЗаписиРасчетыЧерезКассу";
	ЗаполнитьЗначенияСвойств(ЭтотОбъект, Параметры, СписокСвойств);
	
	Если НЕ ЗначениеЗаполнено(РежимЗаписиРасчетыЧерезБанк) Тогда
		РежимЗаписиРасчетыЧерезБанк = Перечисления.бит_РежимыЗаписиДокумента.Запись;
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(РежимЗаписиРасчетыЧерезКассу) Тогда
		РежимЗаписиРасчетыЧерезКассу = Перечисления.бит_РежимыЗаписиДокумента.Запись;
	КонецЕсли;

	Элементы.ГруппаРасчетыЧерезКассу.Видимость = Ложь; // Пока не поддерживаем.
	
КонецПроцедуры

&НаКлиенте
Процедура ПередЗакрытием(Отказ, ЗавершениеРаботы, ТекстПредупреждения, СтандартнаяОбработка)
	
	ПрограммноеЗакрытие = Не Модифицированность;
	
	ТекстПредупреждения = НСтр("ru = 'Настройки были изменены. Закрыть без сохранения?';
								|en = 'Settings were changed. Close without saving?'");
	
	ОписаниеОповещения = Новый ОписаниеОповещения("ПриОтказеОтЗакрытияФормы", ЭтотОбъект); 
	
	ОбщегоНазначенияКлиент.ПоказатьПодтверждениеЗакрытияПроизвольнойФормы(ЭтотОбъект,
		Отказ, ЗавершениеРаботы, ТекстПредупреждения, "ПрограммноеЗакрытие", ОписаниеОповещения);
	
КонецПроцедуры
	
// Обработчик завершения открытия формы.
//  См. Синтакс-помощник: ОткрытьФорму().
//	
&НаКлиенте
Процедура ПриОтказеОтЗакрытияФормы(Результат, ВходящийКонтекст) Экспорт
	
	Модифицированность = Ложь;

КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура Готово(Команда)
	
	Модифицированность = Ложь;
	
	ПараметрыПередачи = Новый Структура; 
	ПараметрыПередачи.Вставить("ОбъединятьНазначениеПлатежа", ОбъединятьНазначениеПлатежа);
	ПараметрыПередачи.Вставить("РежимЗаписиРасчетыЧерезБанк", РежимЗаписиРасчетыЧерезБанк);
	ПараметрыПередачи.Вставить("РежимЗаписиРасчетыЧерезКассу",РежимЗаписиРасчетыЧерезКассу);
	
	Закрыть(ПараметрыПередачи);
	
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

//Код процедур и функций

#КонецОбласти