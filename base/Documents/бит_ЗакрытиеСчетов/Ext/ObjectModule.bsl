#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ОбработчикиСобытий

Процедура ПередЗаписью(Отказ, РежимЗаписи, РежимПроведения)

	Если ОбменДанными.Загрузка Тогда
		Возврат;			
	КонецЕсли; 
	
	бит_ОбщегоНазначенияПереопределяемый.ПередЗаписьюДокумента(ЭтотОбъект, РежимЗаписи, РежимПроведения);
	
КонецПроцедуры // ПередЗаписью()
	
Процедура ПриЗаписи(Отказ)
		
	Если ОбменДанными.Загрузка Тогда
		Возврат;			
	КонецЕсли; 
		
КонецПроцедуры // ПриЗаписи()

Процедура ОбработкаПроведения(Отказ, РежимПроведения)
		
	бит_ОбщегоНазначенияПереопределяемый.ОбработкаПроведенияНачало(ЭтотОбъект);
	Если РучнаяКорректировка Тогда
		Возврат;
	КонецЕсли;
	
	ПараметрыПроведения = Документы.бит_ЗакрытиеСчетов.ПодготовитьПараметрыПроведения(Ссылка, Отказ);
	
	Документы.бит_ЗакрытиеСчетов.СформироватьПроводки(ПараметрыПроведения.ЗакрытиеСчетовТаблица,
		ПараметрыПроведения.ЗакрытиеСчетов, ПараметрыПроведения.НастройкаЗакрытия, Движения, Отказ);
		
	Документы.бит_ЗакрытиеСчетов.СформироватьПроводки(ПараметрыПроведения.СторноЗакрытиеСчетовТаблица,
		ПараметрыПроведения.СторноЗакрытиеСчетов, ПараметрыПроведения.НастройкаЗакрытия, Движения, Отказ);	
		
	бит_ОбщегоНазначенияПереопределяемый.ОбработкаПроведенияЗавершение(ЭтотОбъект, Отказ); 
	
КонецПроцедуры

Процедура ОбработкаУдаленияПроведения(Отказ)
	
	бит_ОбщегоНазначенияПереопределяемый.ОбработкаУдаленияПроведения(ЭтотОбъект, Отказ);
	
КонецПроцедуры

Процедура ОбработкаПроверкиЗаполнения(Отказ, ПроверяемыеРеквизиты)

	НепроверяемыеРеквизиты = Новый Массив; 
	
	Если НЕ Сторно Тогда
		НепроверяемыеРеквизиты.Добавить("ДатаСторно");	
	КонецЕсли;
	
	Если ЗначениеЗаполнено(РегистрБухгалтерии) Тогда
		ИмяРегистра = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(РегистрБухгалтерии, "ИмяОбъекта");
		МетаданныеРегистра = Метаданные.РегистрыБухгалтерии.Найти(ИмяРегистра);

		Если МетаданныеРегистра = Неопределено Тогда
			ВызватьИсключение СтрШаблон(НСтр("ru = 'Не найден регистр бухгалтерии ""%1"".'"), РегистрБухгалтерии) ;
		КонецЕсли;
		
		Если МетаданныеРегистра.Измерения.Найти("Сценарий") = Неопределено Тогда
			НепроверяемыеРеквизиты.Добавить("Сценарий");		
		КонецЕсли;
		Если МетаданныеРегистра = Метаданные.РегистрыБухгалтерии.бит_Бюджетирование Тогда
			НепроверяемыеРеквизиты.Добавить("Организация");
		КонецЕсли;
		Если МетаданныеРегистра.Измерения.Найти("ЦФО") = Неопределено Тогда
			НепроверяемыеРеквизиты.Добавить("Закрытие.ЦФО");		
		КонецЕсли;
	КонецЕсли;
	
	НастройкаИменаРесурсов = НастройкаЗакрытия.ИменаРесурсов.Выгрузить();	
	ДокументИменаРесурсов  = Документы.бит_ЗакрытиеСчетов.ИменаРесурсов();
	
	ОбщегоНазначения.УдалитьНепроверяемыеРеквизитыИзМассива(ПроверяемыеРеквизиты, НепроверяемыеРеквизиты);
	
КонецПроцедуры

Процедура ОбработкаЗаполнения(ДанныеЗаполнения, ТекстЗаполнения, СтандартнаяОбработка)
	
	Если ТипЗнч(ДанныеЗаполнения) = Тип("СправочникСсылка.бит_НастройкиЗакрытия") Тогда
		ЗаполнитьПоНастройкеЗакрытия(ДанныеЗаполнения, СтандартнаяОбработка);
	ИначеЕсли ТипЗнч(ДанныеЗаполнения) = Тип("Структура") Тогда
		ЗаполнитьЗначенияСвойств(ЭтотОбъект, ДанныеЗаполнения);
	КонецЕсли;

	Если НЕ ЗначениеЗаполнено(ДатаСторно) Тогда
		ДатаСторно = ТекущаяДатаСеанса();
	КонецЕсли;
	
	Ответственный = Пользователи.ТекущийПользователь();	
	
КонецПроцедуры

#КонецОбласти

#Область СлужебныйПрограммныйИнтерфейс

// Процедура заполняет табличную часть "Закрытие".
// 
Процедура ЗаполнитьТчЗакрытие(ПараметрыРБ) Экспорт
	
	НайденоПолеСумма = Истина;
	Для каждого Строка Из НастройкаЗакрытия.Закрытие Цикл
		
		Если Строка.ВидЗакрытия = Перечисления.бит_ВидыЗакрытия.Простое Тогда
			Продолжить;
		КонецЕсли; 
		
		ТаблицаБаза   = Строка.База.Поля;
		ТаблицаДанные = Строка.Данные.Поля;
		ЕстьБаза 	  = Ложь;
		Для каждого ЭлБаза Из ТаблицаБаза Цикл 		
			Если ВРег(ЭлБаза.Синоним) = ВРег("Сумма") Тогда 				
				ЕстьБаза = Истина;
				Прервать;
			КонецЕсли; 
		КонецЦикла; 
		
		ЕстьДанные = Ложь;
		Для каждого ЭлДанные Из ТаблицаДанные Цикл 		
			Если ВРег(ЭлДанные.Синоним) = ВРег("Сумма") Тогда				
				ЕстьДанные = Истина;
				Прервать;
			КонецЕсли; 
		КонецЦикла; 
		
		Если НЕ ЕстьБаза ИЛИ НЕ ЕстьДанные Тогда
		
			НайденоПолеСумма = Ложь;
			Настройка = ?(НЕ ЕстьБаза, "База", "Данные");
			ТекстСообщения = СтрШаблон(НСтр("ru = 'Список ""%1"" не содержит колонку ""Сумма"". Заполнение не возможно. Проверьте настройку ""%2"" строка №%3.'; 
											|en = 'The ""%1""list does not contain the ""Amount"" column. Filling is not possible. Check the setting ""%2"" line №%3.'"), 
						   					Настройка, НастройкаЗакрытия, Формат(Строка.НомерСтроки, "ЧГ="));
			ОбщегоНазначения.СообщитьПользователю(ТекстСообщения, ЭтотОбъект, НастройкаЗакрытия, "Объект"); 
			Прервать;
		КонецЕсли; 
	КонецЦикла; 
	
	Если НЕ НайденоПолеСумма Тогда 	
		Возврат;		
	КонецЕсли; 
	
	СтруктураПараметров = Новый Структура;
	СтруктураПараметров.Вставить("Организация", Организация);
	Если ПараметрыРБ.ЭтоРегистрБюдж Тогда		
		СтруктураПараметров.Вставить("Сценарий", Сценарий);		
	КонецЕсли; 
	СтруктураПараметров.Вставить("Дата"              , Дата);
	СтруктураПараметров.Вставить("НастройкаЗакрытия" , НастройкаЗакрытия);
	СтруктураПараметров.Вставить("КоличествоСубконто", ПараметрыРБ.МинКоличествоСубконто);
	СтруктураПараметров.Вставить("ЕстьПодразделение" , ПараметрыРБ.ЕстьПодразделение);
	
	// Заполним закрытие по настройке закрытия.
	бит_РегламентныеЗакрытия.ЗаполнитьДанныеЗакрытия(Закрытие, СтруктураПараметров);	 		

КонецПроцедуры // ЗаполнитьТчЗакрытие()

// Процедура заполняет документ.
// 
// Параметры:
//  СтруктураЗаполнения - Структура.
//
Процедура ЗаполнитьДокумент(СтруктураЗаполнения) Экспорт

	// Обязательные для заполнения поля: Дата, ВидЗакрытия, НастройкаЗакрытия, Сценарий (бюджетирование).
	Для каждого КлЗнч Из СтруктураЗаполнения Цикл
		ИмяРеквизита = КлЗнч.Ключ;
		Если ИмяРеквизита = "Дата" ИЛИ Метаданные().Реквизиты.Найти(ИмяРеквизита) <> Неопределено Тогда
			ЭтотОбъект[ИмяРеквизита] = КлЗнч.Значение;
		КонецЕсли; 	
	КонецЦикла;
	
	Если ЗначениеЗаполнено(ВидЗакрытия) И Не ЗначениеЗаполнено(РегистрБухгалтерии) Тогда
		РегистрБухгалтерии = ВидЗакрытия.РегистрБухгалтерии;
	КонецЕсли;
	
	// Заполняет табличную часть "Закрытие"
	Если ЗначениеЗаполнено(РегистрБухгалтерии) Тогда
		ПараметрыРБ = бит_РегламентныеЗакрытия.ПараметрыРегистраБухгалтерии(РегистрБухгалтерии);
		ЗаполнитьТчЗакрытие(ПараметрыРБ);	
	КонецЕсли;

КонецПроцедуры // ЗаполнитьДокумент()

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

Процедура ЗаполнитьПоНастройкеЗакрытия(ДанныеЗаполнения, СтандартнаяОбработка)
	
	Организация = бит_ОбщиеПеременныеСервер.ЗначениеПеременной("ОсновнаяОрганизация");
	ВидЗакрытия = бит_РегламентныеЗакрытия.ПолучитьВидЗакрытия(Организация, ДанныеЗаполнения, ТекущаяДатаСеанса());
	
	Если ЗначениеЗаполнено(ВидЗакрытия) Тогда
		РегистрБухгалтерии = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(ДанныеЗаполнения, "РегистрБухгалтерии");
		НастройкаЗакрытия  = ДанныеЗаполнения;	
						
		Если бит_РегламентныеЗакрытия.ЭтоРегистрБюджетирования(ОбщегоНазначения.ЗначениеРеквизитаОбъекта(РегистрБухгалтерии, "ИмяОбъекта")) Тогда
			Сценарий = бит_УправлениеПользователямиСервер.ПолучитьЗначениеПоУмолчанию("ОсновнойСценарий",
			                                                Пользователи.ТекущийПользователь());
		КонецЕсли;
	КонецЕсли;
		
КонецПроцедуры

#КонецОбласти

#Иначе
  ВызватьИсключение НСтр("ru='Недопустимый вызов объекта на клиенте.';en='Invalid object call on client'");
#КонецЕсли