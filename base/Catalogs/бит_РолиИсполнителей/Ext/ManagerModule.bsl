#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область СлужебныйПрограммныйИнтерфейс
	
// Функция возвращает настройки объектов адресации.
// 
// Параметры:
//  РольИсполнителя - СправочникСсылка.бит_РолиИсполнителей.
// 
// Возвращаемое значение:
//  СтрНастройки - Структура.
// 
Функция НастройкиОбъектовАдресации(РольИсполнителя) Экспорт

	СтрНастройки = Новый Структура;
	
	Для каждого СтрокаТаблицы Из РольИсполнителя.ОбъектыАдресации Цикл
	
		НастройкаОбъекта = КонструкторНастройкиОбъектаАдресации(СтрокаТаблицы.Вид, СтрокаТаблицы.Обязательный);
		
		СтрНастройки.Вставить(СтрокаТаблицы.Имя, НастройкаОбъекта);
	
	КонецЦикла; 

	Возврат СтрНастройки;
	
КонецФункции // НастройкиОбъектовАдресации()

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

// Функция инициализирует структуру настройки объкта адресации.
// 
// Параметры:
//  Вид - ПВХ.бит_ВидыОбъектовАдресацииЗадач.
//  Обязательный - Булево.
// 
// Возвращаемое значение:
//  СтрНастройка - Строка.
// 
Функция КонструкторНастройкиОбъектаАдресации(Вид, Обязательный)

	СтрНастройка = Новый Структура;
	СтрНастройка.Вставить("Вид", Вид);
	СтрНастройка.Вставить("Тип", Вид.ТипЗначения);
	СтрНастройка.Вставить("Синоним", Вид.Наименование);
	СтрНастройка.Вставить("Обязательный", Обязательный);

	Возврат СтрНастройка;
	
КонецФункции // КонструкторНастройкиОбъектаАдресации()

#КонецОбласти

#КонецЕсли
