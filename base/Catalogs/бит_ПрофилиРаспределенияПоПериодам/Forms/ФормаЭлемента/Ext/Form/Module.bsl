
#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	// Стандартные действия при создании на сервере.
	бит_РаботаСДиалогамиСервер.ФормаОбъектаПриСозданииНаСервере(Отказ, СтандартнаяОбработка, ЭтотОбъект, Объект);
		
	ТекущийВид 		 = Объект.Вид;
	ТекущаяАналитика = Объект.Аналитика;
	
	УправлениеФормой(ЭтотОбъект);
	УстановитьУсловноеОформление();
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытийЭлементовШапкиФормы

&НаКлиенте
Процедура ВидПриИзменении(Элемент)
	
	Если Объект.Профиль.Количество() > 0 Тогда
		Оповещение = Новый ОписаниеОповещения("ВопросПриИзмененииВидаЗавершение", ЭтотОбъект);
		ТекстСообщения = НСтр("ru = 'При изменении вида табличная часть будет очищена. Продолжить?';
							  |en = 'When changing the type, the tabular section will be cleared. Proceed?'");
		ПоказатьВопрос(Оповещение, ТекстСообщения, РежимДиалогаВопрос.ДаНет, 15, КодВозвратаДиалога.Нет);
	Иначе
		ТекущийВид = Объект.Вид;
		УправлениеФормой(ЭтотОбъект);
	КонецЕсли;
		
КонецПроцедуры

// Процедура обработчик оповещения "ВопросПриИзмененииВидаЗавершение".
// 
// Параметры:
// Ответ - КодВозвратаДиалога
// ДополнительныеДанные - Структура.
// 
&НаКлиенте
Процедура ВопросПриИзмененииВидаЗавершение(Ответ, ДополнительныеПараметры) Экспорт
	
	Если Ответ = КодВозвратаДиалога.Да Тогда
		Объект.Профиль.Очистить();
		ТекущийВид = Объект.Вид;
		УправлениеФормой(ЭтотОбъект);
	Иначе	
		Объект.Вид = ТекущийВид;
	КонецЕсли; 
	
КонецПроцедуры


&НаКлиенте
Процедура АналитикаПриИзменении(Элемент)
	
	Если Объект.Профиль.Количество() > 0 Тогда
		Оповещение = Новый ОписаниеОповещения("ВопросПриИзмененииАналитикиЗавершение", ЭтотОбъект);
		ТекстСообщения = НСтр("ru = 'При изменении аналитики табличная часть будет очищена. Продолжить?';
							  |en = 'When the analytic changes, the table section will be cleared. Proceed?'");
		ПоказатьВопрос(Оповещение, ТекстСообщения, РежимДиалогаВопрос.ДаНет, 15, КодВозвратаДиалога.Нет);
	Иначе
		ТекущаяАналитика = Объект.Аналитика;
	КонецЕсли;
	
КонецПроцедуры

// Процедура обработчик оповещения "ВопросПриИзмененииАналитикиЗавершение".
// 
// Параметры:
// Ответ - КодВозвратаДиалога
// ДополнительныеДанные - Структура.
// 
&НаКлиенте
Процедура ВопросПриИзмененииАналитикиЗавершение(Ответ, ДополнительныеПараметры) Экспорт
	
	Если Ответ = КодВозвратаДиалога.Да Тогда
		Объект.Профиль.Очистить();
		ТекущаяАналитика = Объект.Аналитика;
	Иначе	
		Объект.Аналитика = ТекущаяАналитика;
	КонецЕсли; 
	
КонецПроцедуры

&НаКлиенте
Процедура АналитикаОбработкаВыбора(Элемент, ВыбранноеЗначение, СтандартнаяОбработка)
	
	ТекстСообщения = АналитикаОбработкаВыбораНаСервере(ВыбранноеЗначение);
	Если НЕ ПустаяСтрока(ТекстСообщения) Тогда
		СтандартнаяОбработка = Ложь;
		ПоказатьПредупреждение(, ТекстСообщения,, НСтр("ru='Профиль распределения.';en='distribution profile.'"));
	КонецЕсли; 
		
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытийЭлементовТаблицыФормыПрофиль

&НаКлиенте
Процедура ПрофильПриНачалеРедактирования(Элемент, НоваяСтрока, Копирование)
	
	Если НоваяСтрока Тогда
		КолВоСтрок = Объект.Профиль.Количество();
		Если Не КолВоСтрок = 1 Тогда
			МаксНомерПериода = 0;
			// Поиск максимального номера периода.
			Для Каждого ТекСтрока Из Объект.Профиль Цикл
				Если МаксНомерПериода < ТекСтрока.НомерПериода Тогда
					МаксНомерПериода = ТекСтрока.НомерПериода;
				КонецЕсли;
			КонецЦикла;
			// Установим номер периода.
			Элемент.ТекущиеДанные.НомерПериода = МаксНомерПериода + 1;
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура КомандаРавномерно(Команда)
	
	бит_БюджетированиеКлиент.РавномерныйПрофиль(Объект.Профиль);
	
КонецПроцедуры

&НаКлиенте
Процедура КомандаПодбор(Команда)
	
	ОбъектПодбора 	 = Неопределено;
	ПараметрыПодбора = ПодготовитьПараметрыПодбора();
	
	Если ПараметрыПодбора.ОбъектыПодбора.Количество() > 0 Тогда
		ОбъектПодбора = ПараметрыПодбора.ОбъектыПодбора[0];
	КонецЕсли; 
	
	Если НЕ ОбъектПодбора = Неопределено Тогда
		
		ИмяФормыПодбора = ОбъектПодбора.ИмяКласса + "." + ОбъектПодбора.ИмяОбъекта + ".ФормаВыбора";
		
		ПараметрыФормы = Новый Структура;
		ПараметрыФормы.Вставить("РежимВыбора",			Истина);
		ПараметрыФормы.Вставить("МножественныйВыбор",	Ложь);
		ПараметрыФормы.Вставить("ЗакрыватьПриВыборе",	Ложь);
		Если ОбщегоНазначенияКлиентСервер.СвойствоСтруктуры(ОбъектПодбора, "ОтборПоВладельцу", Ложь) Тогда
			ЗначениеОтбора = Новый Структура("Владелец", Объект.Аналитика);
			ПараметрыФормы.Вставить("Отбор", ЗначениеОтбора);
		КонецЕсли;
		ОткрытьФорму(ИмяФормыПодбора,ПараметрыФормы,ЭтотОбъект,,,,, РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);
	КонецЕсли; 
	  
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

&НаСервере
Функция АналитикаОбработкаВыбораНаСервере(Знач ВыбранноеЗначение)

	ТекстСообщения = "";
	
	// Проверка на примитивные типы
	МассивТипов = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(ВыбранноеЗначение, "ТипЗначения").Типы();
	Для каждого ТекущийТип Из МассивТипов Цикл
		Если НЕ ОбщегоНазначения.ЭтоСсылка(ТекущийТип) Тогда
			Если ПустаяСтрока(ТекстСообщения) Тогда
				ТекстСообщения = СтрШаблон(НСтр("ru = '- %1'; en = '- %1'"), ТекущийТип);
			Иначе	
				ТекстСообщения = СтрШаблон(НСтр("ru = '%1- %2'; en = '%1- %2'"), ТекстСообщения, ТекущийТип);
			КонецЕсли;
		Иначе
			Если ТекущийТип = Тип("СправочникСсылка.бит_ДополнительныеЗначенияАналитик") Тогда
				МассивОтборов = Новый Массив();
				МассивОтборов.Добавить(Новый ПараметрВыбора("Отбор.Владелец", ВыбранноеЗначение));
				ОбщегоНазначенияКлиентСервер.УстановитьСвойствоЭлементаФормы(Элементы, "ПрофильЗначениеАналитики",
					"ПараметрыВыбора", Новый ФиксированныйМассив(МассивОтборов));
			Иначе
				ОбщегоНазначенияКлиентСервер.УстановитьСвойствоЭлементаФормы(Элементы, "ПрофильЗначениеАналитики",
					"ПараметрыВыбора", Новый ФиксированныйМассив(Новый Массив));
			КонецЕсли;
		КонецЕсли;
	КонецЦикла;
	
	Если НЕ ПустаяСтрока(ТекстСообщения) Тогда
		ТекстСообщения = НСтр("ru='Аналитика содежит неподдерживаемые типы:';
							  |en='Analytics sodezhit unsupported types:'") + Символы.ПС + ТекстСообщения; 
	КонецЕсли; 
	
	Возврат ТекстСообщения;
	
КонецФункции

&НаКлиентеНаСервереБезКонтекста 
Процедура УправлениеФормой(Форма)

	Объект 	 = Форма.Объект;
	Элементы = Форма.Элементы;
	
	ЭтоПрофильАналитики = Объект.Вид = ПредопределенноеЗначение("Перечисление.бит_ВидыПрофилейРаспределения.ПоАналитике");
	
	Элементы.Аналитика.Видимость 				= ЭтоПрофильАналитики;
	Элементы.ПрофильЗначениеАналитики.Видимость = ЭтоПрофильАналитики;
	Элементы.ПрофильНомерПериода.Видимость 		= НЕ ЭтоПрофильАналитики;
	Элементы.ПрофильКомандаПодбор.Видимость 	= ЭтоПрофильАналитики;
	
КонецПроцедуры // УправлениеЭлементамиФормы()

// Функция готовит параметры для подбора аналитик.
//
// Возвращаемое значение:
//  Структура.
//
&НаСервере
Функция ПодготовитьПараметрыПодбора()

	РезДанные = Новый Структура;
	РезДанные.Вставить("ОбъектыПодбора", Новый Массив);
	
	Если ЗначениеЗаполнено(Объект.Аналитика) Тогда
		  МассивТипов = Объект.Аналитика.ТипЗначения.Типы();
		  Для каждого ТекТип Из МассивТипов Цикл
		  	   МетаОбъект = Метаданные.НайтиПоТипу(ТекТип);
			   Если НЕ МетаОбъект = Неопределено Тогда
			   	    Имена = бит_ОбщегоНазначенияКлиентСервер.РазобратьПолноеИмяОбъекта(МетаОбъект.ПолноеИмя());
					Имена.Вставить("ОтборПоВладельцу", Ложь);
					Если ТекТип = Тип("СправочникСсылка.бит_ДополнительныеЗначенияАналитик") Тогда
						Имена.ОтборПоВладельцу = Истина;
					КонецЕсли;
					РезДанные.ОбъектыПодбора.Добавить(Имена);
			   КонецЕсли; 
		  КонецЦикла; 
	КонецЕсли; 

	Возврат РезДанные;
	
КонецФункции // ПодготовитьПараметрыПодбора()

&НаКлиенте
Процедура ОбработкаВыбора(ВыбранноеЗначение, ИсточникВыбора)
	
	Если ЗначениеЗаполнено(ВыбранноеЗначение) Тогда
		СтрОтбор = Новый Структура;
		СтрОтбор.Вставить("ЗначениеАналитики", ВыбранноеЗначение);
		
		НайденныеСтроки = Объект.Профиль.НайтиСтроки(СтрОтбор);
		
		Если НайденныеСтроки.Количество() > 0 Тогда
			ТекстСообщения = СтрШаблон(НСтр("ru='Значение %1 уже подобрано.';en='The %1 already matched.'"), ВыбранноеЗначение);
			ОбщегоНазначенияКлиент.СообщитьПользователю(ТекстСообщения); 
		Иначе	
			НоваяСтрока = Объект.Профиль.Добавить();
			НоваяСтрока.ЗначениеАналитики = ВыбранноеЗначение;
			Модифицированность = Истина;
		КонецЕсли; 
	КонецЕсли; 	
	
КонецПроцедуры

&НаСервере
Процедура УстановитьУсловноеОформление()

	УсловноеОформление.Элементы.Очистить();

	ЭлементУО = УсловноеОформление.Элементы.Добавить();
	КомпоновкаДанныхКлиентСервер.ДобавитьОформляемоеПоле(ЭлементУО.Поля, "ПрофильЗначениеАналитики");
	
	ОбщегоНазначенияКлиентСервер.ДобавитьЭлементКомпоновки(ЭлементУО.Отбор,
		"Объект.Аналитика", ВидСравненияКомпоновкиДанных.НеЗаполнено, "");
	
	ЭлементУО.Оформление.УстановитьЗначениеПараметра("ЦветТекста"	 , ЦветаСтиля.бит_ТекстНедоступнойНадписи);
	ЭлементУО.Оформление.УстановитьЗначениеПараметра("Текст"		 , НСтр("ru='<Не заполнена аналитика>';en='<Do not filled analyst>'"));
	ЭлементУО.Оформление.УстановитьЗначениеПараметра("ТолькоПросмотр", Истина);

КонецПроцедуры
  
#КонецОбласти