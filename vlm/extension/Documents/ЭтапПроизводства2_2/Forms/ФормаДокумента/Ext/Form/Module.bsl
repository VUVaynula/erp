
&НаКлиентеНаСервереБезКонтекста
&После("НастроитьЗависимыеЭлементыФормы")
Процедура бит_НастроитьЗависимыеЭлементыФормы(Форма, СписокРеквизитов)
	// {{ПервыйБИТ ЛистТребований [ЛТ5.26]  [VUVaynula@1cbit.ru] [02.12.2025 19:28:18] +        
	// решили скрыть кнопку, все равно в типовую не работают
	Форма.Элементы.ФормаЗаполнитьПоОперациям.Видимость = Ложь;
	// }}ПервыйБИТ ЛистТребований [ЛТ5.26]  [VUVaynula@1cbit.ru] [02.12.2025 19:28:18] -
КонецПроцедуры

&НаСервере
Процедура бит_ПриСозданииНаСервереПосле(Отказ, СтандартнаяОбработка)
	// {{ПервыйБИТ ЛистТребований [исправление после сквозного]  [VUVaynula@1cbit.ru] [16.03.2026 16:13:31] +        
	ДобавляемыеРеквизиты = Новый Массив;
	НовыйРеквизит = Новый РеквизитФормы("бит_КомментарийЗаказаКлиента", Новый ОписаниеТипов("Строка", , Новый КвалификаторыСтроки(1000))); 
	ДобавляемыеРеквизиты.Добавить(НовыйРеквизит);   
	
	ЭтаФорма.ИзменитьРеквизиты(ДобавляемыеРеквизиты);
	
	НовыйЭлемент = ЭтаФорма.Элементы.Добавить("бит_КомментарийЗаказаКлиента", Тип("ПолеФормы"), Элементы.СтраницаОсновное);
	НовыйЭлемент.Вид = ВидПоляФормы.ПолеВвода;
	НовыйЭлемент.ПутьКДанным = "бит_КомментарийЗаказаКлиента";
	НовыйЭлемент.Заголовок = "(бит) Комментарий заказа клиента";
	НовыйЭлемент.МногострочныйРежим = Истина; 
	НовыйЭлемент.ТолькоПросмотр = Истина;
	Если ЗначениеЗаполнено(Объект.НазначениеПродукция) и ТипЗнч(Объект.НазначениеПродукция.Заказ) = Тип("ДокументСсылка.ЗаказКлиента") Тогда
		ЭтаФорма.бит_КомментарийЗаказаКлиента = Объект.НазначениеПродукция.Заказ.Комментарий;
	КонецЕсли;
	// }}ПервыйБИТ ЛистТребований [исправление после сквозного]  [VUVaynula@1cbit.ru] [16.03.2026 16:13:31] -
КонецПроцедуры

