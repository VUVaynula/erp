
&НаСервере
Процедура бит_ПриСозданииНаСервереПосле(Отказ, СтандартнаяОбработка)  
	// {{ПервыйБИТ ЛистТребований [исправления после сквозного]  [VUVaynula@1cbit.ru] [16.03.2026 16:35:30] +   
	СвойстваСписка = ОбщегоНазначения.СтруктураСвойствДинамическогоСписка();

	ТекстЗапроса = СтрЗаменить(Список.ТекстЗапроса, "ВЫБРАТЬ", "ВЫБРАТЬ 
	|	Выбор когда НазначениеПродукция.Заказ Ссылка Документ.ЗаказКлиента Тогда НазначениеПродукция.Заказ.Комментарий Иначе """" Конец КАК бит_КомментарийЗаказаКлиента,");
	
	СвойстваСписка.ТекстЗапроса = ТекстЗапроса;
	СвойстваСписка.ОсновнаяТаблица = Список.ОсновнаяТаблица;
	СвойстваСписка.ДинамическоеСчитываниеДанных = Список.ДинамическоеСчитываниеДанных;
	
	ОбщегоНазначения.УстановитьСвойстваДинамическогоСписка(Элементы.Список, СвойстваСписка);
	       
	НовыйЭлемент = Элементы.Вставить("Списокбит_КомментарийЗаказаКлиента", Тип("ПолеФормы"), Элементы.Список, Неопределено);
	НовыйЭлемент.ПутьКДанным = "Список.бит_КомментарийЗаказаКлиента";
	НовыйЭлемент.Заголовок = "(бит) Комментарий заказа клиента";
	// }}ПервыйБИТ ЛистТребований [исправления после сквозного]  [VUVaynula@1cbit.ru] [16.03.2026 16:35:30] -
КонецПроцедуры
